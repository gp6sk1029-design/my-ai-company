# ブログ部門 MEMORY.md
# 学習・経験の蓄積

> 🔗 関連: [この部門の手順 SKILL.md](SKILL.md) ・ [目次](../目次.md) ・ [全社ルール CLAUDE.md](../CLAUDE.md)

> タスク完了のたびに追記する。効果がなかったパターンは削除する。月1回整理する。

---

## 成功パターン（効果があったもの）

### 記事構成
- キャラクター対話（オオタニ所長×タナカ）は読者の理解度を高める鉄板構成
- 金額換算セクション（年間〇万円の価値）は読者の購買意欲に直結
- 星1レビューの分析 → 「でもこう使えば解決する」の構成が説得力を生む
- 導入文の「数字衝撃型」は離脱率が低い傾向
- **水色アンダーライン装飾（#56CCF2, 2px）で太字に視覚的リズムを作る**（記事605で確立・約40%のboldに付与）

### 技術面
- WordPress REST API（wp_api.py）での投稿自動化が安定稼働
- wp_block_builder.py でJIN:Rブロック構造の自動生成が可能に
- ファクトチェック手順を型化したことで誤情報リスクが低下
- **md_to_html_inline()にJIN:R装飾自動付与を実装（2026/04/21）**。`***重要***` と `**数値を含む**` を自動で水色下線化
- **記事めしPWAにメモ機能実装（2026/04/25）**：アップロード時に「記事タイプ」＋「読者に伝えたいポイント（優先度順）」をDriveの画像フォルダに `PROMPT.md` として自動生成。AIはこれを最優先で読んで記事方針に反映（SKILL.md §0参照）。GAS deployment v13、Cloudflare Pages `blog-capture` に反映済み（2026-04-26）。本番動作確認：savePrompt + getPrompt + パース・既存復元すべて成功
- **GASデプロイは clasp + create-deployment -i &lt;deploymentId&gt; で「URL不変・新版」可能**：clasp push → `clasp create-deployment -i AKfyc...&lt;同ID&gt; -d "v13: ..."` でWeb App URLを変えずに新バージョンを公開できる。Cloudflare PWAのGAS_URL更新が不要で安全

### 前回記事スタイル踏襲チェック（必須フロー・2026/04/21新設）
記事執筆前に以下を必ず実行する：
1. 直前記事のHTMLをREST APIで取得（`?context=edit`）
2. `<strong>` タグ中の装飾（`text-decoration:underline` 等）の採用率を確認
3. 独特な装飾がある場合はMarkdown記法に反映（`***xxx***` 等）
4. 見出し・表・吹き出し・段落の構成パターンも参考にする
→ これを怠ると「記事ごとに装飾・文体がバラバラ」で、ブログ全体の統一感が崩壊する

---

## 失敗パターン（二度と繰り返さないこと）

> 📌 **運用注記（2026-05-31）**：個別の修正生ログは `blog/edits/_log.md`（`edit_logger.py` で記録）。
> 同じ種別タグが**2回以上**出たらこの表へ昇格する。本質的な繰り返しミスは `memory/feedback_blog.md` へ。

| 日付 | 失敗 | 原因 | 対策 |
|---|---|---|---|
| 2026/04 | Garminの発売年を推測で記載 | 公式情報を確認せずに執筆 | 発売日は必ずメーカープレスリリースで確認 |
| 2026/04 | 価格を「5〜6万円台」と曖昧に記述 | 実売価格と定価の混同 | 税込定価を公式サイトから取得 |
| 2026/04 | Venu 2S の発売日を「2021年5月」と誤記 | 予約開始日（5月25日）と発売日（6月1日）を混同 | 「発売日」は公式プレスリリースの発売開始日を採用する |
| 2026/04 | Venu 2S を「ECG 後から対応」と誤記 | Venu 2 Plus と Venu 2S を混同 | ECG対応機種は Garmin 公式の対応モデル一覧でモデルごとに確認する（Venu 2S は現時点で非対応） |
| 2026/04 | 「不満3つ」と宣言したが実際は4項目あった | おまけ項目を追記する際に冒頭の数字を更新し忘れ | 章追加時は必ず冒頭の総数宣言を見直す。Prose Polisherで「宣言数と実数の一致チェック」を必須項目化 |
| 2026/04 | HUAWEI WATCH GT Runner 2 の存在を一度疑ってしまった | 既存知識のみで判断・フォルダ名を信用しなかった | **ユーザーのフォルダ名・入力情報は基本的に正しい前提で動く**。疑う前にWebで直接検索。新製品は知識カットオフ外の可能性が高い |
| 2026/04 | Claude API 画像寸法エラー（2000px超）でセッション失敗 | スマホ縦長写真(1080×2340)等をそのまま会話に投入 | **新しい画像を扱う前に必ず `image_resizer.py` を実行**。長辺1800px以下に統一（200pxマージン） |
| 2026/04 | 画像を添付していないのに2000pxエラーが再発（記事複数作成中） | **Readツールで画像ファイルを読むと自動で会話履歴に埋め込まれる**。1セッションで21回Readした結果、履歴に50枚蓄積 | ①Readで画像を直接読まない ②画像分析はサブエージェントに委譲（Explore/general-purpose）③画像10枚超えたら即 `/compact` ④1記事=1セッション原則 |
| 2026/06 | SwitchBotロックLite記事でスペック値の誤記が2件（取付方法を「両面テープのみ・穴あけ一切なし」=ネジ留め固定も可能なのに断定／オートロックを「1〜180秒」=正しくは1秒〜60分） | 製品の設定範囲・固定方法を実機/公式で確認せず思い込みで記載 | **スペックの数値・選択肢（設定範囲・固定方法・対応規格等）は実機アプリ画面 or メーカー公式で必ず裏取り**。特に「○○のみ」「△△は一切なし」と断定する前に例外がないか確認。ユーザーが実機所有者なら入力情報を正とする（fact-accuracyタグ2回で昇格） |
| 2026/06 | タイトル・見出しが冗長＆煽り語で安っぽい（タイトル末尾「〜される魔法」、見出し「？→ で疑問文と理由を強引接続」） | 情報を詰め込みすぎ＋インパクト狙いの安易な語尾 | **タイトル/見出しは簡潔第一**。①「〜魔法」「〜の真実」等の煽り語尾は使わない ②疑問文＋理由は「？→」でつながず1つの問いか「問い？ 答え」の形に ③タイトルは「製品名＋レビュー＋具体ベネフィット」で製品名先頭固定。長いと感じたら主役1メッセージに絞る（prose-clarityタグ2回で昇格） |
| 2026/06/08 | 記事めしPWAで画像をAI加工→再アップロードしたら、事前に書いた記事メモがPROMPT.mdから消えた（画像役割行だけに置き換わった） | GASのsavePromptは「PROMPT.md削除→ローカルメモで全書き直し」。アップ完了でclearMemoState()がローカルメモを空にするため、その後の再アップで「空メモ＋画像役割行」だけでPROMPT.mdが上書きされた | **「全書き直し型」保存＋「ローカル状態クリア」の組み合わせはデータ消失の温床**。app.jsに二重防御を実装：①役割行の書き直し前にローカルメモが空ならDriveのgetPromptで既存メモを救出してマージ ②記事選択時の自動読込も「置き換え」→「マージ」に変更（書きかけメモを保持）。本番デプロイ・マーカー検証済 |
| 2026/07/18 | ROI表の計算式セルで「\`」記号がそのまま表示（963で発覚→945/908/927にも計14〜8個ずつ露出していた） | md_to_html_inline がバッククォート記法を変換しない仕様なのに、計算式を \`x\` で書く習慣が定着していた | **builderに_strip_backticks追加（\`x\`→x）で恒久修正**＋既公開4記事はWP raw直接置換で除去。読者画面の実物確認（ユーザー指摘）が発見の起点＝公開後も実画面を見る |
| 2026/07/18 | publish_article.py --update で公開記事963が**下書きに戻る**事故（即復旧） | --status の既定値"draft"が更新時も常に送信されていた | **更新時はstatus未指定なら送らない（現状維持）**に修正。『更新系ツールは、明示されない項目を既定値で上書きしない』を設計原則に |
| 2026/07/17 | MaGdget記事、記事めしメモの価格「2,970円」で執筆（タイトル・ROIの根幹に使用）→公開直前の公式裏取りで現価は「3,960円」と判明（旧セール価格が古くなっていた） | 記事めしメモの価格は「撮影/取得時点のセール価格」で、公開時には陳腐化する。実機所有者の入力を正としても、価格は変動が速い | 🔴 **fact-accuracyタグ5回目＝昇格。価格・在庫・URL・肩書き等の"変動が速い項目"は、実機入力があっても公開直前に必ず一次ソース（公式/EC）で再取得**する。特に価格はタイトル・ROIの根幹に入るため、間違うと記事全体が破綻。[[feedback_verify_latest_before_publish]] と同一方針。記事めしの価格メモには取得日を併記する運用も検討 |
| 2026/07/24 | MaGdget記事(945)でアイキャッチが**ヘッダー＋本文先頭に二重表示**（ユーザー指摘） | 既存記事を `--images-dir /tmp/__no_images__`（空）で再公開すると、publish_article.pyの「本文からアイキャッチ除去」は**images-dir内のファイル名照合で判定**するため空フォルダでは効かず、mdの本文先頭に残っていたアイキャッチ`![](...)`行がfeatured_mediaと重複した | 🔴 **空images-dirで既存記事を再公開するときは、mdの本文にアイキャッチ画像`![](...)`行を残さない**（アイキャッチはfeatured_media専用にする）。判定法：本番の`featured_media`のsource_urlが`content.raw`本文にも出現していないかで検査（今回3本中945のみ該当・削除して解消） |
| 2026/07/26 | HUAWEI GT Runner 2記事(703)が **発表時の「市場想定価格」54,780円を定価として全ROI計算**していた（実売46,182円＝約8,600円乖離）。タイトル・日次コスト50円・回収10ヶ月がすべて破綻 | プレスリリースの「市場想定価格」を定価と混同。かつ4月執筆の価格を7月まで再取得していなかった | 🔴 **fact-accuracyタグ6回目＝再昇格。①「市場想定価格」「メーカー希望小売価格」「参考価格」は定価でも実売でもない。ROIの基準は必ず"読者が実際に払う額"＝実売価格にする** ②**価格は必ず取得日を併記**（例「46,182円（Amazon・2026年7月26日時点）」）③記事内の価格出現箇所を `grep -o '<旧価格>'` で数え、全件を潰したか件数で確認する。[[feedback_verify_latest_before_publish]] と同一方針 |
| 2026/07/26 | 703の個人情報対応で、**本文の大会名を消しても画像3枚に大会名が写り込んで残っていた**（さらに1枚は大会名＋日付＋タイム＋距離が同一画面に揃い、大会公式リザルトと突合で氏名特定が可能な状態） | PII除去を「本文テキストの修正」だと捉え、スクリーンショットの中身を検査していなかった | 🔴 **PIIは本文だけ直しても画像に残る。スクショを含む記事は「本文grep」＋「画像の目視監査」を必ずセットで実施する**（画像監査はサブエージェントに委譲＝mainの画像蓄積を防ぐ）。手順：①本文をgrepして0件を確認 ②全掲載画像をサブエージェントで目視監査 ③加工したら**加工後の画像を再度目視検証**（「消えたはず」で公開しない）④加工は「ぼかし」より**トリミングで物理的に切り落とす**方が確実で検証も容易 |
| 2026/07/26 | 703を「4月執筆の本文に7月の新情報を継ぎ足す」方式で改稿したら、**旧記事の主軸（Garmin乗り換え・Suica不満）と新情報の主軸がズレたままつぎはぎになり**、ユーザーから「いろいろ混じっておかしい。最初から作って」と差し戻し | 部分改稿は「古い情報を新しい値に置換する」作業には向くが、**記事の骨子そのものが変わる場合には向かない**。今回は記事めし素材の★イチオシが「GPS精度」「無料の練習プラン」で、旧記事の主軸と別物だった | 🔴 **改稿の前に「骨子が変わるのか、値が変わるだけか」を判定する。骨子が変わるなら部分改稿ではなく最初から書き下ろす**。判定の目安：①元記事が3ヶ月以上前 ②記事めしに新しい注釈画像が追加されている ③新素材の主訴求が元記事のH2構成に無い ── 2つ以上当てはまれば書き下ろし。書き下ろしでも**検証済みの事実・画像・アフィリリンクは資産として引き継ぐ**（捨てない） |
| 2026/07/26 | 記事プレビューを**アプリ内ブラウザに表示せず**、DOM検証（`javascript_tool`）と`curl`だけで「表示OK」と報告した。ユーザーから「ルールでプレビューはブラウザに手直ししやすいように表示させるのでは？」と指摘 | **SKILL.mdのクイックリファレンス#10（2026-07-18制定）に明記があるのに、参照せず記憶で作業した**。DOM検証で「壊れていない」ことは示せるため、確認できたつもりになっていた | 🔴 **プレビューの目的は"検証"ではなく"ユーザーが推敲するため"。まず `preview_start` でBrowserペインに開き、スクリーンショットで描画を確かめてから報告する**。DOM検証・curl・grepは代替ではなく追加（壊れ検出用）。根本対処として**作業開始前にSKILL.mdのクイックリファレンス12項目を実際に読み直す**（記憶で進めない）。→ memory `feedback_preview_in_browser` に昇格 |
| 2026/07/26 | 記事めしの `PROMPT.md` に列挙されたセクション画像7枚だけを見て執筆を始めたが、**フォルダには注釈済み画像が12枚あり5枚見落としていた**。しかも見落とし分に**★イチオシ指定が2つ**（＝筆者の主訴求）含まれていた | PROMPT.mdのメモ列挙と、フォルダの実ファイル一覧が一致していなかった（メモ保存後に画像を追加すると起きる） | **`article_from_meshi.py` が出す context.md の「🖼 画像一覧（役割別）」を必ず突き合わせる**。メモの列挙だけを信用しない。加えて**注釈画像の見出し文言そのものが筆者の主訴求**（例「実際のGPSの精度がかなりいい」「★イチオシ」バッジ）なので、画像の中身を読んでから記事の骨子を決める |
| 2026/07/26 | 703で**画像2枚がキャプションと中身不一致**（`screenshot_vip`＝実際は開封写真／`screenshot_plan`＝実際は心拍チャート）。「VIPが有料である証拠」として無関係な写真が貼られていた | ファイル名を信じて配置し、画像の中身を確認していなかった | **画像を「主張の根拠」として貼るときは、必ず中身を見て根拠として成立するか確認する**。ファイル名は当てにならない。簡易判定：スクショと写真は**アスペクト比・解像度が違う**（当ブログはスクショ=1080幅系・写真=1200幅系）ので `PIL` でサイズを出すだけでも取り違えを検知できる |
| 2026/07/26 | 552（MX ERGO S持ち運び）で、**マウス本体価格が冒頭まとめ18,000円／ROI表15,000円と記事内で2通りに割れていた**（実売17,800円）。さらにROIの土台「ケース約2,000円＋アダプタ約1,500円＝3,500円」が実売とズレ、**損益分岐日数も保険ROI倍率も全部誤り**になっていた | 冒頭の3行まとめとROIセクションは**執筆タイミングが別**なので、片方だけ直すと必ず割れる。人間の目視では離れた2箇所の数字比較を見落とす。`fact-accuracy` タグは**7回目** | 🔴 **公開・改稿の最後に記事全文へ `grep -oE '[0-9][0-9,]{2,}円'` をかけ、同じ商品の価格が2通り出ていないか機械的に確認する**。ROIは「土台価格→損益分岐→倍率→言い回し（『1ヶ月で元が取れる』等）」の依存順に全部引き直す。価格には**取得時点を明記**。置換スクリプトは `assert ct.count(old)==n` で想定件数不一致なら落とす（silent fail防止）。→ memory `feedback_blog_price_consistency` に昇格 |
| 2026/07/26 | 552の楽天リンク作成で、検索上位のWERJIA収納ケースが**商品名は完全一致なのにサイズが「大きい」2,459円**で、レビュー対象のAmazon品「小さい」1,799円とは**別バリエーション**だった | 楽天は**同一商品名で複数の別物が並ぶ**。既知の罠（楽天メイン画像が別商品）と同じ構造 | **楽天商品ページのバリエーション選択欄（size/color/個数）と商品名末尾の括弧書きを必ず読み、Amazon側の括弧書きと1対1で照合する**。バリエーションを選べるショップを優先。価格がAmazonと±30%超ズレたらバリエーション違いを疑う。メーカー公式ストアがあれば最優先。→ memory `feedback_rakuten_link_variation` に昇格 |
| 2026/07/26 | 552のアフィリ商品で、**記事に書かれていたASINが実機と違う商品だった**（変換アダプタ）。ユーザーから実機の商品リンクを提示されて発覚 | 既存記事に載っているASINを「検証済み」と暗黙に信用し、実機と一致するか確認しなかった。**過去記事のASINは過去の自分が間違えている可能性がある** | 🔴 **既存記事のアフィリを刷新するときは、ASINを引き継ぐ前に「記事の説明・掲載写真と商品ページが一致するか」を必ず確認する**。特に変換アダプタ・ケーブル類は**端子の向き（オス/メス）と規格**が1文字違いで別物になる。疑わしければユーザーに実機の商品リンクを聞く |
| 2026/07/26 | WordPress REST が403（WAF遮断）を返した際、**5回リトライ＋合計450秒のバックオフを自動で回した**。解除されずユーザーがWAFをOFFにする事態に。さらに教訓を記録する前、同日の旧メディア削除でも**403をループで4回受けた**（1件目で止めるべきだった） | fail2ban系WAFは**遮断中のリクエストをさらに違反としてカウントしブロックを延長する**。リトライは逆効果 | 🔴 **403を受けたらその場で処理を止める。ループなら即break。リトライ回数を1回も設定しない**。止めた事実をユーザーに即報告し判断を仰ぐ。遮断対象の実測：`POST /media`（連続で遮断）・`PUT`/`DELETE`（常時403）／通る：`GET`・`POST /posts/<id>`。**メディア削除はWAFを一時OFFにしてもらうか、wp-adminで手作業**。消す前に必ずローカル（scratchpad。PIIはリポジトリに置かない）へバックアップし、どの記事にも未使用であることを確認する。→ memory `feedback_waf_403_no_retry` に昇格 |
| 2026/08/02 | 記事めしPWAの既存 `config.js` に共有トークンが直接記載され、ブラウザへ配信される状態を公開前確認で発見 | GASを直接呼ぶ旧設計で、ブラウザ側に認証値を置かないと動かない構造になっている | **解消済み**：漏えい済み扱いで新しいランダム値へローテーション。ブラウザから撤去し、Cloudflare側はSecret `GAS_SHARED_TOKEN`、GAS側はScript Propertiesで保持。Access認証＋Worker内JWT署名/AUD/issuer/期限検証後だけ、同一オリジン`/api/gas`がGASへ認証値を付与する構成に変更。旧Pagesプロジェクトと過去デプロイ138件を削除 |
| 2026/07/27 | Keychron K1 Max 設定編（836）で、ROIの土台「実勢価格25,000円」が**実売19,360円**とズレていた。損益分岐日数・3年純利益・ROI倍率・時給950円換算の回収倍率の**4つすべてが誤り**。しかも同じ価格が**3箇所に分散**（ROI表／時給950円の段／まとめ直前の締め）していて1箇所直しただけでは終わらなかった。`fact-accuracy` **8回目** | **価格は書いた時点で正しくても時間が経てば古くなる**。今回は値**下がり**だったのでROIが実際より不利に書かれていた＝「高く書いてしまう誤り」だけを警戒していると見逃す | 🔴 **アフィリリンクの追加・刷新タスクは、必ず価格の取り直しとセットで行う**（リンクだけ新しくして価格が古いと、読者はクリック先で違う金額を見る）。取り直したら `grep -oE '[0-9][0-9,]{2,}円\|[0-9.]+万円'` で全出現を洗い、**「◯倍」「◯日で元が取れる」「◯ヶ月」といった計算結果の言い回しまで**引き直す（数字だけ直して言い回しが残るのが典型的な取りこぼし）。→ memory `feedback_blog_price_consistency` に追記 |

---

## 記事台帳（復帰時の参照台帳）

| # | ファイル | タイトル | WP投稿ID | 公開URL | 公開日 | 状態 |
|---|---|---|---|---|---|---|
| 1 | garmin-venu2s-review.md | Garmin Venu 2S を4年半使ったリアルレビュー | **605** | https://www.ootanisatan.com/?p=605 | 2026/03/29 | **公開**（🔴mdは画像ゼロ＝本番と非同期。2026/07/23に**非破壊追記**でアフィリカード設置：Amazon B094VVQR6C＋楽天 a.r10.to/hPx5BI＋PR表記。本番の画像・装飾は保持）／**2026/07/30 後継機を掲載**：h2「今から買うなら Venu 3 か Venu 4 か」に作り替え、**Venu 3（Amazon B0CGJDW7T6・47,691円／楽天 a.r10.to/hPo09t）**と**Venu 4（Amazon B0FS1BXD9Z・69,804円／楽天 a.r10.to/hPCOQf）**のカードを追加（計4枚）。3列比較表＋実売価格行に更新し、まとめの「Venu 2S は今でも有力な選択肢」を現行ラインナップ案内に修正。**Venu 3/4 は未所有のためスペック比較に限定する旨を本文に明記**） |
| 2 | huawei-gt-runner2-review.md | 【10km実走データ】HUAWEI GT Runner 2 | 703 | https://www.ootanisatan.com/?p=703 | 2026/04/21（公開 2026/07/23） | **公開**（2026/07/23に下書き→公開。🔴mdは画像ゼロ＝本番と非同期→**非破壊追記**でアフィリカード：Amazon B0GR2GM467＋楽天 a.r10.to/h5kzLz＋PR表記） |（**2026/07/26 全面書き下ろし＋PII対応**：本文から大会名・日付・タイムを除去し画像9枚を差し替え・media 1000/1001/1004-1010。**PIIが写り込んでいた旧メディア728・729・733・734は2026/07/26に削除済み**＝URL直打ちでも404を確認。削除前のバックアップは scratchpad/wp_media_backup へ退避（PIIのためリポジトリには置かない）。アフィリ：Amazon B0GR2GM467＋楽天 a.r10.to/h5kzLz）
| 3 | keychron-k1max-jis-setup-guide.md | Keychron K1 Max 設定編｜1台で4配列を切替する完全ガイド｜年21万円の時短 | 836 | https://www.ootanisatan.com/?p=836 | 2026/05/11 | **公開**（カテゴリ：ガジェット研究室+時短ツール研究室／アイキャッチmedia_id=837） |（**2026/07/27 アフィリ一新**：7/23の全記事標準化で「設定ガイドは対象外」として飛ばしていたぶんを対応。`:::product`カード**2枚**設置（ROI結論の直後＋関連リンク）。Amazon B0FHWX7X7K ¥19,360 ★3.8/12件＋**楽天 Keychron公式（国内総代理店コペックジャパン）`a.r10.to/hYLZNi`**。🔴**価格・ROI全面修正**：記事は「実勢価格25,000円」だったが実売19,360円→損益分岐 約30日→**約23日**、3年純利益 60万→**61万円**、ROI 24倍→**32倍**、時給950円換算の初年度回収 2.7倍→**約3.5倍**に引き直し。**楽天公式はカラー/LED/配列/軸の4項目を選べてブラックのJISも買える**（AmazonのJIS版はレトロカラー1種のみ）ため、その差をカード直前に明記＝楽天を貼る実利がある例。PR表記をAmazon＋楽天に更新）
| 4 | mx-ergo-s-settings-guide.md | MX ERGO S 設定編｜Logi Options+ で年6万円の時短を生むカスタマイズ術 | 873 | https://www.ootanisatan.com/mx-ergo-s-settings-guide/ | 2026/05/17 | **公開**（カテゴリ：ガジェット研究室+時短ツール研究室／アイキャッチmedia_id=877 ChatGPT生成版／実機スクショ7枚埋込／時給950円基準ROI） |
| 5 | switchbot-lock-lite-review.md | SwitchBot ロックLite レビュー｜賃貸OK、鍵を持ち歩かない生活へ | **908** | https://www.ootanisatan.com/switchbot-lock-lite-review/ | 2026/05/26（最終更新 2026/06/02） | **公開**（カテゴリ：ガジェット研究室+時短ツール研究室／2026/06/02にローカルプレビュー見ながら大幅推敲：タイトル刷新（「魔法」等の煽り削除）・オートロックをタイマー式に事実修正・ROI数値統一（時給950円で約8.5ヶ月回収・5年約+8万円）・専門用語平易化・章重複解消／**Amazonアフィリリンク2本追加**（本体B0D3QT5RP2・電池PanasonicB08RC7DF53、tag=gp6sk1029-22）＋PR表記／タナカ吹き出しを低画質ドヤ顔→高画質ニヤ顔に差替／Markdown残骸0件） |
| 6 | braun-clean-renew-compatible-review.md | ブラウン シェーバー洗浄液 互換品コスパ検証｜純正の約半額「シェーバークリーンNEW X」を実機レビュー | **927** | https://www.ootanisatan.com/braun-clean-renew-compatible-review/ | 2026/06/21 | **公開**（カテゴリ：暮らしハック研究室 id=5／アイキャッチmedia_id=922・本文画像4枚 media923-926＝生活感のある実物写真採用／互換NEW X・クリンニュ・純正CCRの3軸コスパ比較＋ROI（互換 約2.0円/mL＝純正の約半額・年約3,400円節約）／実体験反映：香りは純正が長持ち（互換は一度使うと無臭）・水分残りで乾燥遅延＆オジサン臭→天日干し対策・容器2個ローテ／**アフィリ3本**＝NEW X(amzn.to/4w1RgM3=B0CW1PYL1G)・クリンニュ(B01KZEYAI8 1本)・純正(B081MS3XYM CCR5CR)全てtag=gp6sk1029-22＋PR表記／Markdown残骸0・表情記法露出0・水色UL60） |
| 7 | magdget-shoulder-strap-review.md | MaGdget スマホショルダーストラップ レビュー｜3,960円で「手ぶら」と「スマホ落下・置き忘れ」を同時に解決 | **945** | https://www.ootanisatan.com/magdget-shoulder-strap-review/ | 2026/07/17 | **公開**（カテゴリ：ガジェット研究室 id=1＋暮らしハック研究室 id=5／アイキャッチmedia_id=941・本文画像3枚 media942-944＝同梱物一覧/パーツ接写/着用シーン／記事めしPWA素材から `article_from_meshi.py`→画像はサブエージェント分析でキャプション確定／**公開前fact-checkで価格修正**：メモ2,970円→公式現価3,960円に統一しタイトル・ROI全再計算（時給950円で約10.7ヶ月回収・3年+9,400円）／画像分析で判明した事実追加：フォンテザー2枚付属・「決まった角度からしか外れず盗難に強い」／生産技術の"探すムダ/ポカヨケ"＋損失予防（画面割れ期待値）を軸／Amazon該当ASIN未確認のため公式リンクのみ・アフィリなし → **2026/07/21にASIN判明: B0GHMZMM7Q**（商品名『[MaGdget] Shoulder Strap/マジェット ショルダー ストラップ』・価格3,960円で記事記載と一致・★4.6/28件）。DORIBIRU記事から相互リンク＋アフィリリンクを設置済み。**この記事自体にもアフィリリンクを追加する余地あり（未対応）**。⚠️2026/07/21時点で在庫切れ・予約受付中（発売予定7/30）／吹き出し8個・Markdown残骸0・表情露出0・水色UL120／公開はscratchpadのpublish_magdget.py＝画像upload→wp:image化→markdown_to_blocks→POST /posts） |

| 8 | braun-ccr2-vs-ccr4.md | ブラウン洗浄液 CCR2とCCR4はどっちがお得？｜1個あたり209円差・全パック単価を徹底比較 | **963** | https://www.ootanisatan.com/braun-ccr2-vs-ccr4/ | 2026/07/18 | **公開**（カテゴリ：暮らしハック id=5／GSC分析で発見した「1位クエリ braun ccr2 vs ccr4・クリック0」を刈り取る戦略記事。中身同一・単価勝負の構図で CCR2=709円/個 vs CCR4=500円/個(B0GWLTTPD6 ¥1,998) を軸に CCR6/10/12 全パック比較（CCR10=449円/個が最安・B0DYJVB1XZ）／ROI：毎日派はCCR2→CCR4で年約2,500円・5年12,500円差／アイキャッチ・本文画像は927のWP media(923/924)を再利用＝撮影ゼロ／**アフィリ4本**(CCR4/CCR10/CCR12/CCR2・tag=gp6sk1029-22)＋PR表記／927と相互リンク（ハブ&スポーク）／publish_article.pyで公開→ホームカード自動同期の初実戦・正常動作） |

| 9 | doribiru-coin-case-review.md | DORIBIRU 小銭入れ レビュー｜レジで小銭を探すムダが消えるL字ファスナー薄型財布 | **992** | https://www.ootanisatan.com/doribiru-coin-case-review/ | 2026/07/23 | **公開**（カテゴリ：暮らしハック id=5／画像9枚upload=media983-991・アイキャッチmedia_id自動／Amazon B0FH7FW9K6 価格1,580円・★4.2/98件を一次情報で再確認・競合比較セクションあり／**:::productカード2枚**＝DORIBIRU本体(Amazon B0FH7FW9K6＋楽天hb.afl フルURL・画像Amazon公式81dhz7g6juL)＋イチオシ内MaGdget(Amazon B0GHMZMM7Q＋楽天a.r10.to/hY9L5o・画像518UOvIc)／公開と同時にホームカード#1同期） |

| 10 | （mdなし・本番のみ） | MX ERGO Sの持ち運び問題を完全解決！収納ケース＆変換アダプタの最強セット | **552** | https://www.ootanisatan.com/?p=552 | 2026/02/25（最終更新 2026/07/26） | **公開**（🔴ローカルmdなし＝本番`content.raw`が正本。2026/07/23の全記事アフィリ標準化から**漏れていた**ため2026/07/26に対応：`:::product`カード**5枚**設置（インライン2＝解決策①末尾にケース／解決策②末尾にアダプタ、関連リンクに3枚）。**ASIN**：ケース B0DHCPVHQX ¥1,799 ★4.8/6件・アダプタ **B0CGHP27ML** ¥1,299 ★4.2/2,354件・本体 B0DC5X912P ¥17,800 ★4.4/1,046件。⚠️**記事に元々載っていたアダプタASIN B074PQPLDQ は実機と別商品**でユーザー指摘により判明→差し替え済み。**価格・ROI全面修正**：本体価格が冒頭18,000円／ROI表15,000円と割れていたのを17,800円に統一、ROI土台を実売合計3,098円に直し損益分岐 約31日・保険ROI 約5.7倍へ再計算。PR表記を「Amazonアソシエイト・楽天アフィリエイト」に更新）

| 11 | serena-c26-tire-16inch-mp7-rv505.md | セレナC26タイヤ交換｜楽天総額59,680円、店舗との差を比較 | **1110** | https://www.ootanisatan.com/serena-c26-tire-16inch-mp7-rv505/ | 2026/08/22 | **公開**（カテゴリ：暮らしハック研究室 id=5／本人撮影の実車写真を使ったアイキャッチv4／店舗見積75,600円と楽天側57,280〜62,180円を、本体・交換チケット・ゴムバルブ・廃タイヤ処理まで含めて比較／同条件59,680円・差額15,920円／楽天市場の195/60R16横断検索アフィリエイトカードを結論直後とまとめの2か所に配置／交換チケットはタイヤと同一ショップで購入する注意点を明記／SNSキュー作成済み） |

> **📌 2026-07-23 アフィリエイト掲載を全レビュー記事に標準化（SKILL新標準`:::product`カードの一斉適用）**
> SKILL.mdに「💰アフィリエイト掲載の標準」を新設（Amazon＋楽天2ボタン＋**ASIN一致のAmazon公式画像**＋PR表記）。既存レビュー全7記事に楽天リンク＋公式画像カードを設置し本番反映。
> **生成済み楽天アフィリ短縮リンク（再生成しないこと）**：CCR4→`a.r10.to/hP6YmI`／シェーバークリーンNEW X(互換)→`a.r10.to/hH7t4T`／SwitchBotロックLite→`a.r10.to/hPkAEz`／HUAWEI GT Runner 2→`a.r10.to/h5kzLz`／Garmin Venu 2S→`a.r10.to/hPx5BI`／MaGdget→`a.r10.to/hY9L5o`／DORIBIRU本体→hb.afl フルURL／WERJIA収納ケース→`a.r10.to/h5m2Ha`（ファミラ・大きい/小さい選択可）／UGREEN変換アダプタ2点セット→`a.r10.to/hkSDmR`（UGREEN公式店・¥1,299）／ロジクール MX ERGO S→`a.r10.to/hg5XXN`（ロジクール公式店・¥17,800）。※`a.r10.to/hg6j7X`（UGREEN OTG 2個セット）は**誤商品のため未使用**。／Keychron K1 Max→`a.r10.to/hYLZNi`（Keychron公式・国内総代理店・19,360円〜・色/LED/配列/軸を選択可）／**Garmin Venu 3 45mm Black/Slate→`a.r10.to/hPo09t`（ガーミン公式楽天市場店・型番010-02784-41・54,720円）**／**Garmin Venu 4 45mm Slate/Black→`a.r10.to/hPCOQf`（Joshin web・型番010-03014-30・77,560円。※ガーミン公式店にVenu 4の取扱なし・AMI楽天は「在庫確保でき次第」表示のため大手を採用）**
> **リンクの流用実績（2026-07-30）**：`hYLZNi` は記事836に加え**450**でも使用／`hg5XXN`（MX ERGO S）は記事552に加え**526・873**でも使用。**同一商品なら再生成しない**（生成のたびに別リンクが増えると計測が分散する）。
> **公開方式の教訓**：mdが本番と同期している記事（963/908/945）は`publish_article.py --update`で安全再公開。**mdが本番と非同期（画像がDriveサムネや空）の記事（927/605/703）は再公開すると本番の画像・装飾が消えるため、`content.raw`を取得しカードだけ末尾追記する非破壊方式**（scratchpad/append_product_card.py）を使う。楽天画像は別商品(ブランド共通)の罠あり→[[feedback_affiliate_card_image]]。設定ガイド2本(keychron836/mx-ergo873)は今回対象外。

> **台帳メンテナンスルール**
> - 新規公開時に1行追加（必須）
> - WP投稿ID・URL・公開日を記録
> - ID・URL不明の場合は `-` でOK（`article_status.py` で後から検索可能）
> - `python3 blog/scripts/article_status.py <キーワード>` で曖昧検索可能

---

## 記事別の振り返り記録

### 記事1: Garmin Venu 2S レビュー
- 日付：2026年4月
- ジャンル：ガジェット（スマートウォッチ）
- ファイル：articles/garmin-venu2s-review.md
- 学び：
  - スペック検証でモデル間の混同が起きやすい → 型番ごとに仕様を確認する
  - バッテリー持続時間は測定条件（スマートウォッチモード等）を明記する
  - 複数モデルがある製品は比較表を入れると読者に親切

### 記事2: HUAWEI WATCH GT Runner 2 レビュー
- 日付：2026年4月21日
- ジャンル：ガジェット（スマートウォッチ・ランナー特化）
- ファイル：articles/huawei-gt-runner2-review.md
- WordPress記事ID：703（下書き）
- 使用画像：8枚（アイキャッチ：装着写真、本文：本体・裏面・実走10km詳細・フォーム分析・AI設定・VIP表示・ウォーキングルート）
- 学び：
  - **記事1との連続性（乗り換え視点）が差別化の最大軸**。前機種を所有している読者は自然に記事2に誘導できる
  - **実走データ（宍粟さつきマラソン10km）の公開**は他レビュー記事にない独自性。数値データはスクショ付きで信頼性が跳ね上がる
  - **ランナー向けガジェットの4軸評価は再解釈が必要**（時短→練習効率化、効率化→フォーム改善、費用対効果→ジム/トレーナー代比較、再現性→初心者でも装着するだけでデータ取れる）
  - **Suica非対応・Body Battery不在を正直に書く**ことで星1レビュー対策になる。Garmin比較視点だとこの弱点が鮮明
  - **ファクトチェッカーの効果が絶大**：Venu 2S の発売日誤記・ECG 誤記を公開前に発見して修正
  - **SiteGuard Liteが複数画像連続アップで弾く**ことがある：レート制限は数分待つか完全別名ファイル名で再試行
  - **マラソン大会名（宍粟さつきマラソン）**のような固有名詞を入れると地域SEOに効く可能性がある
- 要追加確認：AIフィットネス計画のVIP料金（記事内では「アプリ内表示」としてぼかした）
- Venu 2S記事にも波及要修正：**記事ID 605の比較表でECG欄の修正が必要**（✅ → ❌ or 「非対応」）
- **🚨 重大発覚（2026/04/21）: 記事1（605）で手動で付けていた水色アンダーライン装飾（#56CCF2）が、記事2（703）では完全に欠落（73/73のstrongが装飾なし）**
  - 原因：wp_block_builder.py の md_to_html_inline() が単純bold変換のみ、JIN:R装飾を付与するロジックがなかった
  - 対策：builder に自動装飾機能を実装
    - `***xxx***` → 水色アンダーライン付きstrong
    - `**xxx**` 内に数値・単位・日付を含む → 自動で水色アンダーライン
  - 教訓：**前回記事のスタイルは毎回確認すべき**。目視確認せず自動変換に任せると品質が退化する
- **画像問題（2026/04/21）**：装着写真に太腿・Tシャツ・ケーブルが映り込みでブログ画像として汚い。撮影時の注意＋AI後処理（Gemini背景クリーンアップ）が必要

---

## ブログ運営の開発履歴

### 全社ルール改定の記録
- 2026/04/27（CPO就任時）: **AI操作の優先順位を Claude in Chrome 優先に転換**。global_rules/CLAUDE_global.md と CLAUDE.md（プロジェクト）に追記。理由：API利用料削減＋学習一貫性＋デバッグ容易性＋OAuth再利用。実証済みの成功事例として「JIN:R吹き出しスロット10枚一括切替（手動15分→自動2分）」がある
- 2026/05/04: 上記ルールを「**一択**」から「**Claude in Chrome優先・API は適材適所**」に補正。判断フロー3問（①ブラウザで見ながら？②裏で動かす？③エンドユーザー向け？）を追加。PWA内蔵AIや大量バッチ処理ではAPIを積極使用してOKと明確化
- 2026/05/04: **デザイン・画像制作の標準フローを制定**。①ChatGPT/Gemini画像生成 → ②Canva仕上げ → ③image_resizer.pyリサイズ の3段階。AI生成画像をそのまま使わずCanvaで必ず仕上げる。global_rules/CLAUDE_global.md とプロジェクトCLAUDE.mdに追記
- 2026/05/04: 上記補正。「Canvaだけで完結NG」を削除し、「**Canva単体で目的達成できる場合はAI生成をスキップしてOK**」に変更。シンプルな図解・告知バナー・SNSサムネ等はテンプレで間に合う場合がある。ROI観点で最速ルートを選ぶことを優先
- 2026/08/02: **Canvaでの最終仕上げを必須から任意へ変更**。ブログ・SNSとも、ユーザーが必要と判断した場合のみCanvaで編集する。不要と判断したAI生成画像はそのまま使ってよいが、画像サイズ・内容の正確さ・公開先のルール確認は引き続き必須
- 2026/08/03: **記事めしPWAのAI接続先をGoogle Driveで端末間同期**。`記事めし_AI接続先設定.json`にChatGPTプロジェクトURL／Gemini Gem URL／SNS統括PDMのCodexタスクIDを保存し、PC・スマホの起動時に読み込む。Mac固有のCodexワークスペースパス・フォルダ権限と、認証トークン／APIキーは同期対象にしない
- 2026/08/03: **記事めしPWAのCodex画像生成を既存SNS統括PDMへ接続**。公式の`codex://threads/<task-id>`で既存のローカルタスクを開き、画像生成の指示はクリップボードへコピーする。SNSセッション更新後は、設定欄のタスクIDを新しいSNS統括PDMへ変更してGoogle Driveへ保存する
- 2026/08/03: **記事めし→SNS統括PDMの画像ジョブ指示を正常化**。既存SNSセッションへ毎回「SNS統括として動作」「役割キーsns」「CLAUDE/SKILL/MEMORYを読む」「handover実行」を貼っていたため、画像作業と無関係なセッション初期化が混入していた。これらを全削除し、`$imagegen`＋ジョブJSON＋編集元画像の絶対パス／Markdown参照＋保存条件だけの短い依頼へ変更。編集画像は`view_image`で自動読込するのを標準とし、実画像をチャットへ貼りたい場合だけ使う「🖼 編集元画像をコピー」を追加。**学び：既存の役割セッションへ渡す作業依頼に、役割宣言や引き継ぎルールを再注入しない。ジョブ固有情報だけを渡す**
- 2026/08/03: **記事めしのCodex画像受け渡しを実添付方式へ再修正**。ローカルパスとMarkdown参照だけではCodex入力欄へ画像は添付されないため、既存SNSタスクを開く方式を廃止。画像生成専用の新規Codexタスクを指示入力済みで開き、`mode=edit`では編集元PNGをクリップボードへ画像データとして自動コピーする。ユーザーは入力欄で`⌘V`して画像表示を確認後に送信する。設定画面のSNSタスクID欄も撤去。**学び：ファイルパスの参照とチャット添付は別物。ディープリンクで渡せないバイナリは、貼り付け直前まで自動化して残操作を明示する**
- 2026/08/03: **Codex画像ジョブの受け渡し文だけを短文化**。選択した用途別の高品質プロンプト全文は、従来どおりジョブJSONの`prompt`へ保存してCodexにも渡す。一方、Codexを開くための受け渡し文からはMarkdown画像参照、ワイルドカード、セッション関連の文言を排除する。**学び：良いテンプレート本文は削らず、運搬用メッセージだけを簡潔にする**。
- 2026/08/03: **ChatGPT／Geminiでも高品質プロンプト全文を維持**。長文テンプレートをURLに入れる際の自動要約が、詳細指示を欠落させていた。ChatGPT／Geminiを開く前に全文をクリップボードへコピーし、入力欄で貼り付ける運用へ統一。高品質プロンプト一覧の全選択肢と本文は維持する。
- 2026/08/03: **Codex画像編集は記事めしで内容を調整してから送信**。画像カードでCodexを選んだ直後にジョブを開始すると、ユーザーが高品質プロンプトを選択・編集する前にCodexが開いてしまう。Codex選択は対象画像を記事めしの編集欄へセットするだけにし、明示的な「Codexで画像生成」で初めてジョブを作る。
- 2026/08/05: **一時保存と既存ファイルの編集操作を統一**。一時保存だけ鉛筆ボタン→サービス選択画面となっていたため、既存ファイル一覧と同じくChatGPT／Gemini／Canva／Codex／別記事への直接ボタンをカードに表示。用途もラベル付きボタンへ統一した。
- 2026/08/06: **Google Drive接続待ちをキャッシュ＋自動再接続で解消**。起動時にGASの記事一覧取得を待つ方式から、前回の正常取得結果を即表示して裏側で更新する方式へ変更。失敗時は15秒後・ネット復帰時に再試行し、接続状態を記事選択欄の下に常時表示する。
- 2026/08/06: **失敗パターン：GASへのbase64一括転送は禁止**。高速化目的で最大6枚を1リクエストへまとめたが、実測は2件15.6秒で、従来の個別転送1件5.7秒より遅く、実画像では「全然転送できない」状態になった。原因は巨大なフォーム本文のbase64解析とDrive逐次作成が残り、往復削減より処理負荷が上回ったこと。即日撤去し、個別転送へ完全復旧。今後の高速化は画像本体をGASへ通さない直接アップロード方式だけを検討する。
- 2026/08/03: **ChatGPTプロジェクトURLを最優先で開くよう記事めしPWAを修正**。従来はプロンプト自動入力のため`?q=`付きの通常チャットへ強制移動しており、設定済みプロジェクトを開けなかった。保存URLがある場合はそのまま開き、プロンプトはクリップボード経由で渡す。画像編集・商品リサーチも同じ挙動に統一

### システム構築（2026/03/30〜）
- 2026/03/30: my-ai-companyシステム初期構成（CLAUDE.md + blog/agents/9名 + scripts/）
- 2026/04/01: CLAUDE.md刷新、全エージェント定義を更新
- 2026/04/14: 9エージェントの知見をSKILL.mdに統合、MEMORY.md新設
- 2026/05/02: SNS部門新設（`sns/`）でハブ&スポーク戦略採用。`blog/SKILL.md` §12をsnsへ移管
- 2026/05/02: コノハWING→Cloudflare移設プラン詳細書を作成 → `blog/migration/cloudflare-migration-plan.md`（エンジニア相談用・着手前段階）
- 2026/05/02: Cloudflareプレビューサイト構築（案A・自動）→ https://ootanisatan-preview.pages.dev 稼働。`blog/cloudflare-preview/` 配下に build.py + JIN:R風CSS + 静的HTML。noindex+robots.txtで検索除外。本番無影響。所要約30分
- 2026/05/02: 上記プレビューを「本物サイトミラー」に切替（mirror.py 追加）。本番ootanisatan.comから HTML/JIN:R CSS×7/画像×23/jin-iconsフォントを取得しCloudflareへ転載。**本物JIN:Rが生成したHTMLそのまま**を再現（90KB級・1:1の見た目）。noindex/noarchive設定済
- 2026/05/02: ミラーをサイトマップベースに拡張。**全16ページ**（ホーム＋記事5＋固定ページ9＋sitemap.html）を一括ミラー。49画像＋9 CSS/フォント。内部リンクをCloudflare内相対化したので**クリックで全ページ巡回可能**。本物の機能を持つ静的レプリカ完成
- 2026/05/02: customize.py 追加。プレビュー版ホームに「★注目の記事3件＋★最新の記事5件」セクションを注入し、不足5記事はダミー（Coming Soonスタブ）に置換。本物には影響なし。スクショ通りの見た目を実現
- 2026/05/02: ホーム全面リビルド（customize.py を本物ミラー注入型→自前完全構築型に切替）。ヒーロー＋4カテゴリ＋注目記事＋最新記事＋サイドバー＋ボトム特徴の全要素をスクショ準拠で再現
- 2026/05/03: ヒーローを ChatGPT生成のPTGLバナー画像（2172×724）背景型に切替。装飾（ロボットアーム・ギア・回路パターン・ビル・チャート・吹き出し付きキャラ）を1枚で実現。`blog/images/characters/PTGL-hero-banner.png` に永続保存
- 2026/05/03: キャラクター表情画像11種をGoogle Drive `【ブログ全体】共通素材・キャラクター/キャラクター表情画像/` から強制再同期。md5一致確認済。プレビューにも全表情を配信（オオタニ所長：通常/ドヤ顔/悩む/恥ずかしい/焦り/悲しい / 新人タナカ：正常/ドヤ顔/ニヤ顔/驚き/絶望顔）。今後の吹き出し・記事内表現で表情使い分けが可能に
- 2026/05/03: ↑の旧画像（500-900KB）が誤りと判明、Google Drive ダウンロードフォルダの新画像（2.2〜2.4MB高解像度）に全面差し替え。サブエージェントで11ファイルの表情を識別→マッピング→既存ファイル名に上書きコピー。新カテゴリ「オオタニ所長 驚き／絶望」を追加。プレビューに日本語名・ローマ字名（ootani-normal.png等）両方を配信
- 2026/05/03: 画像識別運用ルール確立。CLAUDE.md「画像分析はサブエージェントに委譲」を実践。今後似たケース（複数キャラ画像の表情マッピング）はサブエージェント1往復で完結可能
- 2026/05/03: 残り3表情（オオタニ所長 ドヤ顔／恥ずかしい・新人タナカ ドヤ顔）の高解像度版（各2.1〜2.3MB）も新画像に差し替え。**全15表情パターン**が高解像度で揃った（旧500-900KB低解像度ファイルは完全に解消）
- 2026/05/03: 本番WordPressへ新キャラ画像13枚をREST API一括アップロード（12/13成功・media_id 737-748）。残り1枚「新人タナカ 通常」はConoHa WAF（ContentDispositionまたはバイト列パターンを永続ブロック）に弾かれ、JPEG変換+ASCII別名+UA偽装+40秒待機の対策全部試したが突破不可。手動アップロード手順を `blog/migration/jinr-fukidashi-update-manual.md` に明記
- 2026/05/03: ConoHa側でWAF一時OFFにしてもらい、残り1枚（新人タナカ 通常）も自動アップロード成功（media_id=749）。**13/13完了**。WAFは作業後オーナーがONに戻す前提。**学び：ConoHa WAFは特定リクエスト指紋を永続ブロックするため、UA/別名/JPEG等のクライアント側対策では突破不可。バルクアップロード時は事前にWAF一時OFFが現実解**
- 2026/05/03: WP REST API認証情報を `blog/config.json` に追加（wp_auth.username=ootanisatan + Application Password）。今後 wp_api.py 経由で記事公開・更新が可能。`.gitignore` 済
- 2026/05/03: **本番JIN:R吹き出しスロット10個を全自動切替成功**。Chrome拡張（Claude in Chrome）経由で wp.customize._value から `jinr__fukidashi{1-10}_image` キーを発見→ `wp.customize(key).set(newUrl)` で10件一括設定→ `wp.customize.previewer.save()` で一括公開（changeset_status: publish）。所要約2分。Garmin記事HTMLで新画像URL 17箇所・MX ERGO記事で4箇所検出して反映確認。**手動カスタマイザークリック10回（15分作業）が完全に自動化された**
- 2026/05/03: 学び：WordPressカスタマイザーは `wp.customize._value` 経由でJSオブジェクトとして全設定にアクセス可能。テーマ独自設定（theme_mods）もこのオブジェクトに含まれるため、**REST APIでは触れない設定もChrome拡張経由ならJSで一括変更可能**。今後の本番WP操作（ホームページレイアウト変更・SEO設定・ウィジェット配置等）も同パターンで自動化できる
- 2026/05/03: **JIN:R吹き出しスロットマッピングの根本的訂正**。旧 `jinr_fukidashi_slots.md` で「shortcode番号 = registerData + 1」と書かれていたが**誤り**。実画像URLのhexデコード＋画像内容識別で「shortcode番号 = slot番号」が正解と確定。`memory/jinr_fukidashi_slots.md` 全面書き換え済
- 2026/05/03: **wp_block_builder.py 全面改修**。旧版は `block_fukidashi_ootani` が registerData=1 + shortcode=2（slot 2=ドヤ顔）の不整合を生成していた。新版は registerData/shortcode を slot番号で揃え、テキスト内容から表情を自動推定する `choose_ootani_expression` / `choose_tanaka_expression` を追加。表情明示記法 `**オオタニ所長[ドヤ顔]：**` も対応。self-test合格
- 2026/05/03: **公開4記事の吹き出し表情を文脈別に再アサイン**。Garmin Venu 2S（17件中9件修正）、MX ERGO 持ち運び（4件中2件）、MX ERGO レビュー（4件中1件）、HUAWEI GT Runner 2下書き（17件中9件）。**「オオタニ悩む」「タナカ絶望/怪しげ偏重」を解消**。ConoHa自動キャッシュクリアプラグインが post更新時に自動発火するため、本番反映即時。Garmin記事の最終配分：通常6/ドヤ顔5/驚き2/恥ずかしい2/絶望1/怪しげ1 = 文脈に沿った自然なバランス
- 2026/05/03: 学び：**registerData と shortcode番号は揃える**こと。JIN:R は shortcode で表情を決定し registerData は無視するが、Gutenbergエディタの選択ハイライトには registerData が使われるため、両者を slot番号で揃えないと wp-admin GUIで開いた時に違うキャラが選択された状態になる
- 2026/05/04: **本番ブログ www.ootanisatan.com のホーム画面を Cloudflareプレビュー同等デザインに刷新成功**。手法：固定ページ「ホーム」(id=756) を新規作成→FSEのCustom HTMLブロックで全7セクション（ヒーロー・カテゴリ4カード・★注目の記事・★最新の記事・サイドバー・ボトム4特徴）を構築→ JIN:R Customizer で `show_on_front=page, page_on_front=756, custom_css[jinr]=12029bytes` を Chrome拡張のJS経由で一括設定。所要約30分・全自動。プレビュー版CSS変数（#1d4ed8/#f97316/Noto Sans JP 800）完全移植
- 2026/05/04: **JIN:R固定ページの『デフォルトUIを非表示にする』テクニック確立**。`body.page-id-756 .o--jinr-mainvisual / .o--widget-area / #postHeader / .c--entry-title / .l--sidebar { display: none !important }` で、JIN:Rが自動レンダリングするヒーロー・ホームウィジェット・ページタイトル・サイドバーを完全に消し、固定ページのHTMLだけを表示。これにより自前デザインを完全独立で展開できる
- 2026/05/04: 学び：JIN:Rの **`spcv_category` は2スロット制限**で4カード化不可だが、Custom HTML ブロックで自前カード4枚を作れば制限なし。テーマ機能との整合より「Custom HTMLで自由設計」がROI最大。同様にメインビジュアルも JIN:R標準を捨てて自前ヒーローセクションを CSS背景画像で実装する方が柔軟
- 2026/05/04: ヒーロー画像は ConoHa WAFが画像upを永続ブロックするため**Cloudflareプレビューの画像URLをhotlink** で暫定運用（`https://ootanisatan-preview.pages.dev/assets/img/hero-banner.png`、CORS解放済み）。後日 WAF OFF時に WP本番にアップして差替予定。スナップショット類は `blog/migration/snapshots/` に保存（home-before/home-after/customizer-backup）
- 2026/05/04: **本番文字サイズを全体的に拡大**（ヒーロータイトル2.1→2.5rem、カテゴリ見出し1.05→1.2rem、セクション見出し1.25→1.55rem、記事本文17px等）。Customizer 追加CSSに2.7KBの拡大セクションを追記して公開
- 2026/05/04: **PTGLヘッダーロゴを Pillowで自動生成→本番反映**。ChatGPT/DALL-Eウェブ操作はCookieセキュリティでブロック・Gemini API は期限切れだったため、フォールバックとして Pillow で「中央太字日本語＋下にPTGL副題＋左右オレンジ装飾線＋上部ブルードット3つ」のテキスト中心ロゴv3を構築。サブエージェント評価8.5/10。WP REST APIでアップ（media_id=767、29KB→WAF回避）→ JIN:R `jinr__header_logo_url` に設定→公開。`blog/migration/build_logo_v3.py` で再生成可能
- 2026/05/04: 学び：**ロゴ自動生成の最も確実な手段はPillow直接描画**。AI画像生成サービス（ChatGPT/DALL-E/Gemini）はWeb UI経由だとデータ流出防止のためCookie/URL含む結果がブロックされ画像取得不可、APIは認証情報切れリスクあり。Pillowは時間ゼロで確実に出力でき、フィードバックループ（生成→サブエージェント評価→改善）も高速
- 2026/05/04: 学び：**ConoHa WAFの画像ブロックはファイルサイズ依存**（小：23-36KBは通る・大：1MB以上は弾く）。PNG最適化＋画像縮小で WAF を回避できる場合あり
- 2026/05/04: ロゴv4 (Pillow・歯車14歯+稲妻+太字日本語+PTGL副題) 生成→WPアップ完了 (media_id=771)。`jinr__header_logo_size=250` で公開
- 2026/05/04: **学び：ChatGPT Web UI で生成した画像は Chrome MCP のセキュリティポリシーで自動取得不可**。canvas経由の base64 化、文字列chunk化、obfuscation、clipboard書込、`<a download>` クリックすべて阻止される。「Exfiltrating image data from a third-party authenticated session」と明示判定される。**正規の方法は2つ**：①オーナーが手動で画像を保存→Drive経由 / ②OpenAI API キーを使ってAPI経由で画像生成（gpt-image-1 モデル、$0.02/枚）
- 2026/05/04: **ChatGPT高品質ロゴを本番ヘッダーに反映成功**。フロー：ChatGPTで生成→オーナーがGoogle Driveのダウンロードフォルダに右クリック保存（10秒手動・1916×821・PNG・1.5MB）→PDMが検知→PIL で1200×514にリサイズ＋PNG最適化（427KB→WAF回避）→WP REST APIアップ（media_id=774）→ JIN:R Customizer `jinr__header_logo_url` 更新→公開。標準フロー「①AI生成 ②（Canva仕上げ）③リサイズ ④WP反映」の③④を自動化、①②をオーナー作業に委ねるのが最効率
- 2026/05/04: ロゴ生成の手段比較：(A) Pillow直接 = 完全自動だが品質6-8.5/10、(B) ChatGPT Web UI + 手動保存 = 品質9-10/10・1ステップ手動、(C) OpenAI API直接 = 完全自動・品質9-10/10・$0.02/枚。長期的には (C) が理想だがAPIキー要発行

- 2026/06/08：**記事めしPWA「比較表ビジュアル」を2〜4製品＋製品ごと実写真ひも付けに改修**（app.jsのみ）。
  - 比較数は専用セレクタを足さず「比較対象欄の `/` 区切り製品名の数」で自動判定（2〜4にクランプ・5個以上は4・空欄は3）。HTML/CSS追加ゼロで実現でき壊れにくい。
  - 画像役割「⚖️ 比較」を選ぶと renderQueue 内で「製品1〜4」セレクタ（compare-idx-sel）を動的描画。選ぶと item.compareIndex を保存→アップ時ファイル名 `compare_p{n}_◯◯.jpg`→PROMPT.md に「製品n（製品名）: 写真」と製品ごと記録。
  - **学び：画像役割タグ→ファイル名prefix→roleUploadMap→memos配列→savePrompt の経路を使えば、PROMPT.mdへの追記はGAS改修不要でクライアント側だけで完結する**（compareの製品別行もこの経路で実現）。GAS(savePrompt)が送るのは articleType + memosJson のみ。
  - 触った関数：TEMPLATE_FIELDS.compare / AI_TEMPLATES.compare（矢印関数を本体ブロック化しnを動的算出）/ renderQueue（セレクタ描画＋change保存）/ cycleRole（非compareでcompareIndexクリア）/ uploadAll（prefix分岐＋newNotes製品別）。
  - 検証：`node --check` 通過＋count判定のstandaloneテスト＋本番curlでマーカー検出（compare_p/2〜4ガイド/compare-idx-sel）。デプロイは `wrangler pages deploy . --project-name=blog-capture --commit-dirty=true --branch=main --commit-message="ASCII"`（git pushでは反映されない教訓を順守）。

- 2026/06/08：**記事めしPWA全体監査（6視点×並列バグハント→反証検証）で確定バグ21件を特定、クライアント側11件＋UI改善3件を修正・本番反映**。
  - 修正済（app.js/styles.css）：①記事切替でのメモ混入（記事IDひも付け＋確認ダイアログ＋fetch世代ガード）②リロード後の「編集中…」固着（起動時クリーンアップ）③ObjectURLリーク（再描画ごと一括revoke）④staleクロージャ書き戻しでcompareIndex消失（queueUpdate部分更新ヘルパー新設）⑤削除画像のゾンビ復活（存在チェック＋新規追加フォールバック）⑥転送中の誤操作（isUploadingロック）⑦【critical】比較役割行の複数行メモがPROMPT.md往復で消失→**役割行は必ず1行＝1件で生成**（GASのparsePromptMd_は番号付き1行しか読み戻せない）⑧ROLE_NOTE_RE不一致「アイキャッチ画像」vs実ラベル「アイキャッチ」→共通定義に統一（旧形式も後方互換）⑨製品名ソース誤り（compareテンプレ選択中の欄のみ信用）⑩製品番号未割当の送信前警告 ⑪1製品入力時の矛盾プロンプト解消／UI：製品セレクタ右上移動＋製品名表示・メモ「●未保存」表示・トースト複数行＋warn長め表示
  - **学び：PROMPT.mdへ書くメモは「1行＝1件」が鉄則**（複数行メモは往復で2行目以降が消える）。**正規表現や定数はROLE_DEFS等の単一ソースから導出**しないとラベル変更で静かに壊れる。
  - 🔶 **残課題（GAS側・要再デプロイのため未着手）**：(a)Deduperが記事をまたいで重複スキップ→別記事に同じ画像を使えない＆キューから消える (b)ハッシュ台帳が削除/差替に追従せず誤スキップ永続 (c)downloadFile/replaceFileがDrive全体の任意fileIdを受ける（トークンは公開JSに露出・要スコープ制限）(d)savePromptの「ゴミ箱→新規作成」方式は作成失敗時にメモが消える窓（setContent上書き化が安全）(e)resumableUrlのみLock無し (f)大容量アップ成功時にfileId/正規化名が返らず役割行が実在しない名前を指す。対応時は clasp push → create-deployment -i <同ID> でURL不変デプロイ。
    → **2026/07/09追記：(a)(b)(d)はGAS v23で解消済み**（replaceFileの記事フォルダ所属チェックも追加＝(c)の一部緩和）。残りは(c)のスコープ制限・(e)・(f)。
  - 監査手法メモ：Workflowツールで6視点find→1指摘1反証verifyの2段構え。誤検知2件を棄却でき、修正対象を確定リスト化できた（62エージェント・約15分）。

- 2026/06/09：**記事めしPWA：既存Drive画像の役割表示＋役割変更機能を実装（GAS v20デプロイ込み）**。
  - 🚨 根本バグ発見：GASの `normalizeFilename` がファイル名をタイムスタンプ化する際に**役割プレフィックス（eyecatch_等）を捨てていた** → article_from_meshi.py はファイル名プレフィックスでしか役割判定しないため、**今までアップした画像は全て「その他」扱いだった**。Normalizer.gsを修正しプレフィックス保持（`eyecatch_20260609_123456.jpg`形式）。
  - 既存ファイルカードに役割バッジ（①ファイル名プレフィックス→②PROMPT.md役割行のfileId照合の2段逆引き）＋🏷役割変更セレクタを追加。変更＝GAS新アクション`renameFile`でDrive上のファイル名を付け替え（記事フォルダ内のファイル限定・Lock付き）。ユニーク役割（アイキャッチ/ヒーロー/NG集）は重複時に確認→他方を自動降格。PROMPT.md役割行も同期更新。
  - **GASデプロイ手順の実証（2回目）**：`clasp push -f` → `clasp create-deployment -i AKfycby9BS...（config.jsのGAS_URLと同ID） -d "v20: ..."` でURL不変デプロイ成功。**注意：curlでのPOST疎通確認はGoogleのリダイレクトでHTMLが返り判定不能。GET系アクション（getPrompt等）で確認する**こと。
  - 仕様メモ：アイキャッチが2枚ある場合、記事生成に「どちらを使うか」選択UIはなくAI任せで曖昧 → 役割変更セレクタで片方を降格して解消するのが正しい運用。

- 2026/06/09：**記事めしPWA「⚖️比較画像をまとめてAIへ」実装**。AI編集は1枚しか渡せない制約への解：キューの「⚖️比較」役割画像をcanvasで製品ラベル付き連結シート1枚に合成→スマホはWeb Share（画像＋プロンプト同時）／PCはクリップボード＋ChatGPT自動起動。プロンプト末尾に「各パネルを切り出して対応カードのヘッダーに使用」を自動追記。テンプレ「比較表」選択時のみボタン表示。**学び：複数画像をAIチャットに渡す確実な方法は「1枚に連結（ラベル付き）」**（クリップボードは複数画像非対応・Web Shareは複数可だがAI側の対応が不安定）。

- 2026/06/09：**記事めしPWA：プロンプト編集UIを統合**。編集バナー内の「📝プロンプトを編集」（テンプレ・変数・本文）を撤去し、**上部「プロンプト準備」ヘルパーに一本化**。バナーは受け渡し専用（手順・画像/プロンプトコピー・取込・キャンセル）に。バナーの「📝プロンプトをコピー」「🚀開く」「📤共有」は `currentEditPrompt()`（ヘルパーの現在値優先・空ならスナップショット）で**常に最新を参照**。編集中もヘルパーを隠さない仕様に変更。**学び：同じ編集対象を2画面に持たせると片方だけ直して壊れる（05/09のwidget二重CSS事故と同根）。編集UIは1箇所＋他画面は参照のみ、が鉄則**。Cloudflare Pagesのエイリアス反映は最大30秒程度遅延あり（デプロイ固有URLで先に検証→エイリアスは後追い確認）。

- 2026/06/09：**「比較画像をまとめてAIへ」の貼り付け失敗を根治**。原因＝ボタン押下→シート合成（Drive DL含む）をawaitした後にclipboard.write/window.openすると、**ブラウザのユーザー操作権限（transient activation・約5秒）が切れて拒否**される。対策＝押した瞬間に①window.open（同期）②`new ClipboardItem({'image/png': promiseOfBlob})` の**Promise渡しでクリップボードを先に予約**（合成完了後に中身が入る・W3C公式の正しい方法）。**学び：クリック起点のclipboard/window.open/share は「重い処理のawait後」に呼んではいけない。Promise渡しClipboardItemが正解**。あわせて連結シートにDrive既存比較画像も合流（同製品番号は一時保存優先）、oneClickEditのコピーを画像＋プロンプト同梱化、送信カードに未入力欄表示・連結シート横長サムネ表示を実装済み。

- 2026/06/13：**記事めしPWA：バナーカード直接編集化＋回帰監査で確定11バグを修正**（3視点find→反証verify、14エージェント）。
  - バナー「いまAIに送る内容」カードをその場で編集可能に（テンプレselect＋4欄input、上部ヘルパーと双方向同期・単一ソース維持）。
  - **描画ロジックをstructureKey方式に再設計**：構造（テンプレ・編集対象）が変わった時だけ全再描画、それ以外は軽量同期（全文プレビュー・文字数・非フォーカス欄の値）。→「打鍵ごと全再描画」「focusout {once:true}リスナー蓄積/早期消費」「テンプレ切替が軽量ガードに握りつぶされる」を根治。**学び：DOM部分更新は「フォーカス判定でスキップ」ではなく「構造キー比較」で設計する。{once:true}リスナーを再描画パスで張るのは蓄積/消失の温床**。
  - その他修正：regenerateAIPrompt末尾でカード明示更新（プログラム代入はinputイベント非発火）／updateStepChipsはdata-clip-chip限定（スマホ・Canvaのチップ構成を壊さない）／比較まとめ送りのエラー処理＋前編集待機の孤児化防止／**uploadAll役割行を全削除→マージ方式に**（過去アップ分の役割記録を保持。今回分・新ユニーク役割・同製品番号のみ置換）／ユニーク役割付替えで降格側の役割行も除去／lastExistingFilesの記事切替残留クリア／サムネObjectURLキャッシュ＋クローズ時解放／全文コピーボタン・記事名表示。
  - 残課題：GAS側6バグ（Deduper記事跨ぎ等・前回記録済み）＋UX改善案10件（監査出力 wyt6f908a 参照）。UX担当エージェント1体はAPI利用上限で停止。

- 2026/06/13：**記事めしPWA：編集確認ポップアップ＋比較2枚以上選択＋通信中ロックの3機能を実装**。
  - 汎用モーダル `openModal({title,bodyHTML,buttons,onRender})`（Promise解決・背景/✕でnull・onClickでバリデーション/値返し）を新設し全UIで再利用。
  - ① 画像の🤖🍌🎨を押すと即開始せず `confirmThenEdit` で確認ポップアップ（サムネ＋用途=役割select＋compare時は製品番号）→了承で `oneClickEdit`。既存ファイルは確認のみ→ロック付きDLして編集。ユニーク役割は他を自動降格。
  - ② 比較まとめ送りは `gatherCompareCandidates`（一時保存＋既存Drive画像）→ `chooseCompareImages` モーダルでチェックボックス選択（2枚以上必須・製品番号割当）→ `buildCompareSheet(entries)`（entries引数対応に拡張）。**学び：モーダルで操作を挟むとユーザー操作権限(transient activation)が切れるので、比較フローはauto-open/clipboard予約をやめ、連結シートを一時保存に入れてバナーの「🖼再コピー」「🚀開く」（実ジェスチャー）で渡す方式に統一**＝paste失敗の根治。
  - ③ サーバ通信中ロック：`serverBusy`＋全画面オーバーレイ `lockUI/unlockUI/withServerLock(msg,fn)`。記事切替・メモ再読込/保存・既存ファイルDL・役割変更・転送(uploadAll)を全部ロック付きに。通信中に触ると再描画と衝突して操作しづらい問題を解消。多重起動はトーストで弾く。各ボタンにも `if(serverBusy)return` ガード。
  - 検証：構文＋選択ロジック机上テスト＋本番マーカー（openModal/confirmThenEdit/withServerLock/chooseCompareImages/CSS）確認。

- 2026/06/13：**記事めしPWA：用途/役割の選択肢を全UIで16テンプレ一覧に統一**。①編集確認ポップアップの「用途」を8役割→16テンプレ（上部#ai-template-selectのinnerHTML流用）に統一、TEMPLATE_TO_ROLEでファイル名役割へ変換＋テンプレ選択でプロンプトも連動。②キュー画像の役割ボタンを「サイクル式(cycleRole)」→「16テンプレのポップアップ(openRolePickerForItem)」に変更。item.templateKeyを保存し再オープン時の初期選択に使用（旧アイテムはROLE_TO_TEMPLATEで代表テンプレに逆引き）。「☆役割なし」を先頭に付加（テンプレ一覧には無い状態のため）。**学び：役割(8・ファイル名プレフィックス)とテンプレ(16・プロンプト種別)は本来別軸だが、ユーザーには1つの語彙に統一した方が分かりやすい。テンプレ→役割の一方向マップ＋templateKey保存で両立**。

- 2026/06/13：**バナー「🚀 AIを開く」で⌘Vしても画像が入らないバグを修正**。原因＝`window.open`→新タブにフォーカス移動→その後の元画面 `clipboard.write` が「Document is not focused」で失敗していた。修正＝**clipboard.write を window.open より前**に、Promise渡しClipboardItem（中身は後確定）で予約してから開く。**学び：クリップボード書き込みは window.open の前（フォーカスがある間）に呼ぶ。順序が命**。なお「外部サイト(ChatGPT)の入力欄へ完全自動paste」はブラウザのクロスオリジン制約で不可能＝できるのは「⌘V一発で確実に画像が入る＋プロンプトはURL自動入力」までが上限（ユーザーにも明示）。

- 2026/06/13：**画像が⌘Vで貼れない根本原因＝クリップボードに画像＋テキスト(text/plain)を同梱していたこと**。ChatGPT等のリッチ入力欄は画像+テキスト混在クリップを貼ると**文字を優先し画像を捨てる**。修正＝AI受け渡しのコピーは全経路で**画像のみ(image/png単独)**に統一（oneClickEdit初回・バナー🚀開く・🖼再コピー）。プロンプトはURL自動入力＋必要時「📝プロンプトをコピー」(text単独)で分離。**学び：AIチャットへ画像を渡すクリップボードに text/plain を混ぜない（1 ClipboardItem に複数MIMEを入れると貼付側が1つだけ採用＝画像が負ける）。画像コピーとプロンプトコピーは別操作に分ける**。

- 2026/06/13：**比較表：製品番号の重複警告＋編集開始時の自動合体を実装**。①`duplicateIdxList`/`warnIfCompareDuplicates`で重複番号を検出→割り当て時（インライン製品セレクタ・役割ピッカー）・比較ギャラリー（赤バナー）・合体直前の3箇所で警告。重複のまま合体すると同番号ラベルが2つ出るため合体をブロック。②編集確認ポップアップで「比較表」を選んで開始すると、単体編集ではなく`stageCompareSheetFromAssigned`で**割り当て済み比較画像を自動で1枚に合体**→その合体シートを編集対象(pendingReplace)に載せてバナー表示。手動「⚖️まとめてAIへ」ボタンと共通の`stageCompareSheet`/`ensureCompareNote`に集約。**学び：比較は最初から「1枚の合体画像」を編集単位にする（AIには1画像しか確実に貼れないため）。単体画像のまま編集させない**。

- 2026/06/13：**別画像の編集に切り替えた時に前回の編集データをリセット**。`discardPreviousEdit(exceptId)`を新設：前対象の編集中フラグ解除＋自動生成連結シート(compare_sheet_)の残骸をキューから削除＋pendingReplace破棄。oneClickEdit冒頭・stageCompareSheet冒頭で呼ぶ。confirmThenEditは毎回regenerateAIPromptでプロンプトを作り直し（前回の比較注記等の追記を消す）。あわせて編集バナーの比較ギャラリーに「⚖️比較画像をまとめてAIへ」ボタンを追加し、上部ボタンと共通の`runCompareBundle()`に集約。**学び：編集は対象ごとに状態(pendingReplace/prompt/一時シート)を必ずクリアしてから開始。切替時リセットしないと前の連結シートやプロンプト追記が残る**。

- 2026/06/13：編集ポップアップに変数欄＋リサーチを足したが、**ユーザー判断でポップアップは「つくるもの＋製品番号」のシンプル形に戻し、変数入力＆🔍リサーチは「いまAIに送る内容」カード側に集約**（編集はカードで行う運用に決定）。カードに `bps-research`ボタンを追加し copyResearchPromptForCurrent('helper') を流用（カードの変数入力は元々 ai-var-* と双方向同期済みなので追加同期不要）。編集ポップアップの初期テンプレが画像のtemplateKey/役割を引き継ぐ点は維持。**学び：同じ編集UIを2画面に置かない。入力は1画面（カード）に集約し、ポップアップは確認＋用途選択だけに留める（重複UIは混乱の元）**。

- 2026/06/13：**既存ファイル(再読込)の編集を一時保存編集と完全統一**。editExistingFileを「独自確認→oneClickEdit直行」から「**ロック付きDLで一時保存に追加（ファイル名prefixからrole/製品番号/templateKeyを`parseRoleFromName`で引き継ぐ）→ 既存ファイル一覧を畳んで一時保存セクションへscrollIntoView → 一時保存と同じ`confirmThenEdit`（つくるもの選択ポップアップ）で編集**」に変更。あわせて**セクション間ジャンプボタン**（既存ファイル「⬇️一時保存へ」／一時保存ヘッダ「⬆️既存ファイル(再編集)へ」）を追加。**学び：同種操作はUIフローを1本に統一する（再編集と新規編集で別フローにしない）。検証時、Cloudflare Pagesは `/index.html` パスとルート`/`でキャッシュキーが別＝HTMLはルート`/`で確認すること（/index.htmlは古い結果を返しうる）**。

- 2026/06/13：**比較ギャラリーを「表示専用→各画像に製品番号セレクタ付き・その場で直せて即更新」に作り替え**。原因の本質＝ギャラリーは表示専用で番号を直す手段がそこに無く（一時保存画像とDrive既存画像が混在・Drive側は番号変更不可）、別の場所で直しても反映が分かりにくかった。修正＝各セルに製品番号select（番号なし/製品1-4）を配置。一時保存画像→queueUpdate+renderQueue、Drive画像→changeExistingFileRoleでファイル名の番号付け替え（ロック付き）→loadExistingFiles→ギャラリー再描画。重複セルは枠をオレンジ強調。あわせて`refreshCompareGallery()`をrenderQueue末尾とインライン変更で呼び都度更新。**学び：警告を出すだけでなく「その場で直せるUI」を同じ場所に置く。混在ソース（ローカル/サーバ）はそれぞれの更新経路（local書換 vs サーバrename）に振り分ける**。

- 2026/06/13：**AI生成の比較表を「完成版」として登録する役割 `comparetable`(📊比較表(完成)・prefix comparetable_・unique) を新設**。比較の合体シート(compare_sheet_)をAI編集→戻ってきた画像をtryReplaceWithEditedImageで検出し、自動で role=comparetable／originalName=comparetable_<id> に切替（既存の完成版は降格）。転送でDriveに comparetable_ 名で保存され、記事生成(article_from_meshi.py ROLE_DEFSに追加済み)が「比較セクションのメインビジュアル」として認識。ROLE_DEFS/EF_ROLE_OPTIONS/UNIQUE_ROLE_PREFIXES/ROLE_PREFIX_STRIP_RE/ROLE_NOTE_RE/ROLE_TO_TEMPLATE/parseRoleFromName＋GAS Normalizer.gs ROLE_PREFIX_RE(v21)＋python に comparetable_ を追加。注意：`/^compare_/` は comparetable_ に**マッチしない**（"compare"直後が"t"）ので素材(compare_)と完成版(comparetable_)は安全に区別される。**学び：AIの「成果物」と「素材」は別役割にする（素材=compare_p*、成果物=comparetable_）。両者を同prefixにすると比較ギャラリーや合体対象に成果物が混入する**。

- 2026/06/13：**記事メモの各ポイントに画像（一時保存/Drive）をリンクできる機能**を追加。各メモ行に「🖼 画像をリンク」ボタン→`pickImagesForMemo`（`gatherAllImageCandidates`で一時保存＋Drive画像をサムネ＋役割ラベル付きで列挙）→選択。リンクはメモ文字列末尾に「｜🖼使う画像: トークン, …」で保持（**PROMPT.md往復で壊れない単一行**）。本文編集してもリンクは保持（textareaは本文のみ表示・リンクはチップで別表示）。トークンは役割ラベル（アイキャッチ/比較表(完成)/比較 製品N 等）またはDriveファイル名＝**PROMPT.mdの役割行と突合可能**なので、AIが「どの点にどの画像か」を的確に判断できる。**学び：構造化したいがPROMPT.md(1行=1件・番号付き)の制約があるデータは、行内に人間可読＆機械可読なサフィックス記法で埋める（別データ構造にしない）**。

- 2026/06/13：**「既に役割を割り当て済みの画像（アイキャッチ等）があれば最優先で使い、同役割を新規生成しない」ルールを徹底**。①PROMPT.md(GAS handleSavePrompt_ v22)とcontext.md(article_from_meshi.py 画像一覧見出し)に「🔴最優先：既存の役割画像はそのまま使用・新規生成禁止／無い役割のみ新規提案」を明記。②PWA confirmThenEditでユニーク役割(eyecatch/hero/ngsummary/comparetable)を作ろうとした時、一時保存orDriveに同役割が既にあれば「作り直す？既存を使う？」をconfirmで確認（キャンセル＝既存優先で編集中止）。**学び：AIへの指示(PROMPT/context)と操作UI(確認ダイアログ)の両方で同じ方針を効かせると、ムダな再生成を確実に防げる**。

- 2026/06/13：**AI編集後の上書き保存が反映されない不具合を修正（3点）**。①大容量(>20MB)経路 uploadLarge は replaceDriveFileId を扱えず新規ファイルを作っていた→転送ループで「replace対象は常にuploadSmall(replaceFile)経由」に分岐（`item.size>LIMIT && !item.replaceDriveFileId`）。②**転送後に既存ファイル一覧を再読込していなかった**→Driveサムネが旧fileIdのままで「更新されない」ように見えた。didReplaceフラグで上書き発生時に loadExistingFiles を再実行。③ユニーク役割の重複確認が「再編集中のファイル自身(replaceDriveFileId)」を重複と誤判定し編集中止しうる→自身を除外。**学び：上書き(replace)はサイズ分岐の前に最優先で判定する。サーバ更新後はクライアント一覧を必ず再取得（サムネ/idキャッシュで古く見える）**。

- 2026/06/13：**「AI編集後 最新画像に更新されない」を調査エージェントで徹底追跡し2大原因を修正**。①既存ファイル(特にcompare_pN_)を再編集→つくるもの=比較表(compare)を選ぶと、stageCompareSheetFromAssignedへ分岐し**元の単体アイテム(古いblob＋replaceDriveFileId)がキューに残り、転送時に古い画像でreplaceFile上書きされていた**→比較切替時に元アイテムをqueueDelete。②**Driveのサムネ生成遅延/キャッシュで、上書き後も一覧が古い画像に見えた**→上書き転送した画像は手元blobのObjectURLを`recentReplacedThumbs[新fileId]`に保持し、loadExistingFilesのサムネにDrive thumbnailUrlより優先して使う（＝確実に最新が映る）。**学び：サーバ側の画像更新後、サーバのサムネ生成は遅延する前提で、クライアントは「アップロードした実blob」を即時表示に使う。分岐(compare等)で編集対象を切り替える時は、置き換え対象だった元アイテムを必ず始末する**。

- 2026/06/13：**「フォルダから書き換えファイルを選択しても変わらない」の根因＝取込はローカルqueue差替のみで別途「すべて転送」が必要だった**。対策：再編集（replaceDriveFileId付き）画像を取込/貼付/ファイル選択した時点で、tryReplaceWithEditedImage内から**即uploadSmall(replaceFile)でDrive上書き→queueDelete→loadExistingFiles再読込**まで自動実行（withServerLockでロック）。手元blobを recentReplacedThumbs に入れて一覧サムネも即最新化。**学び：「保存」をユーザーの追加操作に委ねると"変わらない"と感じる。上書き対象が明確な再編集は、取り込んだ瞬間に保存まで完了させる（ワンアクション化）**。

- 2026/07/09：**記事めしPWA：記事間のデータ混入・上書き消失を根治（記事別データ分離・PWA＋GAS v23）**。ユーザー報告「新しい記事に前の記事のメモ・画像が引き継がれる／前の記事のデータが上書きされて消える」を3並列調査（PWA状態管理・GAS・開発史）で全経路特定→修正→実機検証→デプロイ。
  - **根本原因＝キューとメモが「記事に紐づかない単一スロット」だった**：①IndexedDBの画像キューに記事識別子が無く全記事共有→未転送のまま記事切替すると別記事へ送信 ②メモはlocalStorage固定キー1個→切替で残骸が混入・相互上書き ③【最悪】再編集情報(replaceDriveFileId)がキューに残ったまま別記事で送信すると**前の記事のDriveファイルを直接上書き破壊** ④新規記事作成ボタンはプログラム代入のためchangeイベント不発火→既存の混入防止ダイアログを素通り ⑤GAS Deduperが全記事横断hash照合→別記事で同じ画像が「重複スキップ」され消えたように見える ⑥GAS savePromptが「ゴミ箱→新規作成」で作成失敗時にメモ全損の窓。
  - **修正（app.js）**：キュー全レコードに持ち主刻印(ownerFolderId/ownerTitle)＋queueAllを「現在の記事＋未振り分けのみ」に分離（旧データはqueueAllRaw）／メモをkiji-meshi:memo-state:folder:<ID>の記事別キーに分離（旧単一キーは起動時自動引っ越し）／記事切替・新規作成・起動復元をswitchMemoContext()に統一（未選択中の下書きは選択時に引き取り）／**上書きガード＝replace対象が現在の記事フォルダに実在（or持ち主一致）しなければ「新規保存」に転換**（どちらの記事のデータも失わない方向へ倒す）／転送成功後のメモ自動クリア廃止（分離済みなので不要・空上書き事故の温床だった）／破棄ボタンは現在の記事の分のみ。
  - **修正（GAS v23）**：savePromptをsetContent上書き化（消失窓排除・fileId不変）／findByHashに記事フォルダ条件＋台帳残骸チェック（削除済みファイルは重複扱いしない）／replaceFileに記事フォルダ所属チェック／resolveArticleFolder_で「フォルダIDと記事名の不一致」をエラー検出／空白のみタイトルを拒否（共有フォルダ【記事】への合流防止）。
  - **検証**：ローカルサーバー＋実Chrome（テスト用__test__フォルダ）で6シナリオ実証＝未選択下書き→unassignedキー／記事選択で引き取り＋Driveマージ／A⇄B切替で混入ゼロ・各キー無傷／キューはA選択でA+未振り分けのみ表示／リロード復元も記事単位／コンソールエラーゼロ。本番デプロイ後、旧キーの自動引っ越しも実機確認。
  - **学び：複数の「作業対象」を扱うアプリは、全ローカル状態（キュー・下書き・編集中フラグ）に最初から持ち主IDを付ける。「切替時にクリアする」設計はクリア漏れ経路が必ず残る（プログラム代入はchange不発火等）。「持ち主で分離する」設計なら漏れようがない**。また、誤上書きを検出したら「ブロック」より「新規保存に転換」の方がデータを失わない。
  - 注意：preview_startのサンドボックスはこのMacでpython/nodeのファイルアクセスが利かない→検証はBashでnodeサーバー起動＋Claude in Chromeで実施が確実。

- 2026/07/10：**記事めしPWA：画像・メモを「別の記事へコピー/移動」できる📦機能を追加（PWA＋GAS v24）**。記事別データ分離（07/09）で勝手な混入は無くなったが、「意図的に別記事へ渡したい」時の正規操作が無かったため追加。
  - **対象3種類＋UI**：①一時保存の画像（キューカードの📦）②Drive上の既存画像（既存ファイルカードの📦）③メモ（各メモ行の📦）。共通ポップアップ pickTransferTarget で送り先記事＋コピー/移動を選択。
  - **実装**：①はIndexedDBのownerFolderId書換（移動）/複製（コピー）。**replaceDriveFileId・editingWithは転送時に必ず切り離す**（元記事の文脈を持ち込むと別記事のファイル破壊につながる）。②はGAS新アクションtransferFile＝移動元所属チェック→file.moveTo/makeCopy→**ハッシュ台帳の紐づけも更新**（移動=updateHashRecordFolder/コピー=addHashRecord。台帳が古いままだと記事単位の重複判定が狂う）。**ユニーク役割（eyecatch等）が送り先に既にあればプレフィックスを外して渡す**（送り先の既存を壊さない）＋移動時は元記事PROMPT.mdの役割行も除去。③は送り先のgetPrompt→末尾追記→savePromptのマージ保存（送り先の記事タイプ・既存メモ保持・重複はスキップ）＋送り先ローカル下書きにも反映。
  - **検証**：実Chrome＋__test__フォルダ間で3種類ともUIクリック経由でE2E実証（キュー移動→B表示・メモコピー→A到着＆B無傷・Drive画像移動→物理移動確認）。コンソールエラーゼロ。
  - **学び：データを跨がせる機能は「持ち込んではいけない文脈」（上書き先ID・編集中フラグ・台帳の紐づけ）の切り離し/更新をセットで設計する**。転送そのものより、この後始末が事故防止の本体。

- 2026/07/11：**記事めしPWA：📦転送に「☑ まとめて選択」（一括コピー/移動）を追加**（PWAのみ・GASはv24のまま変更なし）。
  - 3セクション（一時保存・既存ファイル・メモ）に「☑ まとめて選択」トグル→チェックボックス→一括バー（N件選択中＋📦別の記事へ…）の共通パターン。送り先選択は単体転送と同じ pickTransferTarget を流用。
  - **実装の要点**：①キュー/既存ファイルはカードDOMにチェックボックスを後載せするオーバーレイ方式（applyBulkOverlaysTo共通化・renderQueue/loadExistingFiles末尾で毎回適用＝再描画で消えない）②メモは選択モード中↑↓✕📦を非表示（**indexで選択管理するため並べ替え・削除によるズレを構造的に防ぐ**）③メモ一括はgetPrompt→まとめて追記→savePromptの**1往復**（1件ずつN往復にしない）④既存ファイル一括は順次transferFile＋進捗表示、移動分の役割行掃除とPROMPT保存は最後に1回⑤記事切替時はexitAllBulkModes()で選択解除（選択対象は前の記事の項目のため）。
  - 検証：実Chromeで3種類ともUIクリックE2E（キュー2件移動・メモ2件コピー=1追記1重複スキップ・Drive画像1件移動）。コンソールエラーゼロ。
  - **学び：一括操作は「選択の識別子」を安定なものにする**（キュー/ファイル=固定ID、メモ=indexなら選択中の並べ替え禁止とセット）。サーバー往復はまとめられるものは1回に（メモ追記）、まとめられないもの（ファイル移動）は進捗を見せる。

- 2026/07/11：**記事めしPWA：「☑ まとめて選択→🗑 一括削除」を追加（PWA＋GAS v25）**。一括バーに🗑削除ボタンを追加（転送と同じ選択UIを共用）。
  - ①一時保存＝ローカル即削除 ②メモ＝ローカル削除＋現在記事のPROMPT.mdを1回で更新 ③Drive画像＝GAS新アクションdeleteFile。**完全削除ではなくsetTrashed（Driveのゴミ箱行き・30日以内復元可）**＋記事フォルダ所属チェック（誤削除防止）＋削除分の役割行をPROMPT.mdからまとめて掃除。
  - confirmで対象ファイル名一覧（最大8件表示）と「ゴミ箱から復元できます」を明示。台帳の残骸はDeduperのisTrashedチェック（v23）が吸収するので台帳掃除は不要。
  - 検証：実Chromeで3種類E2E（一時保存2件削除・メモ1件削除→Drive PROMPT反映確認・Drive画像1件ゴミ箱行き→一覧から消滅）。エラーゼロ。
  - **学び：ユーザー向けの「削除」は可逆（ゴミ箱）に倒し、確認ダイアログに「復元できる」事実を書く**。破壊操作の心理的ハードルが下がり、誤操作時も救済できる。

- 2026/07/11：**入力欄への⌘Vが「編集後画像の保存ポップアップ」に化けるバグを修正**。原因＝全体pasteリスナーが「クリップボードに画像があればテキスト入力欄でも横取りして取り込む」仕様（過去の意図的判断）だったため、画像コピー直後に補足欄等へ文字を貼ろうとすると画像取込フローが発動していた。修正＝入力欄（input/textarea/contenteditable）へのpasteは素通し、画像取込は「入力欄の外」と専用貼付ゾーン（#paste-target）だけに限定。**学び：グローバルなイベント横取り（paste/keydown等）は「入力系要素は除外」を最初から入れる。「便利だから全域で拾う」は、クリップボードに何が残っているか次第で誤爆する**。

- 2026/07/11：**「補足を入れたのにAIに届かない」を修正＝URLプリフィルの短縮を「先頭300字」→「ユーザー入力優先の要約」に変更**。原因＝ChatGPTの431エラー対策でURL埋め込みは300字に切るが、旧実装は**プロンプトの先頭から**300字を取るため、テンプレ共通の定型文で枠が尽き、中盤にある補足・配色などの**入力値が一切AIに届かなかった**（フル文はクリップボード渡しの想定だったが、06/13に画像単独コピーへ変更した時にこの前提が崩れていた）。修正＝buildUrlPromptSummary_で「1行目（何を作るか）＋入力済み変数（【ラベル】値）＋『詳細は📝コピーで』」を構成（全テンプレ共通で有効）。あわせて20テンプレを静的総点検し、定義済み入力欄の未使用が無いことを確認（「（使用しない）」ラベル欄は意図的仕様）。**学び：切り詰めは「先頭から」でなく「ユーザーが入力した情報を最優先」で。二段渡し（URL＋クリップボード）の片方を変更したら、もう片方が前提にしていた役割が崩れていないか必ず確認する**。

- 2026/07/11：**URLプリフィル上限を「300字」→「実測ベースの5000バイト（日本語約550字）」へ拡大**。ユーザーの「300字以上は無理？」を受け、本人のChrome（ログインCookie込み）でchatgpt.com/?q=の限界を実測＝**URL約6.1KBまで200 OK・6.3KB超で431**。マージンを取ってエンコード後5000バイトを上限に採用し、判定も文字数→バイト数基準へ（ASCII混在で損しない）。要約は「入力値優先＋余った枠にテンプレ本文先頭（写真保護・構図）」まで同梱できるようになり、URL経由だけでかなり実用的な指示が届く。**学び：外部サービスの制限値は推測でなくfetch(credentials:'include')で実測する**（ページ遷移せずCookie込みのステータスだけ確認できる）。上限はCookie量で変動するため実測値ギリギリでなく2割程度のマージンを取る。

- 2026/07/12：**記事めしPWAトップに「🛒 商品情報を取得」コーナーを追加（AIリサーチ方式・API課金なし）**。取得方法はユーザー選択で「AIに貼って取得」＝既存のリサーチ流れに乗せる形（Claude in Chrome方針準拠）。フロー＝①商品名/URL入力→リサーチプロンプト自動生成（【ラベル】固定フォーマットで返させる）→②ChatGPT/Gemini/Claudeに貼る→③回答を貼り戻す→④parseAnswerで項目分解（【ラベル】値・ラベル：値・箇条書きを吸収、`/`区切りは個別項目化）→⑤チェックした項目を3つの反映先へ：**📝メモに追加**（1項目=1メモ・1行厳守）／**🖼画像にリンク**（既存pickImagesForMemo＋setMemoImagesを流用し情報をまとめて画像に紐付け）／**📥プロンプト欄へ**（商品名→タイトル・価格→内容・特徴→補足、**空欄のみ埋め上書きしない**）。
  - 実装は独立IIFE。参照する関数（memos/persistMemoState/renderMemos/pickImagesForMemo/setMemoImages/regenerateAIPrompt）は全てクリックハンドラ内＝遅延実行なのでTDZ問題なし。
  - **バグ2件を検証で発見・修正**：①「🔍リサーチプロンプトをコピー」がbtn-primary既定サイズで3行折返し→bulk-barと同じ`white-space:nowrap`＋コンパクト化。②`.pi-actions{display:flex}`が`[hidden]`属性に勝って取込前からボタンが見えていた→`.pi-actions[hidden]{display:none!important}`で修正（**CSSのdisplay指定はhidden属性より強い、の再確認**）。
  - 検証：実Chromeで4機能E2E（プロンプト生成・22項目分解・メモ22件反映・プロンプト欄マッピング＆上書き無し・画像ピッカー表示）＋取込前後の表示切替。エラーゼロ。
  - **学び：既存の「AIに貼る→貼り戻す」パターンと「メモ⇄画像リンク」の仕組みを再利用すれば、API課金ゼロで"取得→反映"機能を足せる**。取得系機能はまず既存資産の組み合わせで作れないか考える。

- 2026/07/12：**商品情報の取込を「カテゴリ単位に集約」＋AI引用マーク除去**。分解しすぎ（主な特徴が5行）で読みにくい→parseAnswerを1ラベル1件（値は/でつないだまま）に統合、同ラベルはマージ・重複値は除去。cleanValueで `[Brand][1]`・`[1]`（出典番号）・`**太字**`・`[text](url)` 等を除去。
- 2026/07/12：**取り込んだ商品情報を「編集カード（いまAIに送る内容）」「プロンプト準備」からも入力欄へ入れられるように**。原因＝「→入力欄へ」ドロップダウンを上部の商品情報コーナー内だけに置いたため、画像編集中の編集カードにいるユーザーには見えず「ドロップダウンが現れない」となった。対策＝取込結果を共有変数`productInfoItems`に載せ、`openProductInfoPicker()`（各項目×テンプレ準拠の欄セレクトのモーダル）を新設。編集カード(bps-pi-fill)と上部ヘルパー(btn-pi-fill-helper)に「🛒取り込んだ商品情報を入れる」ボタンを追加し、その場から欄へ流し込めるようにした（挿入は共有`piInsertIntoField`＝空なら設定/追記/重複スキップ）。検証：実Chromeでヘルパー版・編集カード版ともE2E（ボタン表示→ピッカー→商品名→タイトル欄反映）。**学び：機能の入口は「ユーザーが今いる画面」に置く。上部に置いた便利機能でも作業中の画面から呼べないと"無い"のと同じ。共有state＋ピッカー関数化で複数画面から同じ機能を呼べるようにする**。

- 2026/07/12：**商品情報を記事ごとにDrive（サーバー）保存できるように（PWA＋GAS v26）**。GAS新アクション`saveProductInfo`/`getProductInfo`＝記事フォルダに`PRODUCT_INFO.md`をsetContent保存（空itemsで削除）。クライアントは①localStorage per-article（即時・オフライン耐性・記事切替やリロードで残る）②「💾 Driveに保存」ボタンでサーバー保存＝別日・別端末でも同じ記事を開けば復元③記事切替/起動時に`piReloadForCurrent()`でlocal即時→Drive上書きの順に読込＋記事未選択で取り込んだ分の引き取り。**バグ発見・修正**：保存ファイルの見出し`# …`・引用`> …`行をparseAnswerが項目として拾っていた→`^#`/`^>`/区切り線を除外。検証：実Chromeで保存→記事切替で消える→localStorage全消し→戻すとDriveから復元（見出し混入なし・3項目）。**学び：メモ(PROMPT.md)と同じ「localStorage即時＋Drive権威」の二層で持つと、オフライン/端末跨ぎ両立。保存フォーマットを自前パーサで読み戻す時はヘッダ/コメント行の混入に注意**。

- 2026/07/12：**商品リサーチのプロンプトを記事めし（ガジェットレビュー）向けに最適化**。EC(Amazon等)の商品説明・AIレビュー要約・「参考になった」が多い口コミ・価格比較・他ブログの実機レビューを横断させ、14カテゴリ（商品名/価格/総合評価/主な特徴/主要スペック/同梱物/高評価の口コミ/低評価の口コミ/競合と違い/ブログ実機レビューでの評判/おすすめ・不要な人/購入前の注意点等）を【ラベル】1行形式で返させる。「出典番号や[ ]記号・太字は付けない」を明記し引用マーク混入を発生源で抑制。**制約と対応**：プロンプトはChatGPTのURLプリフィル上限（実測5000B≒日本語550字）に収める必要がある→528字/4178Bに圧縮し全文自動入力を維持（超えるとbuildUrlPromptSummary_が画像用要約で誤動作するため）。検証：URLに末尾ラベルまで載る＋新フォーマット回答が14カテゴリに正しく分解（口コミも1行集約）。**学び：AIへの指示は「集める情報源（EC/口コミ/ブログ）」と「返す形（ラベル・区切り）」を両方指定すると質が安定。URL経由の自動入力を使う機能は文字数を実測上限内に設計する**。

- 2026/07/27（続）：**スマホ最適化の第2弾＝文字サイズ・余白・画像の軽量化**。ユーザーの追加指摘「四隅に余裕を持たせて」「文字を小さくして最適化」「画像も最適なサイズに」に対応。
  **① 余白**：`#postContent`・`#postHeaderInner` とも左右パディング**0px**で本文もタイトルも画面端に張り付いていた → **左右20px**。1行20字（日本語の適正15〜25字）に収まる。
  **② 文字**：本文17px→**16px**／行間1.9→1.8／段落間33px→26px。h2 23→**21px**・h3 19.5→**18.5px**（第1弾で上げすぎたぶんを戻しつつ、h3が本文と同サイズだった問題は維持して解決）。h1は22pxで適正のため触らない。
  **③ 画像の軽量化（合計 約1.1MB削減）**：
    ・**キャラ吹き出しアイコン**＝800×800（約115KB）を**54px**で表示していた。JIN:Rの `jinr__fukidashiN_image` 設定は**URL文字列**なので、WPが生成済みの `-320x320.jpg` に10スロットとも差し替え。**1,146KB→231KB（80%減）**。150x150版もあるがdpr3端末で甘くなるため320pxを採用。
    ・**商品カード画像**＝Amazonの `_AC_SL1500_`（78KB）を**96px**で表示。`wp_block_builder.py` の `block_product_box` に**正規表現で `_AC_SL320_` へ自動書き換え**＋`loading=lazy`/`decoding=async`/`width/height` を追加。既存9記事15枚も一括置換（78KB→11KB＝85%減）。
  🔴**誤検出の記録**：Browserペインの `naturalWidth` はペイン側の縮小が入るため**実ファイル寸法と一致しない**（アイキャッチが297pxに見えたが実体は1200×630・130KBで正常）。**画像サイズの判定はcurlで実ファイルを取得してPILで測る**こと。
  **学び：JIN:Rのキャラ画像はテーマ設定のURLを差し替えるだけで軽くできる**（再アップロード不要。WPが自動生成した `-320x320` 等がそのまま使える）。
- 2026/07/28：**スマホのメニュー復活＋ホームをガジェマガ風に刷新＋時計マーク退治**。
  **① ハンバーガーメニューが「MENU」の文字だけで空だった原因は2段**：(a) **JIN:Rのハンバーガーは外観>メニューではなく『ウィジェット領域(hamburger-widget)』が中身**で、そこが0個だった → REST APIで `nav_menu`＋`search` を投入（`POST /widgets` で作成後、**`POST /sidebars/hamburger-widget` に widgets配列を明示しないと割り当たらない**）。(b) 過去セッションの `body.page-id-756 .o--widget-area{visibility:hidden;left:-9999px}`（ホームのサイドバー隠し）が**ハンバーガー内の同クラスを巻き込んで**いた → `body.page-id-756 .o--hamburger-menu-container .o--widget-area` の高特異度ルールで解除。
  **② メニュー項目**：ヘッダーメニュー(id=2)に4研究室（カテゴリ）を先頭追加、既存3項目は後ろへ。`POST /menu-items`（type=taxonomy/object=category）。**PCグローバルナビとスマホの両方に効く**（同一メニュー共用）。
  **③ トップの記事カードの巨大時計**：`update_home_cards.py` が日付に付けていた 🕐 絵文字をWPが `<img class="emoji">` に変換し、サイズ指定がなく**110×88pxの時計画像**として描画されていた → 絵文字を削除（コメントで再発防止）。
  **④ ホームのスマホ刷新（PTGL-MOBILE-HOME-V2）**：既存768pxブロックが**ヒーロー背景のキャラ画像を単色グラデで消していた**のが「画像がなくて寂しい」の原因 → `::after` でキャラバナー(150px)をCTA下に復活・タイトル18.5px（20pxだと『で、』だけ折り返す）・カテゴリカード2×2で説明文非表示。
  🔴**最大の学び：`!important`同士の勝敗は特異度で決まる**。過去セッションの `#postContent h2` 系(1,0,1)のID入り!importantルールには、classだけのセレクタでは**bodyの最後に置いても勝てない**。**`:is(body,#wrapper) .クラス` と書くと:is()が引数中最強の特異度(ID)を持つため(1,1,0)となり勝てる**（要素の場所も選ばない）。デバッグ手順：インライン`setProperty(x,'important')`で効く→カスケード負け＝特異度を疑う。
  🔴**スキャン関数の罠**：CSSルール総当たりで `if(r.cssRules){walk;continue}` と書くと、**ネストCSS対応ブラウザではCSSStyleRuleにも空のcssRulesがあるため全ルールをスキップ**する。`constructor.name==='CSSStyleRule'` を先に判定する。
  **残タスク**：目次のスマホ初期閉じ（CSSのdisplay:noneではOPENボタンが壊れることを実証済み→RTOC設定の『閉じておく』は全デバイス共通なのでユーザー判断待ち）。※検索ボタン／ROIボタンは2026/07/29に解消。
- 2026/07/30：**記事605（Garmin Venu 2S）に後継機 Venu 3 / Venu 4 のアフィリを掲載し、本文を2択構成へ改稿**（ユーザー依頼）。
  🔴**着手前に判明した問題**：記事は「今から買うなら Venu 4」と勧めていたのに**Venu 4のリンクが1本も無く**、カード2枚とも Venu 2S（B094VVQR6C）だった。さらに**Venu 2S はすでに現行品ではない**のに、まとめが「今でも有力な選択肢です」で終わっていた（読者が買えない商品を勧める状態）。
  🔴🔴**Amazonのバリエーション罠に再度遭遇**：`Venu 4` で検索して出た B0FS1D95LG を開くと「**サイズを選択してください**」の**未選択の親ページ**で、価格が **￥41,980〜￥80,876** と幅表示。しかも「この商品について」が**VENU SQ2の説明**（Venu SQ2 / Venu 2S / Venu 4 が同一親に統合されていた）。→ **画面で 45mm を選択して確定した B0FS1BXD9Z（69,804円）が正解**。**検索結果のASINをそのまま使うと別モデルにリンクする**。同じく Venu 3 も色違いを選び直して黒（B0CGJDW7T6・47,691円）に統一（記事の主役 Venu 2S が黒のため）。
  **楽天**：Venu 4 は**ガーミン公式店に在庫が無く**、AMI楽天市場店は「在庫の確保ができ次第」表示だったため**Joshin web**（型番010-03014-30一致・大手）を採用。Venu 3 は**ガーミン公式楽天市場店**（010-02784-41）を採用＝**公式ストア優先の原則どおり**。
  **本文の作り**：3列比較表（Venu 2S / Venu 3 / Venu 4）に作り替え、価格行を**「発売時定価」＋「実売（2026年7月30日 Amazon）」の2段**に。誤解を招く「**同じコストで4年後も後悔しないVenu 4を選ぶべき**」（実際は2.2万円差）を削除し、**予算で選ぶ2択**（Venu 3＝実売47,691円で著者の購入額45,000円とほぼ同じ／Venu 4＝+2.2万円で何が増えるか）に変更。
  🔴**体験の創作を防ぐため、本文に「私が実際に4年半使ったのは Venu 2S だけです。Venu 3・Venu 4 は所有していないので、以下はスペックと価格の比較にとどめます」と明記**し、使用感は一切書かない構成にした（7/29制定ルールの実践）。検証スクリプトで「Venu 3/4 を使った」系の記述が0件であることも確認。
  **検証**：カード2→4枚／全7リンク（Amazon4・楽天3）／PR表記がカードより前／価格の全出現が実売と一致／体験創作0件。
- 2026/07/30：**GA4で自分（運営者）のアクセスを除外＝分析の前提を整えた**。
  🔴**調べたら「まったく除外できていなかった」**：①データフィルタ `Internal Traffic` は存在したが**状態が「テスト」**（テストは印を付けるだけで実際には除外されない）②そもそも**内部トラフィックの IPルールが0件**（「ルールはまだ作成されていません」）。**＝自分のアクセスは100%計上されていた**。GA4は初期状態でこのフィルタが「テスト」で作られるため、**作った気になって放置されやすい典型的な穴**。
  **やったこと**：(1) データストリーム > Googleタグ > **タグ設定を行う > もっと見る > 内部トラフィックの定義** で自宅回線のルールを作成（IPv4を `/32`、IPv6は端末ごとに下位が変わるため **`/64` プレフィックス**で登録）(2) データフィルタを **テスト → 有効** に変更（確認ダイアログあり）。
  ⚠️**限界（そのままでは漏れる経路）**：**スマホの4G/5G回線は別IPなので除外されない**／回線のIPが変わったら**ルールの更新が必要**（プロバイダによっては変動する）／**過去データは遡って除外されない**（今後の分のみ）。
  **GSCには除外機能が存在しない**。自分でサイト名を検索して自分の記事をクリックすると**表示回数・クリック・順位に影響する**ため、**順位確認はGoogle検索ではなくGSCの「検索パフォーマンス」で見る**運用にする。
  🔴**追記：IP除外だけでは足りないので「ログイン判定」方式を追加した（同日）**。ユーザーの指摘「ここで言う自分って誰？ モバイル通信やwifi変更しても大丈夫？」がきっかけ。**IP方式は"人"ではなく"回線"を見ている**ため、①同じ自宅Wi-Fiを使う家族も一緒に除外される ②**スマホのモバイル通信・外出先Wi-Fiは除外されない** ③回線IPが変わると無効、という穴があった。
  **解決**：JIN:R設定「計測タグ設定 > Googleアナリティクスの計測タグ」を書き換え、**WordPressがログイン時に body へ付ける `logged-in` クラス**で判定して、ログイン中はgtagを一切読み込まない形にした。**回線に依存しないので、モバイル通信でも外出先でも除外される**。
  🔴**最初 `document.cookie` の `wordpress_logged_in` で判定しようとしたが動かなかった**（実機検証で「ログイン中なのにGA読込済み」と判明）。**SiteGuard等の影響でログインCookieがJSから読めない**ため。→ **bodyクラス方式が正解**（`DOMContentLoaded` 後に判定＝body存在後）。
  **検証（両方向で確認したのが肝）**：ログイン中のChrome＝`logged-in:true / GA読込:false / dataLayer:0`（除外OK）／未ログインのブラウザ＝`logged-in:false / GA読込:true / dataLayer:4`（計測OK）。**片側だけ見ると「読者の計測まで止めた」事故に気づけない**。
  🔴**さらに追記：「どんな状態でも除外できないか」の要望に応え、端末ごとのオプトアウトを追加＝3段構えに（同日）**。ログイン判定だけだと**ログアウト状態・シークレット・別ブラウザ**では計測されてしまうため。
  **仕組み**：`https://www.ootanisatan.com/?ptgl_optout=1` を**1回開くだけ**で localStorage に印が入り、以後その端末・そのブラウザでは**回線もログイン状態も無関係に**計測されない。解除は `?ptgl_optout=0`。設定/解除時は alert で結果を知らせる（非エンジニアでも成否が分かるように）。
  **検証（4パターン全部）**：①一般読者＝GA読込true・dataLayer 5（**計測される**）②`?ptgl_optout=1` 直後＝localStorage `1`・GA読込false（その場で停止）③別ページへ移動（パラメータなし）＝**除外が継続**・GA読込false ④`?ptgl_optout=0`＝localStorage `null`（**元に戻せる**）。
  ⚠️**100%は原理的に不可能**と正直に伝えること：**シークレットウィンドウ／ブラウザデータ削除後／未設定の別ブラウザ**は漏れる。ただし3段構えで実用上ほぼ塞げる。
  **現在は3段防御**：①端末オプトアウト（URLを1回開く・全回線/全ログイン状態）②ログイン判定（本人・全回線）③GA4のIP除外（自宅回線・家族含む）。**ログアウト＋オプトアウト未設定の端末では計測される**ので、素の見え方を確認したいときはむしろ好都合。
  **JIN:Rには「ログイン中は計測しない」設定がない**ことも確認済み（GAタグは JIN:R設定 `jinr_google_analytics_tag` に直接貼られており、標準では全訪問者に配信される）。
- 2026/07/30：**アフィリカードを「導入のすぐ下」にも設置＝全12記事の収益導線を前倒し**（ユーザー提案）。
  🔬**実測で機会損失を確認**：カードは**12記事すべてで平均76%地点**にしかなく、**冒頭25%以内にある記事は0本**だった。記事を最後まで読まない読者には「導線が存在しない」のと同じ状態。→ **「急いでいる人へ（3行まとめ）」の直後**にカードを複製設置し、**平均16%・冒頭30%以内が10/12本**に改善。
  **押し付けない工夫**：カードの前に「詳しい理由は本文で書きますが、**先に商品だけ確認したい方はこちらからどうぞ**」と一言添える（急かさない・命令しない＝7/29の「偉そう禁止」ルールと整合）。
  🔴🔴**同時に発覚した法令リスク：PR表記が全12記事で広告リンクより後ろになっていた**。PR表記は記事末尾（88〜95%地点）にしかなく、冒頭にカードを置いたことで**「広告と知らないまま広告に触れる」状態**が生まれた。ステマ規制（景表法）は「広告と気づける状態で広告に触れさせる」ことを求めるため不十分。→ **冒頭カードの直前に簡潔なPR表記を挿入**（`ptgl-pr-top` クラス・楽天リンクの有無で文言を自動出し分け）。末尾の詳細版は残置（二重表示は問題なし）。全12記事で `PR位置 < カード位置` を検証済み。
  **学び：収益導線を前に出す変更は、必ず広告表示の位置とセットで点検する**。カードだけ動かすと法令面が壊れる。
  **常設スクリプト**：`blog/scripts/add_top_product_card.py`（冒頭カード一括設置・二重設置は自動スキップ）／`blog/scripts/add_top_pr_notice.py`（冒頭PR表記一括設置）。ルールは SKILL.md「💰アフィリエイト掲載の標準」D-2・E に追記。
- 2026/07/30：**楽天リンク未対応だった3記事（450・526・873）を対応＝レビュー記事12本すべてに楽天導線が揃った**。
  🔴**新規生成はゼロ**。台帳を先に確認し、`a.r10.to/hYLZNi`（Keychron K1 Max）と `a.r10.to/hg5XXN`（MX ERGO S）を**既存リンクのまま流用**した（同一商品でリンクを増やすと計測が分散するため）。
  🔴**アフィリを触るときは価格を取り直す**（memory `feedback_blog_price_consistency` のルール）を実践し、**3記事とも価格が古いことを発見**：450は記事「約25,000円」→実売**19,360円**、526・873は「18,000円」→実売**16,091円**（Amazon商品ページで直接確認）。値下がりなのでROIは実際より不利に書かれていた。
  **下流の数字も全部引き直し**：450＝損益分岐83日→**136日**・3年累計32.8万→**15.6万円**・純利益30.3万→**13.7万円**・1日コスト23円→**18円**／526＝回収81日→**73日**／873＝損益分岐95日(4.3ヶ月)→**85日(3.9ヶ月)**・3年純利益12.4万→**12.6万円**・時給1,500円版20.7万→**20.9万円**・時給3,000円版43.2万→**43.4万円**・1設定の回収割合「約3割」→**約4割**。
  🔴**450だけ時給2,000円で計算されていた**（旧ルール時代の記事）→ 全社標準の**950円（全国最低賃金水準）**に統一。記事間で時給が違うと読者が混乱するため、価格修正と同時に直した。
  **カード画像で新ルールが早速効いた**：最初にASINから画像URLを**推測して書いた**が（`71OjrkNGWDL`）、商品ページで確認したら**実際は `61WxPz0z2IL` で誤り**だった。7/29に定めた「根拠のないことを書かない」ルールに従って実機確認したため公開前に防げた。**画像URLは必ず商品ページの `#landingImage` から取る**。
  **置換は全て `assert count==1` で件数検証**してから投稿（silent failで直したつもりを防ぐ）。検証は本番HTMLをcurl＋DOM実測（カード740×137px・画像320×193読込・ボタン2つ rel="sponsored nofollow noopener"）。
  ⚠️**未解決**：この日、Browserペイン／Chrome双方で**記事下部のスクリーンショットが白飛び**する現象が続いた（DOM上は要素が存在し画像も読込済み）。原因未特定。**スクショが白いときはDOM実測で代替判断してよい**が、見た目の最終確認はユーザーの実機に委ねる。
- 2026/07/29：**リベシティ合宿（動画97分）の知見を3点まとめて実装＝レベルアップ**。PDF53ページ＋YouTube字幕45,932字を分析し、**動画にしかない情報**を抽出（サブエージェント3体で分担）。
  **① 記事のローカルミラーを新設**（`blog/scripts/sync_posts_to_local.py` → `blog/posts/`）。全13記事を .md 化（264KB）。WPブロックHTML→Markdown変換（見出し/表/リスト/引用/吹き出し→`> 💬`／ショートコード除去）。frontmatterに post_id・URL・公開日・最終更新日・カテゴリ。**冪等**（2回目は差分0）。⚠️**このミラーは読む・探す用の写しで、編集してもWPには反映されない**（正本はWP）。
  **実測効果**：全記事の横断点検が **API13件≒6秒＋トークン → ローカルgrep 0.008秒・トークン0**。今日は同じ取得を4回繰り返していた。楽天リンク未対応記事（12/450/526/873）の特定も一瞬。工程表に**⑪ミラー同期**を追加（省略禁止）。
  **② 「体験を創作させない」を最重要禁止事項に**（writing.md §16 の最上位／SKILL.md 7-d／memory `feedback_no_fabricated_experience`）。合宿講師が自身の失敗として「**実際にやったことのないことを、やったかのように書いてくれる。ブログの信頼性がなくなる**」と警告していた。当ブログの**記事552 ASIN誤り（7/26）と同根**。防ぎ方は3点セット（先にヒアリングさせる／ヒアリング内容以外を予想で書かせない／新製品は自分から情報を与える）。
  **③ 依頼文の定型を新設**（writing.md §14-b）。合宿で講師が実際に打っていた型：**(a)既存記事の修正は「修正した箇所については、どこをどう修正したかを教えて欲しい」を必ず足す**（差分を把握しないまま公開するのがいちばん危ない）**(b)テーマ案出しは「確認したいことはヒアリングして欲しい」**（逆質問させると眠っていたネタが出る）**(c)新記事は4点セット**（ヒアリング／予想で書かない／文字数／**記事の目的**）。
  **合宿との差分（うちが上）**：講師は「**自動投稿はされない・URLを渡す部分は手動**」と明言。うちはREST APIで投稿も更新もミラーも自動。プラットフォームリスク（noteは運営終了で記事が消える）も独自ドメインでクリア済み。
  **合宿の本音で刺さった点**：「**検索意図を満たす力はClaudeの方が上（自分が負けていると思うことがある）／自分らしさは人間が勝つ。ただし過去記事を連携するほど口調を読み取って自分らしさが出る**」→ ローカルミラーは横断点検だけでなく**文体を保つ土台**でもある。
- 2026/07/29：**YouTube動画を「資料」として読み込む手段を用意**（`blog/scripts/youtube_transcript.py`）。リベシティ合宿等の動画教材をPDFと同じように分析したい、という要望から。**URLを渡すだけで字幕をタイムスタンプ付きテキスト化**する（公式字幕→日本語自動字幕`ja`/`ja-orig`→英語 の順に採用）。
  🔴**ハマりどころ**：yt-dlpの既定（webクライアント）は**YouTubeのボット検出で `The page needs to be reloaded` を返す**。`extractor_args={"youtube":{"player_client":["android"]}}` にすると通る。スクリプトは android→ios→tv→mweb→web_safari の順に自動フォールバックする。
  **実測**：6分25秒の動画で3,494字・取得3秒。53ページのPDFが5,226字だったので、**10分程度の動画1本＝スライド資料1本相当**の情報量。
  **使い分け**：字幕がある動画はこれ（無料・速い）／字幕なしの動画は音声の文字起こしが必要（記事めしPWAのGemini経路など）。ログインが要る限定公開動画はClaude in Chrome経由。
- 2026/07/29：**記事冒頭の並びを標準形に統一（post 703）**。ユーザー指摘「いきなりメニュは良くない、他の記事に従って修正して」。**目次(RTOC)は最初のh2の直前に自動挿入される**のが正体で、703だけ本文が `h2「急いでいる人へ（3行まとめ）」` で始まっていたため、**アイキャッチ直後に目次**が出ていた。他12記事はすべて「導入文（引用/段落2〜6ブロック）→ separator → h2 3行まとめ」の順。
  **対応**：703の先頭にあった「hr＋h2まとめ＋リスト＋hr」を導入文3段落の後ろへ移動（**並べ替えのみ**。ブロック構成と文字の内訳が一致することを `Counter` で検証してから投稿）。結果「アイキャッチ → 導入文 → 目次 → 3行まとめ → 吹き出し → 本編」。
  **横展開**：`scratchpad/check_head.py` で全13記事を点検 → **要修正0件**（残りは元から標準形）。ルールを `writing.md` §6 に追加（本文は必ず導入文40字以上から始める）。
  **学び：見た目の不具合が"設定"ではなく"ブロックの並び順"に起因することがある**。目次プラグインは位置を選べないので、**h2の前に何を置くかで制御する**。
- 2026/07/29：🔴**「読者に偉そう」を全記事から排除＋執筆ルール自体を修正**。ユーザー指摘「態度がいきなりドヤ顔は腹が立つ／ドヤ顔自体はいいが読者に偉そうな態度は絶対にするな」。
  **真因はルール側**：`blog/skills/writing.md` §14 が模範例として「結論を先に言い切る：**『結論から言う。このツールは買いだ』**」を掲げていた。記事だけ直しても次で必ず再発するため**ルールを廃止・書換**した（新例「結論からお伝えすると、これは買いです」）。
  **① 冒頭の構造**：post 703 は区切り線の直後に**いきなり吹き出し**で結論を言い渡していた → 吹き出しを導入文の後ろへ移し、順序を **「挨拶・導入文 → 3行まとめ → 吹き出し」** に。§6に「冒頭を吹き出しで始めない」を明記。
  **② 語尾は相手で変える**：所長のセリフは**タナカ相手なら常体でOK**（キャラの距離感）。**読者に直接向けるセリフと地の文は必ず敬体**。この区別がないまま「オオタニは断言口調」とだけ書かれていたのが混乱の元。
  **③ 全13記事を機械スキャン**（`scratchpad/scan_bossy2.py`）。地の文の「〜だ。」は138箇所あったが**これは文体であって偉そうではない**ので触らず、**読者への命令形・詰問・見下し・言い渡しだけ8箇所を修正**：703「結論から言う→結論からお伝えすると」／873「今日始めて…回収していけ→今日始めれば…戻ってきます」「誰がそう決めた？…カウントしてみろ→そこ、一度数えてみてほしい」／836「だから損してたんだ→それは少しもったいない」「回収していけ→戻ってきます」／552「タナカ君、甘いな→そこは意外と見落としがちだ」／526「君にはまだ分からんようだな→順を追って話そうか」／927・605「〜すべきだ→〜が向いている/おすすめしたい」。
  **判定基準を明文化**：**そのセリフを初対面のお客さんに face-to-face で言えるか**。言えないなら書かない（SKILL.md 7-c／writing.md §16／memory `feedback_no_condescending_tone`）。
  **学び：ユーザーが怒った箇所を直すだけでは不十分。"その表現を生んだルール"まで遡って直さないと再発する**（今回はSKILL側に模範例として載っていた）。
- 2026/07/29：**ハンバーガー内の仕上げ（PTGL-MENU-POLISH）＋カード見出しの途中切れとヒーロー吹き出しの見切れ修正（PTGL-CARD-FIX）**。
  **① 検索ボタンが灰色の空箱**：JIN:Rの送信ボタンは `<input type="submit" id="JinrSearchSubmit" value="">`＝**ラベルが空のまま**で、虫眼鏡アイコン(`.jin-ifont-search`)は隣の`<span>`に別途置かれている構造。→ spanを青いボタン(56×46px・角丸10px)に見せ、アイコンを中央配置(pointer-events:none)、**inputは`opacity:0`で全面に重ねてクリック領域**にした。`elementFromPoint`でボタン中心が実際にinputであることを確認し、実際に「Keychron」で検索して4件ヒットまで確認済み。
  **② ROIボタンの見切れ**：`.menu-item-851>a` が `white-space:nowrap` のため画面外へ。スマホのみ13.5px・折り返し可・中央寄せ・**文字色を白に強制**（ハンバーガー内は濃紺が勝っていた）・リンク右の`›`を非表示。結果は1行に収まった。
  🔴**③ 最大の学び：`overflow:hidden` は padding ボックスまでしかクリップしない**。記事一覧の見出しで「3行目が半分だけ見える」不具合の真犯人がこれ。`max-height` を行の整数倍にしても、**padding-bottom の領域に次の行が描画されて見えてしまう**。→ **下パディングを0にし（余白は margin-bottom で確保）、max-height＝行数×行高＋上パディング**にすると行の途中で切れなくなる。
  🔴**④ flex item では `display:-webkit-box` が blockify されて `-webkit-line-clamp`（末尾の…）が効かない**。JIN:Rのタイトルh2は親`.c--post-contents`がflexのため無効化されていた（computed displayが`flow-root`になるのが目印）。親のflexを解除しても復活しなかったため、**…は諦めてmax-height方式で確実に止める**方針に切替（line-clampは併記だけ残す）。
  **⑤ ヒーロー画像の吹き出しが切れる**：`.ot-hero::after` の `background-position:58% 30%` が原因。元画像(2172×724)の吹き出しは**右端98%の位置**にあるため、スマホ幅では右が落ちていた → `100% 35%`（右揃え）に変更。切れるのは左の余白部分だけになり、キャラの大きさも保てた。
  **⑦ 記事一覧の見出しは結局「全文表示」に変更（PTGL-CARD-FULLTITLE）**：2行で打ち切ると何の記事か分からないというユーザー判断。行数制限を外すと本文幅が184pxしかなく6行になったため、**サムネイルを110→96pxに詰めて幅198pxを確保・文字14.5px**で4〜5行に収めた。副産物として日付とカテゴリタグが1行に並び収まりが良くなった。ホームの注目/最新は手組みカード（`.ot-featured-card`/`.ot-latest-item`）で**別物なので影響なし**（テーマ製`.o--postlist-item`はホームに0個）。
  **⑧ ROI診断ボタンをメニュー先頭へ（PTGL-MENU-ORDER）**：`POST /menu-items/<id> {menu_order:n}` で851を1番に、他を1つずつ後ろへ（**WAF対策で0.6秒間隔**）。ただし**同じメニューをPCのグローバルナビも使う**ため、PCではロゴのすぐ隣にオレンジのピルが来て**ナビが2行に折り返す**崩れが出た → `@media (min-width:769px){ .o--nav-box .menu-item-851{ order:99 } }` で**PCだけ表示順を最後に戻す**（DOM順は変えずCSSのorderで見た目だけ入れ替えるのが安全）。**教訓：メニューの並び替えはスマホだけの変更にならない。PC側を必ず確認する**。
  **⑨ トップのカード見出しも全文表示に＋末尾の巨大な空白を除去（PTGL-HOME-CARD-FIX）**：注目/最新も13px・全文表示へ。最新は「サムネ｜見出し｜日付」の**横一列flex**のため見出しが伸びると日付と重なった → **grid（左サムネ・右に見出しと日付の2段）に組み替え**て解消。
  🔴**⑩ ホーム末尾の3,600px超の空白の正体は `visibility:hidden` にしたサイドバー**。`#mainSideBar` 自体は `position:absolute` で高さ0でも、**中身(`#sideBarWidget` 7,352px)がスクロール範囲を広げていた**。`body.page-id-756 #mainSideBar{display:none}` で3,458px削減（7,376→3,918px）。**教訓：非表示は `visibility:hidden` ではスクロール範囲が残る。消すなら `display:none`**。ハンバーガーは `#hamburgerWidget` という別IDなので巻き込まない（前回の事故と違い**ID指定なら安全**）。
  🔴**⑥ Browserペインのスクリーンショットは1手前の描画を返すことがある**。CSSを変えた直後に撮ると「効いていない」ように見えるが、`getComputedStyle`は正しい値を返している。**もう一度撮る／少しスクロールしてから撮る**と正しい絵になる。**computedと見た目が食い違ったら、まず撮り直す**（今回これで30分ほど無駄にした）。
- 2026/07/27：**スマホ可読性の改善（全記事に効くCSS＋強調の間引き）**。ユーザー指摘「スマホで文字や画像がいっぱいで見にくい」を実測して原因を特定。🔬**測定（375px幅・post 836）**：本文17px/行間1.9＝**文字サイズは問題なし**。真因は2つ。①**水色アンダーラインが78箇所**（9,142字＝約117字に1回。59段落中17段落で段落の4割以上が強調＝全部目立って何も目立たない）②**h3が17pxで本文と同サイズ**・h2も20px（本文の1.18倍だけ／目安1.4〜1.6倍）＝**見出しが埋もれ構造が見えず「文字の壁」に見える**。表は7つ・13.5px。**対応A：追加CSS（全記事一括・スマホのみ）** `@media (max-width:768px)` で h2→23px・h3→19.5px・表→14.5px＋セル余白9px＋奇数行ゼブラ。🔴**セレクタの落とし穴**：`.jinr-article h2.jinr-heading` は `!important` でも特異度(0,8,2)でも**効かない**。JIN:Rは**ID起点**で指定しているため **`#postContent h2.jinr-heading` が必須**（`#postContent`＝記事本文section／`#jinrArticle`＝article／h2自身にも目次プラグインのid `rtoc-N`）。追加CSSは既に**51,956字**あるため**末尾に追記のみ**（先頭一致で無傷を検証／マーカー `PTGL-MOBILE-READABILITY` で二重追記を防止）。**対応B：強調の間引き** `blog/scripts/deemph.py <post_id> [--publish]` を新設。1段落1箇所に絞り、残すのは「価値の高い数字（金額・倍率＞時間＞数字）／同点なら後ろ＝結論側」。**本文テキストは1文字も変えず span だけ外す**（assertで検証）。836で 78→43箇所（段落内64→29）。**学び①：「見にくい」は文字サイズとは限らない。まず実測してから直す**（今回は文字を大きくする必要が一切なかった）。**学び②：JIN:RのCSS上書きはID起点で書く**。効かないときは特異度ではなくセレクタの起点を疑う。**学び③：吹き出しの15pxはみ出しは「しっぽ」＝仕様**。横スクロールが出ていなければ直さない（不具合と誤報告しない）。
- 2026/07/26：**記事めしPWA「🛒商品情報を取得」でURL入力を実用化**（PWAのみ・GAS改修なし）。URLは元から受け付けていたが**生URLをAIに渡すだけ**で、①短縮リンク（`link.amazon`/`amzn.to`/`a.co`/`amzn.asia`/`a.r10.to`）は展開先が分からずAIが商品を特定できない or **別商品を答える**、②ASINを抜いていないので取り込んだ情報からアフィリカードが作れない、という穴があった。実際この日、記事552の変換アダプタで**記事記載のASIN(B074PQPLDQ)が実機と別商品**という事故が起きている。触った箇所は `blog/pwa-cloudflare/`：①app.js `analyzeProductUrl()` 新設（Amazonの`/dp/` `/gp/product/` `?asin=` からASIN抽出＋トラッキングパラメータを落とした正規URL生成／楽天は`item.rakuten.co.jp/<shop>/<code>/`から商品コード抽出）②`piRenderUrlHint()` 新設＝入力欄の下に「何を読み取ったか」を帯で即時表示（短縮リンクは黄色で警告・ASIN認識は緑）③`buildProductResearchPrompt()` を分岐化＝短縮リンクには「まず展開して実際の商品ページを特定してから調べる／**展開できなければ推測せず『特定できず』と返す**」を明示（ハルシネーション封じ）、通常URLには正規URLとASINを明示④回答フォーマットに`【ASIN】`と`【商品ページURL】`を追加⑤index.htmlのplaceholder/ヒント⑥styles.cssに`.pi-url-hint`。**バグも同時修正**：`parseAnswer`の`addTo`が値を`/`で分割するため、URL値が`https:` `/` `www…`に砕けていた→URLは分割しないガードを追加。検証：`node --check`＋dev server(:8792)で4パターンE2E（短縮/Amazon dp/楽天/商品名）＋パーサ再現テスト＋console 0エラー→`wrangler pages deploy . --project-name=blog-capture`→**本番URLでも3パターン再確認**。**学び：URL入力欄は『受け付ける』だけでは不十分。何を読み取ったかをその場で返し、読み取れないケース（短縮リンク）はユーザーに代替手段を示す**。AIに渡すときも「分からなければ推測するな」を必ず入れる。
- 2026/07/19：**記事めしPWA「つくるもの」に🖊️商品説明テンプレを追加（PWAのみ・GAS改修なし）**。最終仕様＝**実機写真に注釈・矢印・図形（囲み）を重ねて"この部分はこう"と指し示す説明図**。⚠️初回は「商品名＋キャッチ＋推し3つの概要カード」で作ったがユーザー指摘で全面作り直し＝**「商品説明」は概要を1枚にまとめるものではなく、注釈で部分を指し示す"説明画像の1枚"（ここで全部を説明しきらない・複数枚使う前提）**。入力欄＝見出し／注釈(部分＝説明・/区切り2〜4個)／強調・図形の指示(任意)／配色。画像プロンプトは「添付写真はそのまま・注釈だけ重ねる／指定ポイントのみ・詰め込まない／引き出し線・矢印(オレンジ)・角丸ラベル・囲み図形」を明示。触った箇所は全て `blog/pwa-cloudflare/`：①index.htmlの`ai-template-select`（＝選択肢の唯一のマスター。モーダル/バナー/ラベルはここのinnerHTMLを複製するので1箇所で全波及）②app.js TEMPLATE_TO_ROLE（`product_desc:'section'`＝複数枚OK）③TEMPLATE_FIELDS④buildResearchPrompt⑤AI_TEMPLATES（末尾`${COMMON_GUARDS}`）⑥TEMPLATE_PLAN（rank44・本文の注釈つき説明）。検証：`node --check`＋dev server(:8792)で実機E2E（選択肢/欄ラベル切替/生成プロンプトへ入力値差込/console 0エラー）→`wrangler pages deploy . --project-name=blog-capture`で本番反映。**学び①：記事めしのテンプレ追加はこの6箇所が定石（役割=既存グループと同じにすればGAS/article_from_meshi.py改修は不要）**。**学び②：本番curl検証は`/index.html`が308で`/`へ飛び0バイトになる→`curl -sL`でルート`/`を見る。さらにエイリアス(blog-capture.pages.dev)はエッジキャッシュで最大30秒遅延＆旧アセットを返すことがある→デプロイ固有URL(xxxx.blog-capture.pages.dev)で先に権威確認し、エイリアスは`?v=$(date +%s)`のキャッシュバスター付きで再確認**。**学び③：テンプレ名だけで意図を推測せず、"何を作る画像か(概要か/注釈か)"は着手前にユーザーへ具体案で確認する（今回1往復の作り直しが発生）**。**運用メモ：wrangler OAuthは期限切れになることがあり、その時はユーザーに`wrangler login`（ブラウザでAllow）を依頼→再デプロイ**。

- 2026/07/19：**画像編集スキル（gazo-edit）新設＝「左AI生成×右Canva」2分割ワークフロー**。起動語「画像編集」。ユーザーの不満「記事めしのCanvaボタンはただURLを開くだけ」への回答。仕組み：①Canva Connect APIで**枠デザイン（DAHPxq8x0v8・1200×630・空・『削除しない』命名）を複製**→編集URL取得 ②osascriptでChromeを左右2分割（左＝chatgpt.com/?q=プロンプト自動入力・右＝複製キャンバスの編集URL・既存ウィンドウ再利用）③ユーザーは左で生成→画像コピー→右で⌘V→手動編集。書き出しは「書き出して」でAPI代行（export-design png）。**実測での重要知見**：(a) Canvaの `?create&width=...` URLパラメータは**効かずホームへ飛ぶ**→「URLを開くだけ」問題の根本原因。API複製方式が唯一の「直接キャンバスに着地」ルート (b) Canva APIは白紙デザイン新規作成不可・画像単体のimport-designも`invalid_file`→**既存デザインをcopy→resize(custom)→全要素delete**で空枠を作るのが正解 (c) `copy-design`の複製は**枠と同名になる**ため毎回リネーム必須 (d) Canva Connect APIは**無料**（従量課金なし）・非公開Drive画像は取り込めない（公開URL限定）。枠再作成手順はSKILL.mdに記載。ファイル：`.claude/skills/gazo-edit/SKILL.md`＋`.claude/commands/画像編集.md`/`gazo-edit.md`。⚠️Git同期に乗るのでこのスキルは2台目PCでも使える（Canva MCP接続が前提）。掃除メモ：検証で生じた中間デザイン「【枠】…のコピー元サイズ版」(DAHPxgWX5O8)とCanvaアップロードのテスト画像「記事めしCanvaテスト_heroPNG」は不要（ユーザーがCanva上で削除可）。デモ複製(DAHPxuyhuLE)は最初の作業キャンバスとして利用可。

- 2026/07/19：**Canva⇄記事めしの往復が完成（`blog/scripts/canva_to_meshi.py` 新設）**。「Canvaで仕上げた画像を記事めしに戻す」経路。仕組み＝Canva `export-design`(png/1200×630)のダウンロードURL → スクリプトがDL → base64 → **記事めしGASの `uploadSmall` へPOST**（20MB上限・`articleFolderId`でフォルダ直指定・`token`はconfig.jsから読む）。ファイル名を `eyecatch_YYYYMMDD_HHMMSS.png` 等にするとGASの `normalizeFilename` がprefixを保持するので**そのまま役割つき画像として記事めしに載る**。🔴 **設計上の肝：画像バイナリをAIの会話コンテキストに通さない**（Drive MCPの`create_file`はbase64を引数で渡す必要があり、1〜2MBの画像で数十万トークンを消費して破綻する。curl→curlでプロセス内完結させるのが正解）。疎通確認は `--ping`（`{"ok":true}`が返る）。同一画像の再取り込みはGASのハッシュ判定で「重複スキップ」→仕上げを変えて再書き出しするか `--name` で別名。**学び：外部SaaS(Canva)と自前基盤(記事めしGAS)を繋ぐときは、既存のアップロードAPIを再利用すればGAS改修ゼロで済む。バイナリの受け渡しは「AIを経由させない」設計にする**。

- 2026/07/20：**画像編集スキルを「Chromeのタブ分割ビュー」方式に全面変更（ユーザー発案が正解だった）**。旧方式＝macOSでChromeウィンドウ2枚を左右に並べる → **ウィンドウが別スペースへ飛んで見失う事故が多発**し、専用デスクトップ化(macOS Split View)も私には実行不可（ブラウザはcomputer-use tier=read でクリック/キー入力が全ブロック、Spaces操作のAPIも無し、teachモードも当セッションでは利用不可）。→ ユーザーが**Chrome標準の「新しい分割ビューにタブを追加」**（1ウィンドウ内で左右2分割）を発見し、これで一挙解決。**🔴 分割ビューの"作成"はAppleScriptから不可（Chrome 150の辞書にsplit/tile項目なし＝実測）が、"中身の差し替え"は可能**（分割中のウィンドウもtabsとして見え、`set URL of t` が効く）。よって新方式＝**ユーザーが一度だけ右クリックで分割ビューを作り、以降Claudeはそのウィンドウを探して各タブのURLを入れ替える**（記事が変わっても同じ画面構成を再現）。専用デスクトップ化も「1ウィンドウだから緑ボタン1クリックで全画面→自動で専用スペース」と激減。**学び①：ウィンドウを2枚並べる設計は、macOSのSpacesと相性が悪く事故る。1ウィンドウ内で完結する仕組み（アプリ側の分割機能）があるならそちらが構造的に強い**。**学び②：自動化できない一点（分割ビューの作成）は"毎回"ではなく"初回だけ"に押し込めば実用上ほぼ自動になる**。差し替え時の注意＝AI側タブに入力途中のテキストがあるとページ遷移で消えるので確認する。
- 2026/07/20：**記事めし⇄画像編集スキルの連携が完成（`blog/scripts/meshi_context.py` 新設）＝往復ループ閉じた**。「記事めしでChatGPT/Gemini生成 → Canva分割編集 → 記事めしへ戻す」が一連でつながった。①**現在の記事の特定**＝GASの`listArticles`は更新日時を返さないので、Drive MCPで**役割prefix付き画像を新しい順に1件取り、その`parentId`を記事フォルダとする**（実測でDORIBIRUを正しく特定）②`meshi_context.py --folder-id X` がGASの`getPrompt`から記事タイトル＋登録画像＋**各画像の用途（つくるものテンプレ名）**まで復元（PROMPT.mdのメモ行を正規表現で分解）③その記事名でCanvaキャンバスを作り、分割ビューに流し込む④仕上げ後は`canva_to_meshi.py`で同じ記事フォルダへ戻す。**学び：PWAのlocalStorage（選択中の記事）はClaudeから読めないが、「最後に増えた成果物から逆算する」と現在の作業対象は十分な精度で特定できる**。GAS改修ゼロ（既存のlistArticles/getPromptを読むだけ）。

- 2026/08/01：🔴**「同じミスを二度と起こさない」仕組み化＝検品の統合ゲート化＋公開後フックの自動化**（ユーザー指示「すべて改善して、同じようなミスがおこらないよに」）。この2週間の事故実績（価格不一致8回・PR表記順序事故・ミラー2週間放置・SNS拡散ゼロ・画像116枚でセッションCRIT）を根拠に、多角的な仕組みを一気に実装。
  **① `blog/scripts/article_lint.py` 新設（公開前ゲートの一本化）**：バラバラだった7つの点検（冒頭構造／偉そう表現／PR表記の位置／カード位置／価格割れ疑い／markdown残骸・表情記法露出／体験創作ワード`--unowned`）を1コマンドに統合。`--all`（全記事）／`--post <ID>`／`--slug`（公開前のローカル原稿）。ERROR=公開ブロック・WARN=目視・INFO=価格一覧。
  🔬**初回実走でいきなり本物を検出**：公開中の3記事に残骸6件が眠っていた（**873**＝mdリンク`[…](…)`が生のまま露出＋タナカのセリフが吹き出しブロックでなくプレーン段落で`[ニヤ顔]`が露出／**836**＝md画像`![…](…)`とmd見出し`#### `×4が段落のまま／**450**＝迷子の`**`×2＋鉤括弧のズレ＋カードalt内の`**19,360円**`）→ 全件を `assert count==1` 方式で即修正・再lintでERROR 0件を確認。**手動grepでは2週間見逃していたものが、統合lintの初回で全部出た＝ゲート化の効果の実証**。
  **② `publish_article.py` に統合**：ドライラン時に validate_blocks＋article_lint が自動で走り、**ERRORが1件でもあると実投稿を自動ブロック**（回避`--skip-lint`は原則禁止・使用時は理由をMEMORYへ）。個別チェックの追加先は今後 article_lint 側に一本化（--allでも同じ基準が効くように）。
  **③ 公開後フックも自動化**：公開成功時に**ミラー同期（sync_posts_to_local）を自動実行**＋新規公開なら **`sns/queue/<日付>-<ID>-<slug>.md` を自動生成**（X原稿2案をその場で書き足してから作業を終える運用。「公開したのに拡散ゼロ」の再発防止。運用は sns/queue/README.md）。
  **④ `blog/scripts/suggest_internal_links.py` 新設**：ミラー（blog/posts/）を土台に、対象記事と共通の話題（商品名・技術用語をIDF重み付け）が多い既存記事を提案。実測：605に対し703（同ジャンルのスマートウォッチ）が最上位＝妥当。
  **⑤ スキル2本新設**：起動語「**楽天リンク**」（台帳確認→公式ストア→バリエーション確定→短縮URL→台帳記入の定型化・`.claude/skills/rakuten-link/`）／起動語「**ブログ月報**」（GSC/GA4記録→article_lint --all→**リライト対象と次ネタの決定まで**・`.claude/skills/blog-monthly/`。分析が7/17の単発で止まっていた問題の定例化）。
  **⑥ ルール昇格（SKILL.md）**：(a) 3段昇格に**④「機械で検出できる失敗はlintへ昇格」**を追加＝文章ルールは読み忘れで再発するので、2回目からは publish が自動で止める形まで持っていく (b) 工程⑦をlintゲートに書換・**⑫SNS原稿キュー**・**⑬内部リンク点検**を追加 (c) クイックリファレンス13「**検証はテキスト優先・スクショはユーザー向け最終確認の1〜2枚だけ**」（画像多用がセッションを最も早く殺す。実測：画像116枚でCRIT・圧縮2回）・14「月次分析はブログ月報スキルで定例化」。
  **学び：ルールを増やすほど読み忘れリスクが増える。「文章のルール」は最終的に「実行時に自動で走るチェック」まで昇格させて初めて再発が止まる**（今回の残骸6件は、ルール自体は存在したのに人（AI）が7本の点検を覚えて回す運用だったために漏れた）。

- 2026/08/02：**記事めしPWAへCodex画像生成の往復接続を実装し、安全化して本番公開**。①「🧠 Codexで画像生成」「📥 Codex画像を取込」を追加 ②初回のみCodexワークスペース絶対パス＋ローカル受け渡しフォルダを端末内に保存 ③生成時に全文プロンプト・記事名・用途・保存先・1800px上限・Canva任意方針を`meshi_codex_job_*.json`へ保存 ④編集時は元画像も`_codex_source_*.png`として同フォルダへ保存 ⑤公式`codex://new?prompt=&path=`で新規Codexタスクを開き、`$imagegen`実行指示を入力済みにする（安全仕様上、送信1回はユーザー）⑥完成PNGを同フォルダから読み戻し、新規生成なら用途付きで一時保存へ、編集なら元画像を置換 ⑦取込時にも長辺1800px以下へ再検査・縮小。**安全化**：Cloudflare Worker Static Assetsへ移行、`preview_urls:false`、Cloudflare Accessで本人メール1件のみ許可、Worker内でもAccess JWTを署名/AUD/issuer/期限検証。旧共有トークンは破棄して新規生成し、Cloudflare Secret＋GAS Script Propertiesへ分離保存。旧Pagesは過去デプロイ138件とプロジェクトを削除。**設計上の学び**：長い画像プロンプトをディープリンクへ直載せせず、ローカルJSONへ落として短い参照指示だけ渡すとURL長制限と欠落を避けられる。ブラウザとCodexの間は「フォルダ＝受け渡し口」にすると、画像バイナリを会話へ手貼りせず往復できる。検証：`node --check`／`git diff --check`／ローカル実画面でボタン・未設定案内・390px幅・consoleエラー0／本番未認証302／本人ログイン後の記事一覧取得を確認。
- 2026/08/02：**記事めしのローカル補助スクリプト3本も秘密情報を分離**。`article_from_meshi.py`・`meshi_context.py`・`canva_to_meshi.py`は共通`gas_config.py`を使い、環境変数またはmacOSキーチェーン（service=`article-meshi-gas-token`）からローカル専用トークンを取得する。GAS側はWorker用とローカル用を別々のScript Propertiesで保持し、どちらもソース・ブラウザへ配信しない。初期化用エンドポイントは登録直後に削除し、GAS v34へ反映。3本とも本番GAS疎通確認済み。
- 2026/08/03：**Codex完成画像の最終保存先を、記事めしで選択したGoogle Drive記事フォルダへ変更**。設定画面のローカルパスは「Codex一時受け渡しフォルダ」と明示し、最終保存先ではないことを表示。ジョブ開始時に記事フォルダIDを固定し、「📥 Codex画像をDriveへ保存」で長辺1800px以下に調整後、その記事フォルダへ直接アップロードする。途中で別記事へ切り替えても元の保存先へ戻す。新規生成・一時保存画像の編集は即時新規保存、既存Drive画像の編集は従来の上書き／元を残す選択を維持。役割付き画像はPROMPT.mdと既存ファイル一覧も更新する。
- 2026/08/03：**Codex一時フォルダの絶対パス入力欄を廃止**。非エンジニアには最終保存先と誤認しやすいため、初回だけ「📁 一時フォルダを自動準備」を押す方式へ変更。入力済みCodexワークスペースと同じフォルダを選ぶと、PWAが`.article-meshi-codex`を自動作成し、DirectoryHandleと内部パスを端末内へ保存する。以後は「設定済み」とだけ表示し、通常操作ではパスを見せない。一般の画像取込元フォルダとは別のIndexedDBキーで管理する。

- 2026/08/01：🔴**ブログ部門とSNS部門を「コンテンツ部門」に統合（CLAUDE.md v5.0）**。ユーザー指示「ブログとSNSを統合できないか検討して」→ 検討の結果、統合を推奨し承認を得て反映。
  **判断根拠は実測データ**：①SNS部門は5/2新設から**約3ヶ月ゼロ稼働** ②**12記事公開して拡散ゼロ**（CPO監査でも指摘済み）③X最終投稿は2025/8・フォロワー2（8/1実測）④SNS原稿の原料は記事の実測数字（価格・ROI・時短分数）そのものなので、別セッションだと文脈の再読み込みコストと事実誤りリスクが出る。
  **決め手**：7/31の公開後フック実装で `publish_article.py` が**SNS原稿キューを自動生成**するようになり、**ツール側は既に統合済み**だった。看板（モード・役割）だけが分かれていたので実態に合わせた。
  **変えたもの**：CLAUDE.md（5→4モード・役割カタログの `blog` を「コンテンツ統括セッション（ブログ×SNS）」に・「1セッション=1セクション」の例外を本則化・v5.0追記・課題欄更新）／目次.md（5部門→4部門）／blog・sns の SKILL.md 冒頭に統合注記／`tools/handover.py`（blog役割にSNSキーワード統合・`sns/` 編集は blog と推定・`DORMANT_ROLES={"sns"}` で自動推定から除外）。動作テスト済み（SNS作業でも `blog` が選ばれることを確認）。
  **変えていないもの**：`sns/` フォルダ・チャネル別SKILL・queue運用（中身は健全なので動かさない）。役割キー `sns` も温存し `--role sns` は引き続き使える。
  🚫 **統合＝AIが投稿する、ではない**：リプ送信・投稿の最終送信は従来どおり人間（sns/channels/x/SKILL.md §0-4）。
  **再分離の条件を先に明文化**：①Instagram／YouTube本格運用 ②SNS投稿が週5本超 ③容量圧迫 ── いずれかで `sns` 役割を復活。
  **学び：部門を分けるかどうかは「業務量」で決める。量がないのに分けると、分割線がそのまま仕掛かりの滞留点になる**（未拡散記事12本がその実例）。生産技術で言えば、ブログ→SNSは別工程ではなく**同一ラインの後工程**だった。

### 使用ツール
- WordPress REST API: wp_api.py
- ブロックビルダー: wp_block_builder.py
- 統合検品ゲート: article_lint.py（2026-08-01新設・publish_article.pyが自動実行）
- 内部リンク提案: suggest_internal_links.py（2026-08-01新設）
- パイプライン: run_pipeline.py（スタブ状態、今後実装検討）

---

## 進化ログ

| 記事# | 日付 | SKILL.md改善点 | 学んだこと |
|---|---|---|---|
| 1 | 2026/04 | 価格検証ルール追加、失敗パターン表追加 | 推測で書くと炎上する。型番ごとに仕様確認必須 |
| 2 | 2026/04/21 | ランナー向け4軸再解釈を追加、宣言数と実数の一致チェック追加、画像アップロードのSiteGuard対策追加 | 乗り換え視点の記事は強い。実走データ公開で差別化できる。記事1への波及修正も必要なケースがある |
| 3 | - | - | - |
| 4 | - | - | - |
| 5 | - | - | - |
| 6 | - | - | - |
| 7 | - | - | - |
| 8 | - | - | - |
| 9 | - | - | - |
| 10 | - | Phase 2移行！必要な工程を独立ファイルに | - |

---

## 読者反応の傾向

（PV・検索順位データが溜まったら追記）

---

## 更新履歴

| 日付 | 更新者 | 内容 |
|---|---|---|
| 2026-04-14 | 初期作成 | テンプレート作成 |
| 2026-04-14 | Claude | ブログ運営の開発履歴・Garminレビューの振り返りを遡及記録 |

- 2026/05/04 (続): ロゴ「見切れ」「白背景」問題を完全解決。元PNG (1916×821・白背景) を PIL+numpy で処理：
  - 白ピクセル (min(R,G,B)>=235) を α=0 に
  - 不透明領域で自動クロップ → 1831×365（白マージン除去）
  - 幅1200にリサイズ → 1200×239（5:1スリム比率）
  - PNG最適化保存 → 196KB（WAF回避）
  - WP REST APIアップ → media_id=776
  - Customizer: jinr__header_logo_url=ptgl-transparent.png, size=260, padding=20
  - 結果：透過PNG・ヘッダー余白なく綺麗に表示

- 2026/05/04 (続2): ロゴ「見切れ」根本原因解決。原因はwp-admin bar(高さ32px・position:fixed・z-index:99999)とJIN:R `commonHeader`(position:absolute・top:0・z-index:300)の重なり。ログイン中ユーザーだけ上部32pxが隠れていた。修正CSS：
  ```css
  body.admin-bar #commonHeader { top: 32px !important; }
  @media (max-width:782px) { body.admin-bar #commonHeader { top: 46px !important; } }
  #commonHeader, #commonHeaderInner { min-height: 64px !important; }
  #headerLogo, #headerLogoLink { display: flex; align-items: center; height: 64px; }
  #headerLogoImage { max-height: 48px !important; height: 48px !important; }
  ```
  教訓：DOM `getBoundingClientRect().top=0` が出たら admin-bar(32px)との重複を疑う。CSSの `!important` で上書きすればJIN:Rの内部CSSにも勝てる。

- 2026/05/05: 全体フォント拡大対応。 JIN:R theme_mod 設定変更：jinr__font_size=d--font-pc-xl-size, jinr__font_size_sp=d--font-sp-l-size, jinr__glonavi_font_size=18 + 全ボタンtext_size 16-18。追加CSS で本文17px、ヘッダーメニュー18px(bold600)、見出しh1=32/h2=28/h3=22、サイドバー見出し19px(bold800・青下線)、カードタイトル18px。
- サイドバー見出しの正しいセレクタは `#sideBarWidget h2.wp-block-heading.jinr-heading.d--bold`（`.l--sidebar` ではない）。CSSが効かない時はDOMで実際のid/classを確認するのが速い。
- 検索ボタン文字が縦割れする現象はFlex子要素のmin-widthが0になる時に起きる。`white-space: nowrap !important; min-width: 64px !important;` で解決。

- 2026/05/05 (続): カテゴリーページの記事カード最適化。元は横並び（サムネ130×72＋テキスト231×72）のミニカードで情報密度低・視認性低。最適化方針：「拡大」ではなく「情報設計」へ転換。
  - レイアウト: grid (auto-fill, minmax(280px, 1fr)) で複数列対応
  - 構造: 縦カード（サムネ100%×180px → 16pxパディング → タイトル(2行line-clamp) → メタ情報行）
  - メタ整列: 日付(13px gray) + カテゴリpill(blue 12px rounded-999px) を border-top区切りで底部固定
  - hover: translateY(-3px) + box-shadow + サムネズーム1.05x
- 学び：JIN:R デフォルトCSS で `.c--post-meta { position: absolute }` になっていたためタイトルと重なっていた。`position: static !important` で解除必須。
- 学び：CSS優先度を上げる時は `.o--postlist-item.o--postlist-item` のように同クラスを2回連ねる（特異性を倍化）。`!important`連発より読みやすい。

- 2026/05/05 (続2): カード全体可視化対応。「全体が見えないと記事が分からない」フィードバックに対応：
  - サムネ：`aspect-ratio: 16/9`（固定180px→可変）+ `object-position: center top`（記事冒頭に画像オーバーレイテキストがあるブログ向け最適）
  - タイトル：line-clamp 2→3行（情報量UP）
  - グリッド最小幅：280px→320px（読みやすさ優先）
  - 効果：4記事カードで「タイトル見出し画像」も「記事タイトル」も両方読める設計に
- 設計学び：「全体が見える」を実現するには ① object-fit: cover + object-position: center top（被写体は通常上部）② line-clamp 3行 ③ アスペクト比固定で全カード高さ揃える、の3点セット

- 2026/05/05 (続3): /gadget-lab/ 等のランディングページに反映されていなかった原因。 JIN:R `b--jinr-postlist.d--postlist-slider`（横スクロールスライダー）でカード幅183pxに固定されていた。
  - 解決：スライダー親要素をdisplay: flex から display: grid に変換 + minmax(220px, 1fr) で auto-fill
  - 注意：ネスト・グリッド回避必要（`.d--postslider-scrollbar`はdisplay:block、その子の`.b--jinr-postlist.d--postlist-slider`だけdisplay:grid）
- 教訓：「変更されてない」と言われたら、まず**正確に同じURL/同じセクション**をDOM調査する。私は category/ と /gadget-lab/ を混同していた（前者はカテゴリーアーカイブ、後者は固定ページのランディング）。

- 2026/05/05 (続4): スマホ最適化＋直感操作対応。媒体クエリ `@media (max-width: 768px)` と `@media (max-width: 480px)` で：
  - ヘッダー：PCナビ非表示・44×44pxハンバーガー・コンパクト56pxヘッダー
  - ヒーロー：縦積み・キャラ画像中央寄せ・CTAボタンフル幅
  - カテゴリ4枚→2×2グリッド・タップしやすい110px最低高
  - 記事カード：1列スタック・サムネ16:9・タイトル3行
  - サイドバー：コンテンツ下に移動・全幅展開
  - フォント：iOS拡大防止16px・タップ44px最小
  - スムーズスクロール・admin-bar:46pxオフセット・横スクロール禁止
- Customizer検証：`button.preview-mobile` JSクリックで320×480モバイルプレビューに切替可能。iframe内DOMから`getComputedStyle`で確認できる。
- Customizer保存はsetting変更後に `wp.customize.previewer.save()` を呼ぶが「already_saving」エラー時はリロード必須。

- 2026/05/05 (続5): スマホUI見本デザイン準拠への大改修。
  - PTGL_MOBILE_V2 + PTGL_BOTTOM_NAV CSS（合計43KB→公開済）：ヘッダー44pxハンバーガー、ヒーローグラデ＋大型タイトル＋2ボタン、カテゴリ2x2＋矢印`›`、検索バー大型化、★付き見出し、注目/最新カードを横並び（サムネ110×88+タイトル+メタ）、ボトムナビSP固定。
  - **JIN:Rのsp_menuがCustomizer設定だけでは表示されないことが判明** → 代替策として WP REST API `/wp-json/wp/v2/widgets` で `<nav class="ptgl-mobile-nav">` HTMLウィジェット (block-22) ＋専用CSS (block-23) を **sidebar** に追加 → 全ページ表示成功。
  - **重要**：footer-widget は JIN:R では home固定ページに描画されない。sidebar は全ページ描画される。
  - **WAF制約**：DELETE は ConoHa SiteGuard で 403。POST/PUTは可能（ただしPUTもCSSの`<style>`+ position:fixed組み合わせで時々403）。
  - **Customizer保存の遅延**：33KB+のCSS変更時、`previewer.save()` は90秒〜2分かかる。タイムアウトしても実際は保存される場合多い → curl で確認するのが確実。
- アイコン：fontawesome `fas fa-home` 等の指定だけではJIN:Rは描画しない。SVG直書きが最も確実。
- 教訓：「WP REST API + Custom HTMLウィジェット + sidebar」の組み合わせは、Customizer不調時の最強の代替手段。

- 2026/05/07: ヒーロータイトル PC/SP 切替 ＋ ホーム固定ページのボトムナビ表示問題解決。
  - PC: 「ガジェットと生産技術の力で、「ムダ」をなくし、仕事と生活をアップデート。」（"現場の" 削除）
  - SP: 「ガジェットと生産技術で、ムダをなくす。」（さらに短縮）
  - HTML：`<span class="hero-title-pc">PC文</span><span class="hero-title-sp">SP文</span>` で両方併記、CSSで切替
  - **重要バグ修正**：home固定ページ(ID 756)では既存CSS `body.page-id-756 .l--sidebar { display: none }` でサイドバー全体が非表示→中の block-22(nav HTML), block-23(古いCSS) も非表示になっていた
  - 修正：block-24 CSSウィジェットを追加し `display: block !important; visibility: hidden !important; position: absolute; left: -9999px` でサイドバーを画面外に飛ばす（display:block維持）→ 子の `.ptgl-mobile-nav` には `visibility: visible !important; position: fixed` でviewport底に固定
  - WP REST API のWP標準  `/wp-json/wp/v2/pages/{id}` でページ更新可能（POSTでもUPDATE扱い）。`?context=edit` 必須でraw contentを取得
  - WAF制約：DELETE と PUT は ConoHa SiteGuard で 403。POST のみ許可。

- 2026/05/08: ボトムナビ「埋もれ問題」解消。原因はホーム固定ページ(ID 756)で `body.page-id-756 .l--sidebar { display: none }` が祖先サイドバー全体を消し、子の `position:fixed` ナビも巻き込まれて非表示or通常フローで描画されていた。
  - 解決策：navをサイドバー経由ではなく **ホーム固定ページの post_content に直接 inline 埋め込み**（`#ptglMobileNavInpage`）
  - 別CSSウィジェット（block-27）を ID selector `#ptglMobileNavInpage` で書いて最高特異性 → クラスセレクタ + !important 競合を回避
  - 重要：CSS class同士の `!important` 競合では cascade order に依存して脆い。**ID selector + !important** が最強で確実
  - サイドバー版navは `body.page-id-756 #ptglMobileNav { display: none }` で重複防止
- WAFは `<script>` を含む widget を 403 で拒否。CSS-onlyで設計するのが鉄則。

- 2026/05/18：**記事873で確立したカイゼン案を全公開記事へ展開**。
  ① **🏆 急いでいる人へ型CTA** を全主要記事（605/552/526/450/836）の冒頭に追加。記事性質別に内容を最適化（レビュー記事は設定編誘導／持ち運び系は紛失リスク訴求／Garminは健康データ統合）。
  ② **時給950円（全国最低賃金）基準** に統一。836は時給3,000円ベースの計算に「最低賃金でも年6.7万円・初年度2.7倍回収」を併記。526は時給1,500円→950円で全面再計算（節約価値222円/日・損益分岐81日・約2.7ヶ月で回収）。
  ③ **TOC ホバー演出** は Customizer CSS (PTGL_TOC_HOVER) で全記事自動適用。H2は数字スケールアップ＋オレンジ「→」スライドイン、H3は左端オレンジバー出現。スクロール時の active section ハイライト・スムーズスクロール含む。
  ④ **H見出し scroll-margin-top:84px** で見出しジャンプ時のヘッダー被り回避。
  ⑤ 全記事Markdown残骸=0件・H2構造整合性を最終確認。
  教訓：**「急いでいる人へ」CTAは記事タイプ別にコピーを変えるべき**。レビュー記事は「設定編へ誘導」型／設定編は「最強1設定」型／商品紹介は「リスクゼロ化」型。テンプレ化しすぎると訴求弱まる。

- 2026/05/17：**記事873 MX ERGO S 設定編を公開**。Logi Options+ の Actions Ring × Per-app プロファイル × ジェスチャー の3軸を中心に、時給950円計算で95日損益分岐・3年純利益+12.4万円のROIを提示。記事めしPWAで集めた実機スクショ8枚（うち7枚を本文埋め込み）と Pillow 自動生成アイキャッチで構成。記事526末尾に「設定編はこちら」内部リンク追加で伏線回収完了。

  **教訓1：wp_block_builder.py は `![alt](url)` Markdown画像を処理しない**。回避策：①画像を `@@IMG_{wp_id}_{base64alt}@@` マーカーに置換 → ②markdown_to_blocks() で変換 → ③マーカーを `block_image()` で WP image ブロックに復元 → ④`<p>` で包まれたら剥がす。最終 regex で `**xxx**`/`***xxx***` 残骸を強制 strong 化。

  **教訓2：記事めしPWA の PROMPT.md が「フォルダ流用時に前記事のメモが残置」する不具合**。今回は「MX ERGO S 最適設定」フォルダに「Brown9 洗浄液」のメモが残っていた。回避策：PROMPT.md がフォルダ名と乖離している場合は無視し、画像内容を主軸に採用。PWA側は「新規記事として使う」時点で PROMPT.md をクリア・上書きする実装に改修すべき。

  **教訓3：ConoHa WAF が JPG 連続アップロードで 403 を返すことがある**。回避策：PNG変換＋微クロップ＋別名で再試行。8枚中1枚（#4）が PNG変換版で通過。
- 2026/05/12：**記事めしPWAに「スクショ貼付」機能を追加**。`📋 クリップボードから貼付` ボタン（`navigator.clipboard.read()`）／`document.paste` イベント／ドラッグ&ドロップの3経路、OS別の詳細手順を UI 内に details で展開表示。

**重大な教訓：Cloudflare Pages の Git 連携が実は動いていなかった**。`git push` してもサーバ側は古いコードを配信し続けていた（Git連携設定なし or 切れていた）。確実な反映には **`wrangler pages deploy . --project-name=blog-capture --commit-dirty=true --branch=main --commit-message="ASCII text"`** を実行する必要あり。

注意：`--commit-message` を省略すると wrangler が git の最新コミットメッセージを使うが、**日本語が含まれると UTF-8 エラー（code: 8000111）** で失敗する。必ず **ASCIIメッセージを明示** すること。

今後のPWA変更時のフロー：
```bash
cd blog/pwa-cloudflare
wrangler pages deploy . --project-name=blog-capture --commit-dirty=true --branch=main --commit-message="ASCII summary"
# 即時反映を curl で確認
curl -s https://blog-capture.pages.dev/?t=$RANDOM | grep -c "新機能のキーワード"
```
- 2026/05/11：**ホーム「最新の記事」サムネ全空白問題を解決**。原因は Customizer CSS の `.ot-latest-thumb` に `background: #f3f4f6 !important`（ショートハンド）があり、inline style の `background-image: url(...)` を全部リセットしていた。`background-color: #f3f4f6 !important` に変更で解決。教訓：**CSS の `background: ...!important` ショートハンドは inline style の background-image も上書きする**。`background-color` 単体に分離して指定するのが鉄則
- 2026/05/11：**プロフィール画像差し替え**。JIN:R Customizer の `jinr__profile_image_url` キーが旧画像（2026/02アップの低解像度 8885b3...jpg）を保持していた。これを media_id 737（オオタニ所長 通常・800×800・2026/05/03 アップ）に差し替え。教訓：**JIN:R独自設定は theme_mod に格納されるため、WP REST API では見えない**。Customizerで `wp.customize._value` を全キー走査するのが確実な発見手段
- 2026/05/11：**見出しサイズの異常**を発見・修正。記事 836 で H3=13px、H2=15.5px と本文(17px)より小さい状態だった。JIN:R Customizer 追加CSSに `PTGL_HEADING_FIX` ブロック（1339bytes）を追加し、H1=32 / H2=28 / H3=22 / H4=18 / 本文=17 の正常階層に。色は #1d4ed8（青）、H3には3px下線。モバイル(768px以下)は H2=22 / H3=19。教訓：**JIN:Rテーマのデフォルト見出し設定は本文より小さくなる場合がある**。新記事公開前に必ずH2/H3 サイズを `getComputedStyle` で確認すること
- 2026/05/09：ボトムナビ検索ボタンの**実動作不良を完全修正**（3段階の試行錯誤の末）。
  - 試行1：JIN:R Customizer に display 切替型 CSS を追加 → PC では動いたがモバイルでは動かず
  - 試行2：opacity/visibility + transition 型に書換 → やはり初回 `:target` 適用が rendering quirk で失敗
  - **真因（試行3で発覚）**：sidebar widget `block-28` に **古い display 型 overlay CSS（1664 bytes）** が残っていて、Customizer CSS を後勝ちで上書きしていた。前セッションで HTML（block-22）+ CSS（block-28）の2 widget で実装していたが、片方だけ修正していたため整合性崩壊
  - 解決：block-28 を空paragraph に置換（POST `<style>` 不含なら WAF通過）+ Customizer CSS を再シンプル化（transition 削除・display 切替型）
  - 教訓1：**同一機能のHTML/CSSを複数の widget に分けて置くと、片方の更新だけで「動かなくなった」事故が起きる**。今後は HTMLは inline content / CSSは Customizer の theme_mod に集約する。widget は使わない
  - 教訓2：**CSS `:target` + `transition` の組み合わせは Chrome/Safari の初回ペイントで rendering quirk を起こす**ことがある（4秒待っても visibility:hidden のまま、location.hash 再設定で復活）。シンプルな display 切替型の方が確実
  - 教訓3：**WAFがウィジェットPOSTを403拒否するのは `<style>` タグが含まれるとき**。`<style>` を除けば POST 通る。CSS は Customizer の `custom_css[jinr]` theme_mod に集約するのが正解
  - 教訓4：**iOS Safari の `<a href="#x">` + CSS `:target` パターンは、対象要素が `display:none` の場合にURLハッシュが更新されない既知バグがある**。モバイル対応のCSS-only モーダルは `:target` ではなく **チェックボックスハック（`<input type="checkbox" id="x" hidden>` + `<label for="x">` + `:checked ~`）** を使う。これは HTML標準動作で全ブラウザ100%動作。実装は block-22 widget HTML + page 756 inline + Customizer CSS の3箇所
  - 最終構造：checkbox / nav (with `<label for>` トリガー) / overlay (with `<label for>` 閉じるボタン) を **同一親の siblings として配置**。`#ptglSearchToggle:checked ~ .ptgl-search-overlay { display: flex }` で表示制御
- 2026/05/08：ボトムナビ「プロフィール」(`/profile/`) が 404 だったため、page ID 29 の slug を `プロフィール` → `profile` に変更して機能化。ナビhrefは変えず、ページslugだけで対応する方が最小変更で済む（教訓：URL変更が必要なときは "リンク側" よりまず "コンテンツ側のslug" を考える）
- 2026/05/08：ボトムナビの検索ボタン HTML（block-22）を実装。CSS `:target` 擬似クラスを使ったオーバーレイ検索フォーム（JS不使用・WAF耐性）。※ただし対応CSSが抜けており実動作せず → 翌日 05/09 に修正
  - 仕組み：検索ボタン `href="#ptglSearchOverlay"` → CSS `:target` で半透明モーダル表示 → `<form action="/" method="get" name="s">` で WP標準検索結果ページへGET遷移
  - 変更：block-22(sidebar nav 全ページ) のhref＋overlay HTML、block-28(新規CSS widget)、ホーム固定ページ756 inline nav も同期
  - 教訓：①既存CSS widget(block-23)に追記しようとして WAF 403。`<style>`+`position:fixed` 組み合わせを既存に追加するとブロックされやすい → **新規widget追加で回避** ②新規widget POST時は初期 `wp_inactive_widgets` に入るので必ず後追いで `{'sidebar':'sidebar'}` POST して移動が必要
  - 横展開可能：CSS-only :target モーダルは検索以外（メニュー・お知らせ等）にも応用可
- 2026/05/08（重要）：オーナーから「ROI計算根拠が抜けている」指摘。Garmin Venu 2S(605)/MX ERGO 持ち運び(552)/MX ERGO レビュー(526)/Keychron K1 Max(450) の4記事すべてにROI計算根拠を追加・修正。
- 追加した内容：
  - **計算式table**（購入価格÷使用日数=日次コスト、節約時間×時給=節約価値、投資÷節約価値=損益分岐日数）
  - **仮定値の根拠説明**（「3%」「5%」などの効率向上率を「なぜその数字なのか」の段落つき）
  - 526は既存ROIに「3%」の根拠1段落を追記
- **SKILL.md更新**：「🔴【最重要・絶対遵守】ROI計算根拠の明記ルール」を新設。今後すべてのレビュー記事に：
  1. 専用h2セクション「📐 ガジェット投資対効果（ROI）」必須
  2. 計算式tableで1日コスト・損益分岐点・累計価値を明示
  3. 仮定数値（％・時間）の根拠説明1段落必須
  4. 公開前チェックリスト6項目
- 教訓：**「数字を出す」と「数字の根拠を出す」は別物**。当ブログの売りは後者。生産技術屋として「感覚ではなく計算式で判断する」を体現する記事スタイルが差別化要因。

---

## 【執筆原則】記事めしメモは「種」、本文は「深掘り」（2026-05-27 確立）

### ルール
記事めしPWAで保存される PROMPT.md のメモ（「読者に伝えたいポイント」）は **筆者の伝えたいテーマの種** であって、**そのまま記事化してはいけない**。

### 各テーマに対して必ず行う深掘り
各メモポイントを 800〜1500字相当のセクションに展開し、以下の要素を最低3つ含める：

1. **物理メカニズム／仕組み**：「なぜそうなるか」を構造レベルで（モーター駆動・センサー検知・規格寸法 等）
2. **数値根拠**：実測値・統計・メーカー公称値・警察庁データ・心理学研究値
3. **心理学・人間工学・生産技術原則**：ツァイガルニク効果・注意リソース・確認工程の冗長性 等の理論裏付け
4. **反論への先回り**：読者が抱きそうな疑問1〜2個（「重力で落ちない?」「電池切れたら?」「賃貸退去時は?」）を先取りで論破
5. **実体験・失敗談**：筆者の試行錯誤・つまづきポイント（信頼感UP）
6. **競合・代替案との比較**：他製品／旧運用 との明確な差分
7. **対象読者の解像度UP**：誰のどの悩みを解くか具体化

### 目標感覚
- 読後感：**「ふーん」→ ❌  /  「なるほど、買おう」→ ✅**
- 抽象的美辞麗句のみ：禁止
- メモ4ポイント列挙して終わる箇条書き記事：禁止

### 反映済み
- `blog/scripts/article_from_meshi.py` — context.md 出力時に「メモの扱い方」「執筆チェックリスト」セクションに明記

---

## 他セッションへのTODO（連絡欄）

> 別セッションで作業してほしいことがあれば、ここに記入する。ブログセッションは作業開始時にこの欄をチェックする。
> （2026-06-12 CPO監査で追設。research/skills/handoff.md はこの欄への送出を前提に設計されている）

- [ ] **[CPO監査 2026-06-12] 記事台帳の記事1（Garmin Venu 2S）の状態を修正**
  - 台帳では「ローカルのみ」だが、同ファイル内に WP投稿ID 605 として公開済みの記述が多数あり矛盾。台帳の行を「WP 605・公開済み」に修正する

---

## サイト構成メモ（2026-06-21・サイト導線見直しで判明）
- **トップページは固定ページ「ホーム」(ID 756)** ＝ 表示設定で「固定ページ」指定・**投稿ページ(記事一覧)は未割当**。だからトップに動的な記事フィードが無い。
- ホーム(756)は14,667字の自作HTML（ヒーロー/検索モーダル/カテゴリ4枠/特徴アイコン等）。**中段の記事ショーケース（MX ERGO/Keychron等）は静的カードに見え、新記事が自動では出ない**（日付表記が実公開日と不一致）。
- 🔴 **WPコアの `wp:latest-posts` ブロックをホーム末尾に足すとJIN:Rの自作レイアウトと衝突して崩れる**（カテゴリ枠と重なる）→ 即リバート。ホームに最新記事を出すなら「JIN:R純正の新着記事ブロック/ショートコード」か「静的カードを手編集」で行うこと。コアブロック直挿しは禁止。
- ✅ **記事導線はトップの「リッチメニュー（カテゴリカード4枚）」に集約**（2026-06-21確定）。最初ヘッダーのテキストメニューにカテゴリ4を追加したが、見た目都合で**元の3項目（プロフィール/お問い合わせ/名物）に戻した**。代わりに、ホーム(756)のカードのリンクを修正：3枚が壊れていた（`/gadget-lab/`等の`/category/`抜け・暮らしハックは日本語URL）→**全4枚を `/category/<slug>/` に統一**（gadget-lab/seigi-lab/jitan-tool-lab/kurashi-hack-lab）。これで「トップのカード→カテゴリ→記事」で辿れる。
- ✅ **2026-08-22 PCヘッダー見切れを再修正**：ヘッダーメニューにカテゴリ4項目が再び含まれ、JIN:Rの最大幅1,120px内で7項目＋横長ROIボタンが折り返され、上段が画面外へ見切れていた。Customizer追加CSSの `@media (min-width:769px)` で `#globalMenuList` のカテゴリ項目1046〜1049だけを非表示、`flex-wrap:nowrap` に固定し、PCはプロフィール／お問い合わせ／ROIの3項目へ復帰。**ハンバーガーは別リストなのでスマホには全7項目を維持**し、トップのカテゴリカード4枚も維持。公開後にPC実画面・Customizerモバイル表示・配信CSSを確認済み。
- 🔴 **WAFはPUT/DELETEを403で弾く**（再確認）。メニュー項目の削除はREST DELETE不可→nav-menus.phpの画面操作（POST保存）で行う。メニュー項目の追加/削除/カード編集の知見はこの通り。
- 学び：**JIN:Rの作り込み済み固定フロントページは、コアブロックの直挿しで崩れる前提で扱う**。導線改善は「メニューにカテゴリ追加」が最も安全・確実で効果大。

---

## 🏠 トップ「注目記事・最新記事」運用ルール（2026-06-21制定）
**トップ(ホーム固定ページ id756)の 注目/最新/人気ランキングは静的HTML＝自動更新されない。** 記事公開とセットで手動更新する（公開フロー⑩に組込）。

### 📏 注目記事ルール（明文化）
注目＝トップに固定で見せる「イチオシ3本」。選定基準（優先順）：
1. **最新の主力レビュー**（公開直後の目玉）
2. **ROI・実用性が高く読者に刺さる**記事
3. **定番・フラッグシップ**（長く読まれる代表作）

### 更新運用
- **最新の記事(ot-latest-list)**：実際の新着順 上位5本に差し替え（新記事を先頭・最古を削除）
- **注目の記事(ot-featured-grid・3枚)**：上記ルールで選定
- **人気記事ランキング(ot-ranking)** も静的（閲覧数ベースで任意に見直し）
- カード色：暮らしハック=#ea580c／ガジェット=#2563eb／生産技術=#1e3a8a
- **更新スクリプト**：`blog/scripts/update_home_cards.py`（WP REST `/pages/756` のcontentを差替。記事データ＝URL/サムネ/タイトル/日付は `/posts?_embed` から取得）。新記事公開時はこの記事配列を編集して実行。
- 学び：**自作フロントページ(JIN:R固定ページ)は静的なので、公開＝トップ更新までを1セットにする**。コアの`wp:latest-posts`直挿しはレイアウト崩壊（前例）→静的カード手編集が安全。

---

## 🗂 カテゴリ分類ルール（複数所属OK・2026-06-21制定）
**1記事が複数テーマに当てはまれば、該当する全カテゴリに所属させる**（主従でなく「内容が当てはまるか」で判断・最大3つ目安）。WP REST `POST /posts/<id> {categories:[...]}` で設定（WAFはPOST許可）。

| カテゴリ(id) | 該当内容 | タグ色 |
|---|---|---|
| ガジェット研究室(1) | 物理デバイス・ハードのレビュー（時計/マウス/キーボード/スマートロック/家電） | #2563eb |
| 時短ツール研究室(6) | 作業・生活を効率化する設定/ツール/アプリ（設定編・稼働率・効率化が主題） | (青系) |
| 暮らしハック研究室(5) | 日常の節約・健康・生活改善（消耗品コスパ・健康管理・賃貸生活） | #ea580c |
| 生産技術研究室(4) | 工場・FA・PLC・自動化の知見 | #1e3a8a |

### 判定の目安（例）
- 効率化を生むガジェットの設定/レビュー → **ガジェット＋時短ツール**（MX ERGO・Keychron系）
- 生活が楽になるスマート家電 → **ガジェット＋時短＋暮らしハック**（SwitchBotロック）
- 健康管理デバイス → **ガジェット＋暮らしハック**（Garmin）
- 消耗品の節約/コスパ → **暮らしハック**（Brown9洗浄液）
- 工場・生産技術ネタ → **生産技術**

### 運用
- 🔴 **新記事公開時に「該当する全カテゴリ」を付与**する（公開フロー⑧で categories 配列を複数指定）。単一所属で固定しない。
- カードのタグ表示は1つだが、所属は複数可（各カテゴリ archive に出る）。
- 現状の割当（2026-06-21）：ガジェット7／時短6／暮らしハック3／生産技術1。

---

## 🎯 看板コーナー「何日で元取れる？」をボタン強調（2026-06-21）
- ROIコーナー（メニュー項目 id851「あなたのガジェット、何日で元取れる？」）を**オレンジのボタン型（💰付き）**に強調。プロフィール等の青文字に埋もれず目立つCTAに。
- 実装：**WP追加CSS `custom_css[jinr]`（Customizer・JIN:R仕様）** に `.menu-item-851>a` のボタン化CSSを追記。グローバルナビ＋ハンバーガー両方に適用。
- 🔴 **CSSはこのサイトでは `custom_css[jinr]`（=Customizer→追加CSS、wp-custom-css出力・約5万字）に集約**。編集はカスタマイザーをブラウザで開き `wp.customize('custom_css[jinr]')` を JS で get→append→set→公開、が確実（REST非対応）。ヒーロー/特徴/ホームのデザインCSSも全部ここ。
- 学び：JIN:Rの追加CSS設定IDは `custom_css` ではなく **`custom_css[jinr]`**。カスタマイザーのライブプレビューは反映が遅れることがある→公開後に本番で確認する。

---

## 🛒 レビュー商品 調達戦略ドキュメント（2026-06策定）
- **`blog/レビュー調達戦略.md`** ＝ レビュー商品を無償/格安で得る多角的・高度戦略の正本。
- 核心：「無償提供をもらう」でなく **"レビュー1本あたりの真の総コスト（取得−回収＋手間＋税＋信頼リスク）を最小化"**。KPIで手段を数値選択。自腹比率維持で中立性=信頼を守る。
- 全方位カバー：**回収(中古買→売直し・最強)／借用(Rentio買取充当)／実質割引(セルフバック/海外通販)／持たない(比較記事/展示会)／無償(Vine/ギフティング/メーカー献本/アンバサダー/CF/PRメディア登録/法人サンプル)**。
- クラファン：machi-ya(メディア連動最強)/GREEN FUNDING/Makuake/Kickstarter/Indiegogo を「新着巡回(ネタ)＋実行者へ先行レビュー提供打診」で二役。
- 同梱：手段別ファクトシート・KPIシート雛形・**Vine資産化テンプレ＋試作レビュー**・メーカー献本/CF打診メール下書き・**PR表記ルール(ステマ規制)**・段階ロードマップ。
- 🔴 着手時はサービス条件を最新で再確認（Vine招待基準は非公開・ETV所得区分は税理士確認・献本「月10万PV」は参考値）。

---

## 🏷 記事めしPWA：既存Drive画像の「用途」表示と役割メモ反映（2026-07-15）
- **既存Driveファイルは役割をファイル名prefix（section_等）でしか持てない**＝同じ役割内の複数テンプレ（イチオシ/スペック表/同梱物…）を区別できない。
  - → 細テンプレ選択は **localStorage(`kiji-meshi:ef-template`) にfileId別で記憶**（DriveリネームはfileId不変なのでキーに使える）。
  - → さらに **役割メモ側に「｜用途: ⭐イチオシポイント」を併記**（役割ラベル/ファイル名/fileIdは保持。GASはファイル名prefixで分類するので無害）。
- **失敗と対策**：
  - ① バッジ(役割名)とボタン(細テンプレ名)が別ソースで**表示が乖離** → `displayLabel`を1つにして両方で共有。
  - ② メモの用途ヒントを全角括弧`（用途:…）`で囲んだら、テンプレ名内の`）`（例「同梱物の紹介（付属品一覧）」）で**除去regexが誤爆し stray `）` 混入** → 区切りを **`｜用途:`（画像リンク記法と同じ・半角`(`とも衝突回避 `[^｜(]*`）** に変更。
  - ③ localStorage記憶とDriveメモ用途が**端末跨ぎで乖離** → `reconcileEfTplFromMemo`でメモ側を"正"としてlocalStorageを自動復元。
- **検証の教訓**：Cloudflare Pagesは**エッジ伝播に数十秒**かかり、デプロイ直後は古いapp.jsが配られ「まだ直ってない」と誤認する→`fetch('/app.js',{cache:'no-store'})`でマーカー確認してからリロードして実機E2E。実データでのテスト痕跡（役割メモ・localStorage記憶）は必ず掃除して元の状態へ戻す。
- 汎用化候補：app-dev-playbookの A-5(二層保存)/E(往復で壊れない)/D-2(本番URL確認) の実例。

---

## 💰 全公開記事へアフィリンク一括追加（2026-07-17・ブログ分析→最優先施策の実行）
- **背景**：分析で「アフィリあり2/10・内部リンク2/10」が最大の弱点と判明→収益導線を全記事に敷設。
- **追加内容（7記事・9リンク・全てtag=gp6sk1029-22＋PR表記＋rel="noopener nofollow sponsored"）**：
  - 945 MaGdget：本体 **B0GHMZMM7Q**（¥3,960・公式と同価格。前回未発見→今回Amazon検索で発見）
  - 873/526 MX ERGO S：本体 **B0DC5X912P**（MXTB2通常版¥17,800。Amazon限定版MXTB2dと区別）
  - 836/450 K1 Max：JIS版 **B0FHWX7X7K**（¥19,360。現在JISはレトロカラーのみ→リンク文言に注記）
  - 605 Venu 2S：本体 **B094VTDH46**（¥42,990・現役販売中）
  - 552 持ち運び：WERJIA **B0DHCPVHQX**（¥1,799）＋UGREEN **B074PQPLDQ**（¥760）＋MX ERGO S本体
- **ASIN特定の手順（成功パターン）**：Claude in ChromeでAmazon実検索→`[data-asin]`をJSで抽出→**スポンサー枠除外**・型番/配列/カラーまで題名で照合。**変換アダプタ類は記事本文の使い方から「挿す向き」を確定してから選ぶ**（UGREENは A メス→C オス のOTGが正解。逆向き商品が検索上位に多い）。
- 挿入書式はSwitchBot記事の関連リンクブロックを踏襲（separator→H2→quote(PR)→list）。更新前rawはscratchpadにバックアップ。
- 残り：[12]2023年エッセイのみアフィリなし（商品なしのため対象外＝全対象記事カバー完了）。

---

## 🔗 全公開記事へ内部リンク整備（2026-07-17・ブログ分析→施策②）
- **9記事すべてに「あわせて読みたい」導線**（合計22本・リンクゼロ記事が消滅）。MXシリーズ（526⇄552⇄873）とK1 Max（450⇄836）は相互リンク、非シリーズは「手ぶら生活（945⇄908）」「暮らしのコスト（927）」のテーマで束ねた。
- **発見**：873の既存「関連記事」リストは太字テキストのみで**リンクになっていなかった**（読者はクリックできない）→ in-placeで`<a href>`化。見た目が関連記事風でもhref有無を確認すること。
- 挿入書式：separator→H3「あわせて読みたい」→jinr-list（「導入句 ── リンク」形式・873の既存リストと統一）。URLはWP APIの`link`から取得（日本語スラッグのエンコード事故防止）。更新前rawはscratchpadへバックアップ。

---

## 📊 GSC×GA4 初回アクセス分析（2026-07-17・Claude in Chrome直読み）
- **数字**（GSC 3ヶ月）：クリック24／表示276／CTR 8.7%／平均順位7.3。（GA4 30日）ユーザー55(+77%)・表示186(**+244%**)＝小規模だが急成長カーブ。
- **🏆 勝ち筋の発見：検索流入の71%が「ブラウン互換記事」1本**（17クリック/184表示/順位6.1）。ガジェット本体レビュー（Keychron/MX ERGO/SwitchBot/Garmin/MaGdget）は検索表示ほぼゼロ＝大手と競合して埋もれる。**「消耗品・互換品・比較」系が個人ブログの勝てるジャンル**（記事企画はこの路線を優先）。
- **クエリ「braun ccr2 vs ccr4」で既に1位なのにクリック0**→専用記事がない。→ 📌 **TODO：CCR2 vs CCR4 比較を独立記事化**（ブラウン記事から分岐＋相互リンク。タダで取れる1位）。
- **Bing 15 ＞ Google 10**（7日間の参照元・Bing+650%）。ChatGPT経由も2件（AI検索流入の兆し）。
- **指名検索「ガジェット研究所」34表示0クリック**（順位8.7で埋没）＝一般名詞すぎる名前の弊害。PTGLリブランド方針の裏付けデータ。
- 課題：404閲覧1件あり（URL未特定・要調査）／MX ERGO Sレビューは順位23で「あと一歩」（タイトルに検索語を入れるリライト候補）。
- **手順の学び**：GSC/GA4はClaude in Chromeで直読み可能。ただし拡張の**サイトアクセスを「すべてのサイト」にする必要**（ユーザー操作1回）。GSCのタブ切替はクリック不可→**URLパラメータ`&breakdown=page`で直接ビュー指定**が確実。GA4はホーム画面のカード群にチャネル/参照元/ページ/国が全部載っていて1画面で足りる。

---

## 🔎 アフィリエイト向けガジェット候補調査（2026-07-24・Codex）
- **Google Trends（日本・過去12か月・同一比較）**：ミニPC=68、骨伝導イヤホン=68、スマートリング=60、モバイルモニター=59、ダイソン フィルター=22。絶対検索数ではなく相対指数。
- **急上昇ロングテール**：`gmktec g10`=急激増加、`Switch 2 モバイルモニター`=約400%増、`Switch 2 モニター`=約350%増。
- **収益本命**：GMKtec公式A8案件は購入額の**5%**。G10公式42,000円なら概算**2,100円/件**（AmazonのPC・家電2%より有利）。
- **SEO本命**：既存GSCの勝ち筋を優先し、`ダイソン フィルター 純正 互換`と`テプラ テープ 純正 互換`を低コスト実測記事にする。本体の高検索数だけで選ばない。
- **推奨構成**：「高単価・競合強めのGMKtec」1本＋「購入意図が濃い消耗品比較」2本のポートフォリオ。
- **次回の具体策**：手元に対応ダイソン／テプラがあるかを確認し、所有品に合う型番から先に記事化。GMKtecはA8セルフバックと中古売却を含む実質調達費を確認してから購入。
- 詳細成果物：`blog/reports/2026-07-24-アフィリエイト向けガジェット候補調査.md`

---

## 📝 ネタ帳：消耗品・互換品リサーチ TOP10（2026-07-24・サブエージェントWeb調査）

「勝ち筋＝消耗品・互換品・比較」路線の企画リスト。所有デバイス起点＝追加投資ほぼゼロ優先。**料率メモ：Amazonは替刃・歯ブラシ等ドラッグストア系≈4.5%（家電2%の倍以上）**。

| 優先 | ネタ | 狙いクエリ | 競合 | リピート性 | 状態 |
|---|---|---|---|---|---|
| 🥇1 | **ブラウン替刃 F/C94M 純正vs互換** | 「ブラウン 替刃 94M 互換」「92M 94M 違い」 | 低 | 高(18ヶ月毎) | 未着手。エース記事（洗浄液927）と読者同一→内部リンク即効。高料率 |
| 🥈2 | **SwitchBotロック CR123A電池比較** | 「SwitchBot ロック 電池 おすすめ」「CR123A 充電式」 | 低 | 高(半年毎) | 未着手。所有機で持続日数を実測→1日あたりコスト算出（当ブログの型） |
| 🥉3 | **MX ERGO ボール交換(PERIPRO-303)** | 「MX ERGO ボール 交換」「ペリックス 34mm」 | 中 | 中 | 未着手。個人ブログが上位実証済みジャンル。投資1,500円のみ |
| 4 | Garmin充電Type-C変換アダプタ | 「ガーミン 充電 タイプC 変換」 | 低 | 中 | 500-1,500円 |
| 5 | Garmin Venu 2S バンド18mm互換 | 「Venu 2S バンド 18mm」 | 中 | 中 | 「20mmと誤認が多い→正解を示す」切り口 |
| 6 | HUAWEI GT Runner バンド22mm | 「GT Runner バンド 交換」 | 低 | 中 | 情報希少＝ロングテール独占余地 |
| 7 | 加湿器フィルター型番別互換 | 「(型番) フィルター 互換」 | 低 | 高(季節毎) | 冬季に爆発。メーカーは互換の性能低下を明言→比較が成立 |
| 8 | 車エアコンフィルターDIY | 「(車種) エアコンフィルター 自分で」 | 中 | 高(年1) | 工賃5千円→部品1-2千円のROI明快。車種×型番で横展開 |
| 9 | 電動歯ブラシ互換替えブラシ | 「オーラルB 替えブラシ 互換」 | 中 | 高(3ヶ月毎) | 純正1本400円vs互換84円。「iOは互換なし」注意喚起が独自価値 |
| 10 | Keychron K1 Max スイッチ/キーキャップ | 「K1 Max スイッチ交換」 | ほぼゼロ | 低 | 検索量小だが競合ゼロ。ロープロ互換制約(Gateron LP)が書ける |

### 🛒 レンタル×レビュー戦略（2026-07-24・サブエージェントWeb調査）
- **業界地図**：kikito(ドコモ)は2026/6終了・DMMも終了済→**Rentio一強＋ゲオ**。レンタル側のアフィリ報酬はほぼ無し（RentioのASP直は「なし」情報・A8管理画面での提携検索は未確定）→ **レンタル費＝取材費と割り切り、Amazon/楽天リンクで回収**する設計。
- **回収の条件**：単発レビューで終わらせず「本体比較→消耗品→ランニングコスト実測」の**記事群化**（1レンタル→3〜4本）。
- 優先候補：🥇**ロボット掃除機**（Rentio: ルンバCombo Essential月1,180円/Roborock Q7 Max+月2,500円。比較＋互換フィルタ・ブラシ＋電気代実測）／🥈**ラムダッシュPRO vs ブラウン9**（本命は替刃コストクエリ＝勝ち筋と同型）／他：XGIMI新旧比較・衣類スチーマーNI-FS型番比較・空気清浄機互換フィルター実測（いずれも競合低）。
- ROI目安：2機種で月4,000〜6,000円 → 消耗品系アフィリ積み上げで3〜6か月回収。

### 🎥 レンタル候補の実額ROI比較（2026-07-24・Codex）
- **総合1位：DJI Osmo Pocket 4スタンダード**。レンタル総額6,960円（本体6,480円＋往路480円）に対し公式79,200円。実機・Pocket 3比較・iPhone比較・Creator Combo要否の4本へ展開し、取材費は1記事1,740円。
- **価格レバレッジ1位：Nebula Capsule 3 Laser**。最安表示プラン総額6,582円に対し公式119,900円で約18.2倍。ただし単体レビューは競合が強いため、昼間・壁紙・300 ANSIルーメン・テレビ代替の実測で狙う。
- 次点はInsta360 X5（総額6,960円／公式84,800円）、SwitchBot K10+ Pro Combo（総額9,460円／公式99,800円）、Pocket 3（総額5,460円／公式56,980円、Pocket 4比較用）。
- **重要**：価格レバレッジはアフィリ回収率ではない。提携中ASPの最新料率確認後に「必要成約数＝レンタル総額÷1件報酬」を出す。Rentio友達紹介は公開メディア向け案件と確認できないため収益計画へ算入しない。
- 詳細成果物：`blog/reports/2026-07-24-レンタル候補ROI調査.md`

---

## 🧠 記事めしPWA：編集中バナーからCodexで画像編集（2026-08-03）
- 画像の置換待ちバナーに **「🧠 Codexで編集」** を追加。既存のCodex画像ジョブ連携を使い、元画像・最新プロンプト・保存先を一時受け渡しフォルダへ渡してCodexを開く。
- Codexモードではクリップボード貼付の案内を出さず、完了後は **「📥 Codex画像をDriveへ保存」** で選択中の記事フォルダへ直接保存する流れに切り替える。
- `.article-meshi-codex/` は画像と指示書を含む一時領域のため `.gitignore` に追加。自動Git同期でも作業素材をコミットしない。

---

## 📱 記事めしPWA：スマホのホーム画面へ追加（2026-08-03）
- manifest・アプリアイコンに加え、上部の`⇩`から追加できる導線を実装。Androidはブラウザのインストール確認、iPhoneはSafariの「ホーム画面に追加」の操作案内を出す。
- ChromeでショートカットではなくPWAとして認識されるよう、Service Workerに静的画面のキャッシュ処理を追加。記事データ・Google Drive・認証を伴うAPI通信は対象外とし、認証方針を変えずに単独アプリ起動を実現。
- 制約：スマホではカメラ・素材転送・ChatGPT/Gemini連携を利用。Codex画像編集はMacのCodexアプリとローカル一時フォルダを使うため、スマホ用導線には出さない。

---

## 🍌 AI画像ヘルパー：Gemini起動ボタンの復元（2026-08-03）
- `btn-ai-open-gemini` の実行処理・Gemini接続先設定は残っていたが、画面上のボタンだけが消えていた。AI画像ヘルパーの操作列に **「🍌 Geminiを開く」** を復元し、同じく孤立していたChatGPT起動ボタンも併せて戻した。

---

## 🚀 記事めしPWA：高速一括転送の改善復活（2026-08-08）
- 失敗した旧方式（複数画像をbase64化し、1本の巨大なGAS POSTで中継）は復活させない。GASは最大6件のDrive Resumable Uploadセッションだけを`fetchAll`で並列発行し、画像本体はブラウザからDriveへ最大6件並列で直送する。
- 適用は3枚以上の新規転送。1〜2枚と上書き編集は従来経路のまま。高速経路の個別失敗は従来経路へ自動フォールバックする。画像保存後のハッシュ台帳・ログ更新はUIを待たせないバックグラウンド確定。
- 本番GASでの実測：3件はDrive保存完了まで7.49秒、6件は12.51秒。従来の個別転送は1件約5.7秒だったため、3件で約57%、6件で約63%短縮見込み。テストファイルは全9件ともゴミ箱へ移動済み。
- 速度が体感できない報告を受け、直送同時数を3→6件に引き上げ、完了時に「🚀直送 ○MB/s（準備○秒）」と計測結果を出す。一括重複照合も、画像ごとに台帳を読み直す方式から1回の読み込みへ変更。GAS実測で6件同時のDrive直送は3.16秒、準備を含む合計は9.61秒（GAS起動待ちで変動）。

---

## 🚗 C26セレナ用サマータイヤ比較の下書き作成（2026-08-14）
- `serena-c26-tire-16inch-mp7-rv505.md` を未公開原稿として作成。**未所有・未実走の比較記事は、体験談を作らず、メーカー公式の仕様・ラベリング情報と「選び方」に限定する**。
- タイヤの費用対効果は時短ROIに無理に換算せず、**4本の総支払額 ÷ 年間の燃料代差 = 回収年数**というLCC（ライフサイクルコスト）の式で示す。燃費差は車種・空気圧・走り方に左右されるため、必ず「比較用の仮定」であると明記する。
- 自動車の適合記事では、カタログ上の標準サイズ・空気圧を断定しない。**年式・グレード・駆動方式で差があるため、実車のドア開口部ラベルを最優先**と書く。
- オオタニ所長は読者に丁寧に説明する文章とは分け、**タナカへの返答では「〜だ」「〜なんだ」の短い断言を重ねる**。ただし、読者を見下す命令・説教にはしない。
- 未所有商品の比較記事に画像を入れるときは、メーカー公式の写真を装飾目的で転載しない。**製品仕様は公式リンクで出典表示し、記事用画像はロゴ・製品名・特定車種を含まないオリジナルの説明用イメージ**として、実物写真ではないことをキャプションで明示する。

## 🚗 C26セレナの実体験へ記事を再構成（2026-08-14）
- ユーザー自身が所有するC26（2024年11月に走行距離125,000kmで購入）と、装着済みTRANPATH mp7、店舗で受け取ったRV505見積もりを主軸に変更。一般論の比較よりも、**見積もりを基準に楽天市場の「タイヤ＋交換チケット」を比較する購入記録**にした。
- 店舗見積もりはRV505 4本・組み替え／バランス・ゴムバルブ・廃タイヤ処理を含めて75,600円。楽天で約25,000円低い条件を見つけたのはユーザーの実測条件であり、送料・チケット・追加作業まで同条件かは注文前に再確認する、と明記する。
- 楽天Carタイヤ購入＆交換は、タイヤと交換チケットを**同一ショップ**から同時購入することが前提。ショップ変更時はチケットをカートから消し、変更後ショップのチケットを選び直す注意点を、記事の中心ノウハウとして扱う。
- 個人の実車写真は公開前にリサイズしてGPSなどのEXIFを除去。見積書は氏名・電話番号・見積日・有効期限・店舗名・担当者名・発行店情報をモザイク処理し、比較根拠である金額・商品名・作業内訳は残す。公式サイトのスクリーンショットは転載せず、公式案内へのリンクで代替する。
- 読者を引き込むため、冒頭は「75,600円の見積もり」という数字から開始し、ひび割れで先延ばしできない状況、楽天で約25,000円節約、交換チケットの落とし穴へ続ける。一般論から始めず、**数字→葛藤→発見→注意点**の実録順にする。
- 実車写真の向きがプレビューで崩れ、足が写り込んだため、元写真を正しい向きの横長構図へ修正し、人物部分を完全にトリミング。画像は記事側で最終表示を確認してから完了とする。
- 同じ全景写真を2枚並べても情報が増えなかったため、2枚目は元写真の実画素からサイドウォールだけを拡大。**画像は枚数ではなく、その節の主張を証明できるか**で選ぶ。ひび割れなど状態証拠は生成画像で補わず、見えない場合は追加撮影を依頼する。
- 状態証拠の接写は「タイヤ全体が分かる」より「異常箇所が画面の大半を占める」を優先する。サイドウォールのひび割れでは外周部を正方形に近く部分拡大し、元写真の実画素だけで見せる。
- 記事構成は公開済み記事の共通型（結論3行→実体験→比較→イチオシ→手順→注意点→費用対効果→おすすめ／不要→最終判断）へ統一。元ブリーフの5銘柄比較は戻さず、実体験に直結する**mp7対RV505の2本比較**だけを前半へ配置した。
- 比較表はスマホで4列にすると項目名が1文字ずつ折り返された。**「項目／製品A／製品B」の3列＋判断ポイントを箇条書き**に分けると、情報量を保ったまま読みやすい。
- 楽天アフィリエイトはログイン済みChromeでmp7・RV505の商品別URLを生成し、楽天ボタンだけを表示。交換チケットの汎用リンクは、別ショップ品の誤購入を招くため置かず、選んだタイヤの商品ページから同一ショップのチケットを選ぶよう案内する。
- タイヤの節約額は「タイヤ本体価格」だけで出さず、**本体＋交換チケット＋ゴムバルブ＋廃タイヤ処理**で再計算する。変動費は相場レンジと、店舗見積もりと同額でそろえた比較の両方を出し、ポイント・クーポンは含めない。今回の再計算では、楽天側57,280〜62,180円、同条件59,680円、店舗との差15,920円になった。
- タイヤ記事の楽天導線は、特定ショップの商品ページだけでなく、楽天公式の「URLからリンク作成」で作った**サイズ検索結果へのアフィリエイトリンク**を使える。読者がブランド・ショップ・送料・製造年を横断比較できる一方、交換チケットは選択したタイヤと同じショップで買う注意書きを残す。検索結果へ送るカードは、購入ボタンと誤認されないよう `rakuten_label: 楽天市場で価格比較` を指定する。
- C26タイヤ交換記事のアイキャッチは、実車タイヤ写真を土台に、本文で同条件比較した**「店舗より15,920円安い」**を最大文字として配置した。実体験記事では生成した架空車両よりも本人撮影写真を残し、数字・比較対象・購入方法の3要素だけに絞ると、記事内容との一致とスマホ可読性を両立できる。
- アイキャッチの文字は画像端から約7%のセーフマージンを確保する。プレビューでは角丸とレスポンシブ縮小で端の文字が見切れやすいため、特に最大数字は左端に寄せず、見出し・数字・下部バッジの左基準をそろえる。**生成画像がすでに16:9に近い場合は幅を固定して再クロップしない**。今回1672×941を1536×864へ中央クロップしたことで左右68pxずつ失われ、せっかくの余白が消えた。
- C26タイヤ記事の楽天アフィリエイトカードは、**結論3行の直後とまとめの購入判断直前の2か所**に配置した。中盤の重複カードは外し、どちらも特定ショップではなく195/60R16の楽天市場横断検索へ送り、直後に「交換チケットは同じショップ」の注意点を残す。

---

## 🧮 ROI計算ツールのPC可読性改善（2026-08-26）
- フォーム型ツールはPCだから2列にする必要はない。入力と結果を左右へ分けると視線移動が増えるため、**最大幅880px・入力1列・結果は入力の下**に統一するとスマホと同じ読み順を保てる。
- 本文17px、ラベル16px、補足15px以上、入力欄約53px、主要ボタン約55pxを基準にすると、JIN:R本文領域でも読みやすさと操作性を両立できた。
- 消耗品費は「1回」だけでは使用回数か交換回数か不明確になる。**「交換1回あたり費用」「年間の交換回数」＋具体例**をセットで出す。
- WordPressのHTMLブロック再入稿後は、PCと390px・320pxの本番実URLで、横幅、文字化け、代表計算、コンソールエラーを必ず再確認する。今回の代表計算は（750円−350円）×12回＝年間4,800円。

## ✨ ROI計算ツールの看板CTA改善（2026-08-26）
- 最大の売りを示すCTAは、濃色背景と強い煽り文句だけでは怪しく見える。**明るい背景・濃紺文字・控えめな影・具体的な入力例**の組み合わせが、専門性と安心感を同時に伝える。
- 「厳密に計算」よりも、読者の意思決定を助ける**「買う前に、『元が取れるか』を数字で確認」**の方が、押しつけず価値を伝えられる。
- 実際に料金がかからなくても、**「無料」という言葉があるだけで売り込み・勧誘を連想させ、胡散臭く見える読者がいる**。ROI計算ツールの看板CTAでは「無料」を訴求せず、登録不要・入力非保存・ブラウザ内計算・根拠公開という検証可能な事実だけを示す。
- CTA内に実際の入力例（価格・回収期間・年間価値）を出すと、クリック前に「何が分かるか」を理解できる。例は実際の初期値と計算結果に一致させる。
- スマホの主ボタンは一行で読める長さにする。「条件を入れて計算してみる」は390px・320pxとも2行になったため、**「条件を入れて計算」**へ短縮し、white-spaceを固定して56px高を維持した。

## 🧾 ROI計算ツールの説明文を日常語へ統一（2026-08-26）
- 「買わない場合の費用」のように、計算者には意味が通じても読者が入力場面を想像できないラベルは使わない。**何が増減する費用か・一度きりか毎年か・なければ何を入れるか**まで書く。
- 入力欄の説明は「定義」ではなく、**具体例＋迷ったときの初期値**を示す。例：修理代や代替商品／なければ0円、活用割合／迷ったら60%、電気料金／分からなければ31円。
- 「割引率」「正味価値」「純利益」「損益分岐」は一般読者には理解の負担が大きい。内部計算は維持し、画面上では「将来の効果を控えめに見る設定」「差し引き効果」「1年間に得られる価値」「元が取れるまで」と表現する。
- 320pxでは長いボタン文言と結果2列が窮屈になる。ボタンは**「計算結果を見る」**、350px以下の結果は1列にすると、横幅187pxでも1行・約55px高を維持できる。

## 📤 ROI計算結果の共有導線（2026-08-27）
- Web上で共有ボタンを並べる場合、**リンクを押しただけで送信・投稿されない**ことを共有欄の直下で明記すると、操作への不安を減らせる。
- LINE・メール・Xは結果本文と正規URLを作成画面へ渡す。Instagramは一般的なWebリンクから投稿本文を直接入れられないため、**端末標準の共有画面を優先し、使えない場合は文章＋URLのコピーへ切り替える**。
- 共有先の名前が長いと、スマホ2列では片方のボタンだけが複数行になり、同じ行の短いボタンまで縦長になる。**440px以下は1列、PCは2列**にすると読みやすく、320pxでも横にはみ出さない。
- クリップボードは端末やブラウザの制約を受ける。`navigator.clipboard`を優先し、利用できない環境には一時テキスト欄を使うコピー処理を用意する。
