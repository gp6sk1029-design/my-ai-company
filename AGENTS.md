# AGENTS.md — Codex（および全AIエージェント）向け案内

> このファイルはCodexが自動で読む指示ファイル。**ルール本体はここに書かない**（重複管理を避けるため正本は1つ）。

## 🎯 最優先：作業前に CLAUDE.md を読むこと

このプロジェクト「my-ai-company（専用AI会社）」の**全ルールの正本は `CLAUDE.md`**。
作業前に必ず `CLAUDE.md` を読み、その指示に従うこと（5部門体制・モード・AI操作方針・データ蓄積ルール・セッション管理）。

## あなた（Codex）の立ち位置

- あなたはClaude（司令塔）から **専門家** として呼ばれる：コードレビュー（`codex review`）・壁打ち/調査（`codex exec`）・小タスクの実装（`codex exec`）。
- あなたの出力は**Claudeが検証してから採用される**前提。スコープを厳守し、関係ないファイルは触らない。

## 知識の保存先（Claudeと共通）

- 手順・ルール → 各部門の `SKILL.md`
- 学び・実績・台帳 → 各部門の `MEMORY.md`
- 一度きりの成果物 → `reports/`
- 何をどこに保存したかは、CLAUDE.md「💾保存しました」フォーマットで報告する

## やってはいけないこと

`CLAUDE.md`「やってはいけないこと」と `~/.codex/AGENTS.md` のインフラ固定ルールに従う：
- APIキー・パスワード・トークンを直書きしない（環境変数/Secretsへ）
- 個人情報・未公開情報・機密をコミットしない／リポジトリはPrivate維持
- 本業（work-projects）のファイルをこの副業リポジトリに置かない
