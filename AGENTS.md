# AGENTS.md — Codex（および全AIエージェント）向け案内

> このファイルはCodexが自動で読む指示ファイルです。**ルール本体はここに書きません。**
> 重複管理（ドリフト）を防ぐため、正本は1つに集約しています。

## 🎯 最優先：作業前に CLAUDE.md を必ず読むこと

このプロジェクト「my-ai-company（専用AI会社）」の**全ルールの正本は `CLAUDE.md`** です。
作業を始める前に必ず `CLAUDE.md` を読み、その指示に完全に従ってください。
（5部門体制・モード判定・AI操作方針・データ蓄積ルール・セッション管理がすべて書かれています）

全体像は `🏠 HOME.md` が地図になっています。

## 📂 知識の保存先（Claudeと共通）

- 作業の**手順・ルール** → 各部門の `SKILL.md`
- **学び・実績・失敗パターン・台帳** → 各部門の `MEMORY.md`
- **一度きりの成果物** → `reports/`
- **次セッションへの申し送り** → `handover/`
- どこに何を保存したかは、CLAUDE.md「💾保存しました」フォーマットでユーザーに必ず報告する

## 🔁 セッション同期（自動）

- セッション終了時の自動コミット・push・**競合マーカー検査**は `.codex/hooks.json` が担います
- 開始時に `git pull` で最新を取得します

## ⚠️ やってはいけないこと

`CLAUDE.md`「やってはいけないこと」と `~/.codex/AGENTS.md` のインフラ固定ルールに従うこと。要点：
- APIキー・パスワード・トークンをソースコードに直書きしない（環境変数/Secretsへ）
- 個人情報・未公開情報・機密をコミットしない／リポジトリはPrivate維持
- 本業（work-projects）のファイルをこの副業リポジトリに置かない

## 🤝 ClaudeとCodexの共存ルール

- ClaudeもCodexも全ファイルを編集してよい（役割固定なし）
- 🔴 ただし **同時刻に同じ作業で並走させない**（順番に使う）。
  開始時の `git pull` と終了時の競合マーカー検査で、逐次運用なら競合事故を防げる
