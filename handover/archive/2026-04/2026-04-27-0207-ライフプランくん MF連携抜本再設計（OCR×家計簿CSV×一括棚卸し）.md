# 引き継ぎ書 - 2026-04-27-0207

- **トピック**: ライフプランくん MF連携抜本再設計（OCR×家計簿CSV×一括棚卸し）
- **セッションID**: `fa158a35-af3b-4f16-8bcb-7994cf21adfc`
- **健康状態**: OK
- **規模**: 1.26MB / 画像0枚 / ユーザー入力9回

---

## 🎯 このセッションでやったこと（要約）

### 背景
ユーザー報告：「マネーフォワードのCSV情報は家計簿の情報しか登録できないので現状ツールではつかえない。抜本的に変更が必要、最適アイデアを提案して」

→ 既存の **資産タブ「MF資産CSV取込」機能は前提崩壊**（MFは資産残高CSVを出さない／家計簿CSVのみ）。本番URL `https://life-plan-kun.pages.dev/` のドロップゾーンが空振りしていた。

### 採用した再設計（ハイブリッド3方式）

| 方式 | 配置 | 内容 |
|---|---|---|
| 📸 **資産スクショOCR取込** | 資産タブ | MFアプリ資産画面のスクショを Tesseract.js（jpn+eng）で端末内OCR → 金額抽出 → 差分プレビュー → 反映 |
| 🔄 **一括棚卸しモード** | 資産タブ | OCR失敗時の現実解。全口座を1モーダルで順入力、差分（±円/±%）を即時表示 |
| 📥 **家計簿CSV取込** | 収支タブ | MF家計簿CSVを `計算対象=1 ∧ 振替=0 ∧ 金額<0` で抽出 → 大項目×月で集計 → 直近3ヶ月平均で `expense[]` を実績ベースに更新 |

### 変更ファイル一覧（パスは `tools/life-plan/` 配下）

- ✏️ **public/index.html**：旧 `#mf-drop`〜`#mf-apply` ブロック撤去 → `#ocr-drop` / `#ocr-apply` / `#kakeibo-drop` / `#kakeibo-apply` / `#btn-inventory-mode` を追加
- ✏️ **public/app.js**：`parseMfCsv` / `handleMfFile` / `mf-apply` 系を完全削除 → `handleAssetScreenshot` / `extractBalancesFromOcr` / `classifyAssetLine` / `openInventoryMode` / `parseKakeiboCsv` / `summarizeKakeibo` / `renderKakeiboMapping` を新設。ヘルプモーダルのMF連携セクションも書き換え
- ✏️ **public/styles.css**：`.ocr-status` / `.inventory-row` / `.expense-map-row` / `.diff-delta` を追加
- ✏️ **MEMORY.md**：「2026-04-25 MF連携を抜本再設計」セクション追記、旧の楽観的成功パターンを失敗パターンとして記録
- ✏️ **SKILL.md**：禁止事項に「外部サービスの機能を事実確認せずに実装しない」を追加。トリガー表のMF項目を新仕様に更新
- ✏️ **README.md**：MF連携の3方式を明記
- 🗑️ **functions/api/mf-import.js**：完全削除（資産CSVパース前提の死にコード）
- 🔁 **`mfSnapshots` ストア**：削除せず OCR履歴保管用に転用

### 検証結果
- ✅ app.js 構文OK
- ✅ 単体テスト 18/18 通過（`parseKakeiboCsv` 食費5000円・通信費5000円の3ヶ月平均、`extractBalancesFromOcr` の4種kind分類、ノイズ1000円未満除外、振替=1除外、収入>0除外）
- ✅ ローカルサーバー（python3 http.server 8791）で全アセット HTTP 200
- ✅ 旧コード（mf-drop / parseMfCsv / handleMfFile）残存ゼロ
- ⚠️ Tesseract.js 実機OCR精度はまだ実測していない（次セッション課題）

### プラン文書
- 元プラン：`/Users/shoheikoda/.claude/plans/csv-goofy-salamander.md`（承認済み・実装完了）

---

## ⏭️ 次セッションで残っているタスク

1. **本番デプロイ**（最優先）
   ```bash
   cd /Users/shoheikoda/Documents/my-ai-company/tools/life-plan
   npx wrangler pages deploy public --project-name=life-plan-kun --commit-dirty=true --branch=main
   ```
   現在 https://life-plan-kun.pages.dev/ は旧コード（前提崩壊版）が稼働中。

2. **実機OCR精度測定**（任意）
   - iPhoneでMFアプリ資産画面スクショを撮影 → ライフプランくんで取込 → 抽出精度を確認
   - 精度不足ならキーワード辞書（`classifyAssetLine`内の正規表現）を拡充

3. **証券CSV直接取込**（将来課題・今回見送り）
   - SBI証券・楽天証券の残高CSV対応。ユーザーが両方使用中と確認済み
   - 設計上 `handleAssetScreenshot` と並列のエントリポイントとして追加可能

---

## 🛡 重要な禁止事項（MEMORY.mdから抜粋・必ず守る）

- 外部サービス（MF/SBI/楽天等）の機能を事実確認せずに実装しない。「資産CSVが出る」前提のような誤認は本番UI空振りに直結する
- OCR結果は必ず差分プレビューを挟み、抽出失敗時は手動フォールバック（一括棚卸しモード）に誘導すること

---

## 直近のユーザー指示（古い順）

1. 記事めしツールを改善したい

2. アップデートロードするときに どんな記事にするか判断させるために、AIが記事を作成するときに方向を示すためのメモ機能を追加したい
   まず、ドロップダウン、レビューなのか、商品比較なのか、ツール紹介などか選べるようにしてほしい ドロップダウンの内容に関しては、あとから自分で追加できるようにして、そして、読者に伝えたいポイントなどのメモ を入力できる箇所を優先度順にいくつかメモ欄を追加できる仕組みにして
   そして、記事を書く際はそのメモの指示に従った内容にしてほしい、もしメモがなにもない場合はいままでと同じ方式で記事を作成してほしい

3. [Request interrupted by user]

4. 写真をアップロードするときに写真のフォルダの中に、 どんな記事にするか判断させるために、AIが記事を作成するときに方向を示すためのメモファイル(プロンプト)を生成してほしい、
   まず、ドロップダウン、レビューなのか、商品比較なのか、ツール紹介などか選べるようにしてほしい ドロップダウンの内容に関しては、あとから自分で追加できるようにして、そして、読者に伝えたいポイントなどのメモ を入力できる箇所を優先度順にいくつかメモ欄を追加できる仕組みにして
   そして、記事を書く際はそのメモの指示に従った内容にしてほしい、もしメモがなにもない場合はいままでと同じ方式で記事を作成してほしい

5. Continue from where you left off.

6. 現在どんな状態？

7. 進めて

8. 残りして

9. 引き継ぎの準備して

---

## 直近3時間以内に変更されたファイル

- `tools/ec/data/mercari_chrome/GraphiteDawnCache/data_1`  (02:06)
- `tools/ec/data/mercari_chrome/GrShaderCache/data_1`  (02:06)
- `tools/ec/data/mercari_chrome/ShaderCache/data_1`  (02:06)
- `ec/data/mercari_chrome/Local State`  (01:59)
- `CLAUDE.md`  (01:32)
- `tools/handover.py`  (01:18)
- `tools/session_health.py`  (01:17)

---

## 直近のGitコミット

```
1d23aa3 auto-sync: 2026-04-27 02:00
8faf7d9 セッション容量監視＋引き継ぎ自動化システムを追加
48949bc auto-sync: 2026-04-26 03:43
e61a3ce auto-sync: 2026-04-26 03:34
0ef4500 3セクション体制に再構成（PDM・blog・tools）
```

---

## 🚀 復帰用プロンプト（新セッションでコピペ）

```
前セッションの引き継ぎを行います。以下のファイルを必ず読んでから作業を再開してください。

1. handover/2026-04-27-0207-ライフプランくん MF連携抜本再設計（OCR×家計簿CSV×一括棚卸し）.md  ← 引き継ぎ書（必読）
2. tools/life-plan/SKILL.md ← ライフプランくんの設計原則・禁止事項
3. tools/life-plan/MEMORY.md ← 過去の学び（特に「2026-04-25 MF連携抜本再設計」セクション）
4. .claude/plans/csv-goofy-salamander.md ← 承認済み実装プラン（実装完了）

最優先タスクは本番デプロイ：
  cd tools/life-plan && npx wrangler pages deploy public --project-name=life-plan-kun --commit-dirty=true --branch=main

不明点があれば質問してから着手してください。
```

---

## 関連リソース

### このセッションで触ったプロジェクト（ライフプランくん）
- ルート: `tools/life-plan/`
- ルール: `tools/life-plan/SKILL.md`
- 学び: `tools/life-plan/MEMORY.md`
- プラン: `.claude/plans/csv-goofy-salamander.md`
- 本番: https://life-plan-kun.pages.dev/  ← **未デプロイ（旧版稼働中）**
- 起動: `cd tools/life-plan && python3 -m http.server 8791 --directory public` → http://localhost:8791

### 全社共通
- ルール: `CLAUDE.md`
- 部門別: `blog/SKILL.md` / `ec/SKILL.md`
- 学習: `blog/MEMORY.md` / `ec/MEMORY.md`
